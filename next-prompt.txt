═══════════════════════════════════════════════════════════════════════════════
⚠️  S67 FINAL COMPLETE (2026-05-21) — AUTHORITATIVE READ FIRST
═══════════════════════════════════════════════════════════════════════════════

**FIRST ACTION FOR NEXT SESSION**:

1. Read `memory/project_s67_final_handover.md` BEFORE anything else. This is the
   single comprehensive handover for the entire S67 arc (worker scaling + Track B
   rejection + architectural ceiling + user decisions + reinforced principles).
2. Then check Exp 1m iter 1 result on prod: `tail /tmp/exp1m_48w_pool12.log`.
   - If iter 1 update wall ≤ 200s and no OOM → ship `--mp-workers 48 --pool-anchors 11`.
   - If OOM or update > 200s → fall back to `--mp-workers 30` at pool=15.
3. ALL prior wraps below (S67-mid, S66, S65) are SUPERSEDED by this header + the
   final handover memo. Don't act on them as authoritative.

═══════════════════════════════════════════════════════════════════════════════
S67 FINAL SUMMARY — what we measured, what shipped, what's REFUTED, principles
═══════════════════════════════════════════════════════════════════════════════

**SESSION OUTCOME**: Phase 2 wall reduced from 32 min/iter (8w baseline) to
**~18.5 min/iter (48w + pool=12)** at user's diversity target. ~$62 saved per
200-iter Phase 2 (~$93/run vs $155 baseline). Two flag changes; no code change.
Zero quality compromise.

═══════════════════════════════════════════════════════════════════════════════
CANONICAL Phase 2 LAUNCH (UPDATED — post-Exp 1m)
═══════════════════════════════════════════════════════════════════════════════

```bash
# PRE-LAUNCH (mandatory):
# 1. Verify Exp 1m iter 1 result: tail /tmp/exp1m_48w_pool12.log
# 2. Cleanup if any python running:
#    pkill -9 python; sleep 5; pkill -9 -f battle_server.js; sleep 5
#    for p in 9000 9001 9002 9003 9004 9005 9006 9007; do
#      nohup node battle_server.js --port $p > /tmp/bs_$p.log 2>&1 &
#    done; sleep 10
# 3. Verify torch 2.5.1+cu121 + LD_LIBRARY_PATH on prod

cd /workspace/team_builder/pokemon-ai-starter/pokemon-ai/src
nohup ./launch_rl.sh \
  --init-from data/models/bc/v10_padded_for_cis_dev.pt \
  --bc-anchor-ckpt data/models/bc/v10_padded_for_cis_dev.pt \
  --bc-anchor-coef 0.1 \
  --cis --bf16 --tier3 --tier3-minibatch-size 64 \
  --packed --no-per-chunk-gc \
  --cis-min-batch 32 --cis-timeout-ms 50 \
  --mp-workers 48 \                              # ← S67 NEW SHIP (was default 8)
  --pool-anchors 11 \                            # ← 11 anchors + init = 12 active opps
  --games-per-iter 1600 --max-concurrent 200 \
  --n-iters 200 --warmup-iters 10 \              # ← USER DECISION: 10 warmup iters
  --lr 1e-5 --lam 0.95 --ent-coef 0.02 --target-kl 0.03 \
  --grad-accum 1 --turn-cap 300 \
  --reward-style terminal \
  --procedural-teams /workspace/raw_data/pokemon_usage/2024-04 \
  --adaptive-entropy --adaptive-entropy-low 0.65 --adaptive-entropy-high 0.95 \
  --eval-interval 10 --eval-games 200 --eval-team-set metamon-competitive \
  --snapshot-interval 10 \                        # ← USER DECISION: 5-10 OK; 10 default
  --early-stop --early-stop-patience 3 \
  --win-rate-mode ema --win-rate-ema-alpha 0.3 --win-rate-ema-window 50 \
  --out-dir data/models/rl_v10/<RUN_NAME> \
  > /tmp/<RUN_NAME>.log 2>&1 &
```

**Fallback if Exp 1m iter 1 fails**: drop `--pool-anchors 11`, change to
`--mp-workers 30`. Pool defaults to 15 active. 23.7 min/iter → $115/run.

═══════════════════════════════════════════════════════════════════════════════
USER DECISIONS (2026-05-21) — DON'T RE-LITIGATE
═══════════════════════════════════════════════════════════════════════════════

| Setting | Decided value | User rationale |
|---|---|---|
| Total games/iter | 1600 | Matches ps-ppo/VGC-Bench rollout size. Don't reduce. |
| Active opps/iter target | **10-12** | Stable PFSP weights + ample diversity. 133-160 g/opp. |
| Snapshot interval | **5-10 iters** | Frequent refresh, low rotation overhead. |
| Warmup iters | **10** | Stabilize value head before main training. |
| Acceptable per-iter wall | "<10 min ideal, otherwise architectural floor" | Below 13-14 min needs multi-GPU. ~18.5 min is best practical without architecture change. |
| Phase 2 budget per run | $93 (48w/pool=12) / $115 (30w fallback) | Acceptable. |

═══════════════════════════════════════════════════════════════════════════════
RATIO CURVE (CONCRETE measurements — pool=15 operating point + diagnostics)
═══════════════════════════════════════════════════════════════════════════════

At pool=15 (user diversity floor):
| Workers | Active | Ratio | Wall avg |
|---|---|---|---|
| 8 | 8 | 1:1 | 32 min (baseline) |
| 15 | 15 | 1:1 | 29 min |
| 24 | 15 | 1.6:1 mixed | 25.5 min |
| **30** | **15** | **2:1 clean** | **21.2 min** ← fallback ship |
| 48 | 15 | 3.2:1 mixed | **OOM (mb=64 update)** |

At pool=12 (Exp 1m, NEW SHIP):
| Workers | Active | Ratio | Wall |
|---|---|---|---|
| **48** | **12** | **4:1 clean** | **18.0 min iter 0 / ~18.5 steady projected** ← SHIP |

At pool=8 (diagnostic only — below user diversity floor):
| Workers | Active | Ratio | Wall |
|---|---|---|---|
| 8 | 8 | 1:1 | 31 min |
| 24 | 8 | 3:1 | 17.6 min |
| 32 | 8 | 4:1 | 15.9 min |
| 48 | 8 | 6:1 | 15.4 min |

═══════════════════════════════════════════════════════════════════════════════
ARCHITECTURAL CEILING — per-worker CUDA context
═══════════════════════════════════════════════════════════════════════════════

**Each Python worker process consumes ~490 MB GPU memory** (PyTorch CUDA primary
context + lazy-loaded cuBLAS + cuDNN). Loaded the moment any worker touches CUDA,
lives for process lifetime, can't be released.

| Workers | Worker GPU | Update budget | mb=64 fit? |
|---|---|---|---|
| 8 | ~4 GB | ~73 GB | ✓ |
| 30 | ~15 GB | ~62 GB | ✓ tight |
| 48 + pool=15 | ~24 GB | ~53 GB | ❌ OOM (Exp 1k confirmed) |
| **48 + pool=12** | **~24 GB** | **~54 GB** | **✓ (Exp 1m confirmed iter 0)** |

To push past 48 workers needs **CPU-only-workers refactor** (~50-150 LOC). NOT urgent.

═══════════════════════════════════════════════════════════════════════════════
CONCLUSIVELY REFUTED in S67 — do NOT reopen without new evidence
═══════════════════════════════════════════════════════════════════════════════

1. **Per-worker concurrency confounder** (Exp 1g: 8w + conc=53 = 32 min baseline)
2. **Battle server contention at 30w** (Exp 1f: 8 BS = 30 BS = 21.2 min)
3. **Pool size capping as wall optimization** (Exp 5a: pool=8 = pool=15 active=8)
4. **Active-opps-per-iter cap as wall fix** — user rejected (handicap to PFSP diversity)
5. **S66 mp.send mechanism conjecture** — stage profile showed dispatch = 0.1-3%
6. **Track A (waves)** — math hole, ratio at wave_size=K = ratio at pool=K, no escape
7. **Track B (shared backbone freeze)** — REJECTED on principle, see
   feedback_dont_propose_principle_violations.md. Caps model ceiling; quality non-negotiable.
8. **Diminishing returns past 3:1** — earlier S67 framing, REFUTED by Exp 1j (6:1 ratio
   continued scaling at -14% from 4:1)
9. **Larger minibatch (80/96/128)** — OOM at 30+ workers (worker contexts eat GPU)

═══════════════════════════════════════════════════════════════════════════════
PRINCIPLES (reinforced, re-read every session)
═══════════════════════════════════════════════════════════════════════════════

These are not aspirations — they are how we work. **Past sessions failed when we
drifted from these.** S67 itself drifted into Track B (freeze spatial) before user
caught it on principle. Don't repeat.

1. **No shortcuts. Ever.** No `.float()` shims. No "fix later" TODOs in critical path.
   No skipping smoke validation. No half-finished branches. No bundling scope. The
   "good enough for now" version is the version that needs to be touched again.

2. **No assumptions. Label CONCRETE / STRONG INFERENCE / CONJECTURE.** Every perf
   claim, every "X is the bottleneck", tag with evidence or mark conjecture. S66's
   mp.send framing was conjecture-promoted-to-fact; cost weeks. S67 stage profile
   refuted it in $5.

3. **Quality > cost > calendar. Quality is NON-NEGOTIABLE.** Quality concessions are
   NOT validatable tradeoffs. Don't propose them. Track B was the canonical example
   in S67 — would have capped model ceiling for ALL future learning. User had to push
   back twice before I retracted. **If a proposal violates a stated principle, push
   back yourself instead of laying out options.**

4. **Long-term overarching goal is the load-bearing context for EVERY decision**:
       Phase 1 v3 (DONE) → ELITE GEN 9 (NEXT) → MULTI-GEN → all formats.
   Every decision must serve this trajectory. **Anything shipped must be extendable**
   to the next phase without rework. Don't ship gen-9-specific hacks multi-gen has
   to undo. Don't ship single-pod assumptions multi-GPU has to refactor.

5. **It's me and you against the problem.** Partnership, not deference. Push back
   on user when premise is buggy. Also push back on yourself — if your proposed
   framing has a principle-bug, the same rule applies.

6. **Verify before recommending OR acting.** Memory snapshots are point-in-time, not
   current state. Grep before promising. SSH exit codes can lie. Cheap to verify;
   expensive to act on wrong state.

7. **Diagnostic code on isolation branch; findings/scripts on master via cherry-pick.**
   S67 pattern: `diag/cis-stage-profile` held the perf patches; `perf/freeze-spatial`
   held the rejected scaffold; docs/memory went to master via separate commits.

8. **Take inherited content with a grain of salt — confirm against current code.**
   Older docs, older memos, older session wraps can contain framings correct at the
   time but superseded later. Current code is truth. Newer findings override older
   speculation.

═══════════════════════════════════════════════════════════════════════════════
NEW S67 MEMORY FILES (auto-loaded via MEMORY.md)
═══════════════════════════════════════════════════════════════════════════════

- **project_s67_final_handover.md** — THIS arc's authoritative one-file handover (read first)
- project_s67_worker_scaling_findings.md — detailed experiment data
- feedback_dont_propose_principle_violations.md — Track B rejection lesson (S67)
- feedback_battle_server_restart_after_kill.md — BS cleanup recipe (cost ~$2 to learn)
- feedback_freeze_spatial_use_cases.md — when freeze IS a legitimate scaffold lever

═══════════════════════════════════════════════════════════════════════════════
S67 BRANCHES (preserved, NOT merged to master)
═══════════════════════════════════════════════════════════════════════════════

- `diag/cis-stage-profile` @ 668df0f8 — per-fire stage profile + fwd internal split.
  CIS_STAGE_PROFILE env var. Pure additive, default-off. Useful for any future CIS
  perf investigation.
- `perf/freeze-spatial` @ 671e19e4 — --freeze-spatial flag. REJECTED for Phase 2 use
  on principle (caps model ceiling). KEEP available for TEMPORARY scaffold uses per
  feedback_freeze_spatial_use_cases.md (Phase 2 launch anti-erosion, multi-gen
  transfer, instability recovery, hyperparameter isolation).

═══════════════════════════════════════════════════════════════════════════════
PHASE 2 PREP TODO (separate sessions, no architectural changes left)
═══════════════════════════════════════════════════════════════════════════════

1. **Validate Exp 1m iter 1 result** before launching production Phase 2.
2. Source 100-150 real elite gen-9-OU teams (NEVER mix with 16 mm-competitive eval set).
3. Wire H2H gauntlet (Kakuna/SmallRL/Minikazam adapters).
4. External opps in PFSP pool (mcts-fast + others).
5. lr re-ablation post-S67 arc to confirm `--lr 1e-5` still optimal.
6. Phase 2 launch script with the updated canonical stack.

═══════════════════════════════════════════════════════════════════════════════
ANTI-PATTERNS THIS ARC REVEALED — DON'T REPEAT
═══════════════════════════════════════════════════════════════════════════════

1. **Killing experiments prematurely** — Exp 1 (15w pool=15) was killed at +22 min
   on n_done=0; turned out to be slow-start regime and would have completed in ~29
   min. User caught the error. Pool=15 collect is slow to start showing n_done —
   don't conclude failure before normal completion time.

2. **Forgetting documented lessons mid-session** — Battle server cleanup recipe was
   documented in this same session but I skipped it before launching Exp 1, causing
   chained failures. When launching ANY new experiment, run the cleanup recipe first.

3. **Extrapolating without verifying** — projected pool=8 wall as ~20 min linearly
   from pool=1-5 data; actual measurement (Exp 5a) was 31 min. Wall scales NON-linearly
   with active opps. When projecting outside measurement range, MEASURE before
   recommending.

4. **Soft-pedaling principle violations as tradeoffs** — Track B was framed as
   "tradeoff to validate" when it was actually a structural quality compromise.
   User had to push back twice before I retracted. If a proposal violates a stated
   principle, push back yourself.

5. **Skipping cheap diagnostics in favor of expensive architectural proposals** —
   Stage profile patch (~$3-5) refuted multi-session S66 framing. Should have
   measured per-fire breakdown sessions ago.

═══════════════════════════════════════════════════════════════════════════════
POD STATE AT SESSION WRAP (2026-05-21)
═══════════════════════════════════════════════════════════════════════════════

- Prod (port 47913): Exp 1m running. 51 python procs. GPU 27 GB used / 54 GB free.
  Iter 0 SHIPPED. Iter 1 in flight in collect at handover time.
- Battle servers: 8 running on 9000-9007 from earlier launch
- HEAD on prod: master (verify before launch — `git rev-parse HEAD`)
- Local HEAD: master (S67 consolidation commit pending)

**Cumulative S67 cost**: ~$76 pod time. Pays back in ~1.2 Phase 2 runs.

═══════════════════════════════════════════════════════════════════════════════

═══════════════════════════════════════════════════════════════════════════════
S67 SUMMARY — what we measured, what shipped, what's REFUTED
═══════════════════════════════════════════════════════════════════════════════

**SESSION OUTCOME**: Phase 2 wall reduced from 32 min/iter (8w baseline) to
21.2 min/iter (30w/2:1 ratio) at pool=15. ~$50 saved per 200-iter Phase 2.
ONE flag change. Zero quality compromise. Zero code change.

**Canonical Phase 2 launch (UPDATED)**:
```bash
./launch_rl.sh \
  --init-from data/models/bc/v10_padded_for_cis_dev.pt \
  --bc-anchor-ckpt data/models/bc/v10_padded_for_cis_dev.pt \
  --bc-anchor-coef 0.1 \
  --cis --bf16 --tier3 --tier3-minibatch-size 64 \
  --packed --no-per-chunk-gc \
  --cis-min-batch 32 --cis-timeout-ms 50 \
  --mp-workers 30 \                  # ← S67 CHANGE (default was 8)
  --games-per-iter 1600 --max-concurrent 200 \
  --n-iters 200 --warmup-iters 0 \
  --lr 1e-5 --lam 0.95 --ent-coef 0.02 --target-kl 0.03 \
  --grad-accum 1 --turn-cap 300 \
  --reward-style terminal \
  --procedural-teams /workspace/raw_data/pokemon_usage/2024-04 \
  --adaptive-entropy --adaptive-entropy-low 0.65 --adaptive-entropy-high 0.95 \
  --eval-interval 10 --eval-games 200 --eval-team-set metamon-competitive \
  --snapshot-interval 10 \
  --early-stop --early-stop-patience 3 \
  --win-rate-mode ema --win-rate-ema-alpha 0.3 --win-rate-ema-window 50 \
  --out-dir data/models/rl_v10/<RUN_NAME>
```

**Full ratio curve measured at pool=15 (the operating point)**:
| Workers | Active opps | Ratio | Wall avg |
|---|---|---|---|
| 8 | 8 | 1:1 | 32 min (baseline) |
| 15 | 15 | 1:1 | 29 min |
| 24 | 15 | 1.6:1 mixed | 25.5 min |
| **30** | **15** | **2:1 clean** | **21.2 min** ← SHIP THIS |
| 30 + 30 BS | 15 | 2:1 | 21.2 min (BS scaling = noop) |

**Conclusively REFUTED in S67** (do NOT reopen unless new evidence):
1. Per-worker concurrency confounder (Exp 1g: 8w + conc=53 = 32 min, same as baseline)
2. Battle server contention as bottleneck at 30w (Exp 1f: 8 BS = 30 BS = 21.2 min)
3. Pool size capping as wall optimization (Exp 5a: pool=8 = pool=15 active=8 = 31-32 min)
4. Active-opps-per-iter cap as wall fix (user rejected — handicap to PFSP diversity)
5. S66 mp.send mechanism conjecture (S67 stage profile: dispatch is 0.1-3% of per-fire wall)
6. Track A (waves) — math hole, ratio at wave_size=K = ratio at pool=K, no escape
7. Track B (shared backbone with freeze) — REJECTED on principle, see
   feedback_dont_propose_principle_violations.md

**REMAINING optional optimizations** (in priority order if more wall reduction needed):
- Exp 2: numpy_dict_to_torch H2D opt — projected 5-7% additive at active=15.
  Real code change, quality-preserving. 1 session.
- Exp 1i: 45 workers at pool=15 (3:1 clean). Untested. Projected ~10% further
  gain (~19 min/iter). Diminishing returns past 2:1-3:1 (4:1 only gave -10%
  vs 3:1 at pool=8). ~$5-7.
- Multi-GPU evaluation. Not needed at current scale; preserved as option for
  future.

**Phase 2 prep TODO** (separate sessions, no architectural changes left):
- Source 100-150 real elite gen-9-OU teams (NEVER mix with 16 mm-competitive
  eval set)
- Wire H2H gauntlet (Kakuna/SmallRL/Minikazam adapters)
- External opps in PFSP pool (mcts-fast + others)
- Phase 2 launch script with the updated canonical stack
- lr re-ablation post-optimization arc (S64 reason)

**S67 NEW MEMORIES** (all auto-loaded via MEMORY.md):
- project_s67_worker_scaling_findings.md — THIS arc's authoritative memo
- feedback_dont_propose_principle_violations.md — Track B rejection lesson
- feedback_battle_server_restart_after_kill.md — BS cleanup recipe (cost ~$2)
- feedback_freeze_spatial_use_cases.md — when freeze IS a legitimate lever

**S67 NEW BRANCHES** (preserved, NOT merged to master):
- `diag/cis-stage-profile` @ 668df0f8 — stage profile + fwd internal split
  diagnostic patches. CIS_STAGE_PROFILE env var. Pure additive, default-off.
- `perf/freeze-spatial` @ 671e19e4 — --freeze-spatial flag (REJECTED for
  Phase 2 use; kept available for legitimate scaffold use cases per memo)

**Cumulative S67 cost**: ~$60-65 pod time. Pays back in ~1.5 Phase 2 runs.

**Pod state at session wrap (2026-05-20)**:
- Prod (port 47913): IDLE after Exp 1h cleanup
- 8 battle servers running on 9000-9007 (restart recommended before next prod run)
- GPU: ~1 MiB allocated (clean)
- HEAD on prod: whatever branch we're on (verify before launch)

═══════════════════════════════════════════════════════════════════════════════

pre prompt :

═══════════════════════════════════════════════════════════════════════════════
⚠️  S65 ARCH IMPASSE (2026-05-18) — SUPERSEDED BY S67 ABOVE
═══════════════════════════════════════════════════════════════════════════════

**FIRST ACTION FOR NEXT SESSION**:

**Read `memory/project_s65_arch_impasse.md` BEFORE anything else.** Comprehensive
summary of S65 (2026-05-17 / 2026-05-18) findings.

**Phase 2 IS NOT LAUNCH-READY.** The pool-growth collect-time ballooning
(555s pool=1 → 1048s pool=5) is unsolved. All cheap tuning levers exhausted
this session:
- CIS adaptive batching (Stage A) REFUTED — every iter +14-37% slower
- CPU opp inference REFUTED — 344 inf/s aggregate vs 2300 GPU baseline
- Battle server degradation REFUTED concretely (warm-BS iter 0 ≈ fresh-BS)
- Multi-GPU ruled out by budget
- Pool size cap user-rejected ("will hinder training, unacceptable")

User assessment: "back to the drawing board kinda thing now."

**Next session task: RESEARCH, not implementation.**

Study how similar implementations handle multi-opp PFSP inference scaling at
our regime (single GPU, conc=200, 1600g/iter, 20M transformer):
1. **Metamon** — code at `metamon_ref/metamon/rl/self_play/` in this repo.
   Their `launch_models.py` uses subprocess.Popen per opp; see how inference
   batching works at their scale.
2. **OpenAI Five** — Dota 5v5 self-play, massive distributed inference. Public
   blog posts + arxiv.
3. **AlphaStar / AlphaGo Zero** — DeepMind league self-play. Papers + blog.
4. **ps-ppo** — explicit PPO self-play library; inference design.
5. **VGC-Bench** — Pokemon-specific; we used as Tier 3 reference earlier but
   didn't study their PFSP inference path.

Key questions to answer from each:
- N opps per iter, hardware assumed, how they batch across opps
- Centralized vs distributed inference
- Per-iter wall at scale (compare to our 12-20 min/iter)
- Architecture-level patterns that AREN'T just "throw more hardware at it"

**Output should be a concrete architecture proposal** informed by what works
elsewhere. NOT another tuning iteration. 2-3 hours of careful research.

═══════════════════════════════════════════════════════════════════════════════
S65 SUMMARY (what shipped, what was refuted)
═══════════════════════════════════════════════════════════════════════════════

**SHIPPED (code on branches, opt-in / unmerged):**
- `perf/pfsp-allocator-polish` @ 24067045 — `--pfsp-allocator balanced` flag.
  15/15 unit tests pass. Prod A/B not run. Modest impact at 15w (7-10% on
  new-snapshot iters); biggest impact at 8w/3-5 opp (25-37% max-worker
  reduction). Not the pool-growth fix, but a real improvement.
- `perf/cis-adaptive-batching` @ 4699a030 — `--cis-adaptive` flag. REFUTED
  in prod testing but retained as runtime-mutation infrastructure (could
  support a different formula if one emerges from research).
- `bench/cpu-inference` @ 2c188c91 — `bench_cpu_inference.py` standalone
  benchmark. REFUTED CPU service but useful diagnostic tool.

**REFUTED THIS SESSION:**
- Battle server within-run degradation (task #44) — fully refuted by Run B
  warm-BS iter 0 = 568s vs Run A fresh-BS iter 0 = 555s. 13s noise.
- CIS adaptive batching (task #46) — measured worse across all iters. Per-fire
  fixed overhead (mp.send + GPU launch + Python dispatch) dominates the
  throughput-per-fire savings my formula optimized for.
- CPU opp inference service (task #49 / #50) — benchmark showed 344 inf/s
  aggregate at 32 workers, far below the 2000 inf/s viability threshold. 20M
  transformer is fundamentally GPU-favored.

**VALIDATED:**
- 15w retest (task #42) — 10 iters at 15w with snapshot-interval=2.
  Task #21 (iter-41+ pipe cascade) did NOT reproduce. CIS Phase 4.6 recovery
  caught a partial stall at iter 9 cleanly. **15w is viable.**

**LESSONS:**
- Stage A failed because py_compile catches syntax but not runtime NameError
  (3 references to a removed variable in a function I refactored). Caught by
  prod test, not by my smoke validation. Task #51 added: write a real
  integration test for CIS runtime mutation before any future runtime-param
  work.
- The pool-growth slowdown is fundamentally architectural. Tuning the single
  knob (min_batch / timeout) doesn't help because of fixed per-fire costs.

═══════════════════════════════════════════════════════════════════════════════
POD STATE AT HANDOVER (2026-05-18 03:30 UTC)
═══════════════════════════════════════════════════════════════════════════════

- Master at `d7845354` (S64 arc complete + drop-pipeline shipped)
- All test runs killed. No python running.
- Battle servers up. May want fresh restart before next prod run.
- Lingering `cleanup_watchdog.sh` from earlier — harmless. Kill via
  `pkill -9 -f cleanup_watchdog`.
- Artifacts at `/workspace/team_builder/data/s64_artifacts/` (multiple
  subdirectories with run logs + profile trace from this arc).

**S64 final findings (2026-05-16 → 2026-05-17, full overnight 7-iter A/B)**:

| Config | Steady-state wall (pool=1, iters 2-4 avg) | 7-iter total | Δ |
|---|---|---|---|
| Pipeline-OFF | **728s/iter** | 91 min | baseline |
| Pipeline-ON | **867s/iter** | 112 min | **+19% per iter, +23% over 7-iter run** |

- Pipeline cost validated CONCRETE: GPU contention adds ~+50-66% to update wall (155 → 221-256s); battle-server contention adds ~+17% to collect; BG drain wait at iter end adds 288-577s per intermediate iter.
- Steady-state wall (pipeline-OFF): **~12.1 min/iter at pool=1, ~14.5 min/iter at pool=2**. **Crosses user's "<10 min/iter" stop criterion at pool=1 except for pool>1 territory** — but the pool>1 slowdown is a CIS-tuning issue (see Phase 2 prep below), NOT inherent.
- Profiler trace `data/s64_artifacts/pipeline_steady_state/profile_iter2.json` (2.0 GB, 7.7M events) captured at iter 2 update with BG running. Available for future GPU-contention root-cause investigation if/when multi-GPU revisit happens.

**S64 incidental findings to ADD to Phase 2 prep (per [[project_s62_fix1_b_results]] §8)**:

1. **Pool growth at pool=2 causes +17-30% collect-time slowdown** (run 1 iters 5-6 went 565s → 688s → 733s). CIS-STATS show timeout_pct went from 55-65% (pool=1) → 95-100% (pool=2) due to per-opp-slot fan-in dropping. The S62 32/50 tuning was measured for pool=1 only; pool>1 was explicitly flagged as untested. **Phase 2 will spend most iters at pool>1.**

2. **Two documented but unshipped wins from S58 narrative**:
   - **PFSP allocator polish at 8w** ($0, ~20-30% wall savings documented). Allocations like [2,2,2,1,1] workers/opp create unbalanced game counts — better allocator = free win.
   - **15-worker config validated faster at iter 40 cold start** (collect 20 min, 3 workers/opp at pool=5, max/min ratio 1.07× vs 8w's 1.25×). Broke at iter 41+ with pipe-cascade mid-iter crashes (task #21, unresolved). **The 15w investigation predates S58 ghost-tie + heartbeat fixes** — bug may already be fixed.

3. **Python shutdown hang on Training-complete (CIS workers don't exit)**. Hit twice in overnight runs; cost 8 hr first time. Mitigated overnight with `cleanup_watchdog.sh` (90s grace then SIGKILL). **This is unresolved and would block any sequential prod chain — needs investigation as Phase 2 prep task.** Probably same lifecycle issue as task #21.

**Step 1: DONE — perf/2b-gc-audit merged to master at `4917789f`. `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` baked into `launch_rl.sh` at `c03af174`. `--profile-iters` flag in train_rl.py at `45d756b9`.** All pushed to origin/master.

**Step 2: pivot decision — Phase 2 prep recommended sequence**:

Per quality-first standard, do these BEFORE launching Phase 2 (each is a separate session with its own gate):

1. **PFSP allocator polish at 8w** — $0, documented 20-30% saving. Genuinely free money.
2. **Re-test 15w with post-S58 fixes** — 1-2 hours of controlled experiment (cold start + sustained 5-10 iters). If works → another 10-20% saving. If breaks → diagnose with current observability.
3. **CIS retune for pool>1 regime** — design depends on outcomes of 1+2. Per-slot tuning at minimum; cross-slot batching (deferred S62 Option C) if 15-16 opp cap requires.
4. **Diagnose CIS-worker shutdown hang** — blocks any chained prod runs. Probably same root as task #21.
5. **Other Phase 2 prep**: source 100-150 real gen-9-OU teams, lr schedule design, external opp adapters, H2H gauntlet wiring (from [[project_s62_fix1_b_results]] §7).

**Phase 2 cost projection (with new data + caveats)**: at avg pool=8-10 over a 200-iter run with current 32/50 CIS tuning (NO fix to pool>1 timeout pressure): ~46 hr / **~$70**. With PFSP allocator polish + 15w + CIS retune all shipped: could plausibly drop to ~30 hr / ~$45.

**Step 2: pivot decision**:
- User's stop criterion: "<10 min/iter steady state, then pivot to multi-gen prep." Our worst-case 3-iter avg = 16 min/iter (skewed by iter 1 post-update overhead). Best-case iter 2 = 13.7 min. **We have NOT cleanly crossed the threshold.** But also the remaining techniques (2c BC caching ~5-10%, #2 CUDA Graphs ~5-15%) won't materially change this — the binding constraint at steady state is *collect*, not update.
- **Strong recommendation: pivot to Phase 2 prep / multi-gen now.** The update wall is no longer the dominant cost. Further optimization arc work has diminishing marginal return.
- If user wants to continue: 2c (BC anchor caching) is cleanest; #2 (CUDA Graphs) is highest variance.

**Step 3: before any Phase 2 launch**:
- Restart battle_server.js processes (state degrades — verified in Phase B §3.3)
- Re-verify torch 2.5.1+cu121 + LD_LIBRARY_PATH on prod
- Use the canonical launch block below

**Pod state at handover (2026-05-17 post-overnight)**:
- Master at `45d756b9` (full optimization arc + profiler patch shipped)
- Prod git HEAD: master (synced via run3_chain.sh pull)
- All python + watchers + monitors killed; pod is IDLE
- 8 battle_servers running on ports 9000-9007 (restart recommended before next prod run)
- Artifacts at `/workspace/team_builder/data/s64_artifacts/pipeline_steady_state/`:
  - `run1_nopipe_7iter.log` (327 KB) — pipeline-OFF 7-iter baseline
  - `run2_pipe_7iter.log` (795 KB) — pipeline-ON 7-iter A/B
  - `run3_pipe_profiled.log` (284 KB) — profiler run training log
  - `profile_iter2.json` (**2.0 GB**, 7.7M events) — kernel trace for future forensics
- Logs: `/tmp/opt2_3iter.log` + `/tmp/option2_memory.log` on prod — scp before pod cleanup

**Phase B + 2a (minibatch=64 at bf16) BOTH SHIPPED — MEASURED at prod**:

| Config | Collect | Update | Total | Δ update vs legacy |
|---|---|---|---|---|
| Legacy mb=16 | 573s | 1865s | 2438s | — |
| Packed mb=16 | 573s | 1729s | 2302s | -7.3% |
| Packed mb=32 (initial 2a ship) | 578s | 1054s | 1632s | -43.5% |
| Packed mb=48 (bf16 sweep) | 560s | 660s | 1220s | -64.6% |
| **Packed mb=64 (final 2a ship)** | **550s** | **551s** | **1101s** | **-70.5% / 3.38×** |

- Overall wall: 2438s → 1101s at mb=64 = **-55% per iter**
- 200-iter Phase 2: ~5.7 days / ~$200 → **~2.5 days / ~$90** (saves ~80 hr / ~$110)
- Numerical drift: kl/bc_kl ~-0.003 vs legacy, within bf16 noise; **favorable
  direction** for bc_kl (closer to BC anchor)
- ALL 5 Phase B bit-equiv gates passed (B.2 temporal, B.3+B.4 fwd fp32+bf16,
  B.5 loss 12-combo with non-zero BC anchor, B.6 e2e smoke)
- 2a learning: **fp32 smoke does NOT transfer to bf16 prod for kernel-
  selection-bound questions.** cuBLAS picks qualitatively different kernels
  per precision. Initial mb=32 ship was based on fp32 smoke; corrected to
  mb=64 after bf16 sweep showed mb=48 and mb=64 both faster.
- 2a operational learning: **clean-state ritual must include `pkill -9 python`
  (no filter), not `pkill -9 -f 'python.*train_rl'`.** The filter misses
  multiprocessing-fork children. Orphan CIS workers contaminated several runs.

**CANONICAL PHASE 2 LAUNCH (UPDATED post-2b — `--pipeline` DROPPED based on S64 A/B)**:
```bash
cd /workspace/team_builder/pokemon-ai-starter/pokemon-ai/src
nohup ./launch_rl.sh \
  --init-from data/models/bc/v10_padded_for_cis_dev.pt \
  --bc-anchor-ckpt data/models/bc/v10_padded_for_cis_dev.pt \
  --bc-anchor-coef 0.1 \
  --cis --bf16 --tier3 --tier3-minibatch-size 64 \
  --packed --no-per-chunk-gc \
  --cis-min-batch 32 --cis-timeout-ms 50 \
  --games-per-iter 1600 --max-concurrent 200 \
  --n-iters 200 --warmup-iters 0 \
  --lr 1e-5 --lam 0.95 --ent-coef 0.02 --target-kl 0.03 \
  --grad-accum 1 --turn-cap 300 \
  --reward-style terminal \
  --procedural-teams /workspace/raw_data/pokemon_usage/2024-04 \
  --adaptive-entropy --adaptive-entropy-low 0.65 --adaptive-entropy-high 0.95 \
  --eval-interval 10 --eval-games 200 --eval-team-set metamon-competitive \
  --snapshot-interval 10 \
  --early-stop --early-stop-patience 3 \
  --win-rate-mode ema --win-rate-ema-alpha 0.3 --win-rate-ema-window 50 \
  --out-dir data/models/rl_v10/<RUN_NAME> \
  > /tmp/<RUN_NAME>.log 2>&1 &
```

Six changes vs pre-S64 canonical: `./launch_rl.sh` wrapper (LD_LIBRARY_PATH for
cuDNN 9 + `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` both baked in),
`--packed` (Phase B), `--tier3-minibatch-size 64` instead of 16 (2a, 3.14×
update), `--no-per-chunk-gc` (2b), **`--pipeline` REMOVED** (S64 A/B: pipeline
costs 24% wall at our scale — CIS GPU contention + battle server contention +
BG drain wait. See `data/s64_artifacts/2b_validation/`), drop `--device cuda` +
`--servers ...` (defaults). PROCEDURAL-TEAMS PATH is `/workspace/raw_data/...`
NOT `/workspace/team_builder/raw_data/...` (memory had it wrong).

**Pre-Phase-2-launch checklist**:
1. RESTART battle_server.js processes (per Phase B §3.3 — state degrades over
   runs; the +86% collect regression in B.8 first-pass was env, NOT packed)
2. Verify torch 2.5.1+cu121 + LD_LIBRARY_PATH on prod
3. Verify 8 battle servers up
4. Either --init-from BC v10 (fresh) or --resume <SNAPSHOT> (continuation)

**Pod state at wrap**:
- prod (port 47913): clean (0 python, 8 battle_server.js fresh, GPU 0% with
  ~6 GiB CUDA leftover that's harmless), HEAD `perf/seq-packing` 6b1d4562
  merged to master at this wrap. Torch 2.5.1+cu121 in-place; LD_LIBRARY_PATH
  exported in ~/.bashrc as safety net.
- local: all logs pulled to `data/s64_artifacts/phase_b/` (b8r_legacy.log
  29 MB, b8r_packed.log 125 KB, fp32_* smoke logs)

**Open questions for next session** (per optimization tracker §3):
1. Attention dropout in packed path — adds F.dropout(p=0.05) post-flex shim.
   Documented bias is ~-0.003 kl in favorable direction; likely not needed.
   Revisit only if Phase 2 shows divergent convergence.
2. Delete `perf/seq-packing` branch post-merge? Leaning keep (no cost).
3. argparse `--help` bug (literal → in help string collides with % format).
   One-line fix at a clean moment. Not blocking.

**Phase B wrap surfaced finding — UPDATE PHASE IS ~4× SUPER-LINEAR**:

User question at wrap: "100g/iter ≈ 1 min, 1600g/iter (16×) ≈ 30 min — should be
~16 min linearly?". Answer: yes, BUT update is orchestration-bound, not compute-
bound. Collect scales correctly (15.4× for 16× games). Update scales 62×.

| Phase   | Smoke 100g | Prod 1600g | Ratio | Linear (16×) |
|---------|------------|------------|-------|--------------|
| Collect | 38s        | 586s       | 15.4× | 16× ✓        |
| Update  | 30s        | 1865s      | **62×** | 16× ❌      |

Decomposition under S62 profile (update is 92% Python/CPU, 8% CUDA):
- GPU compute: ~25× for 16× games (slightly super-linear, expected)
- Python orchestration: 71× for 16× games (THE super-linearity)

Mechanism: Tier 3 minibatch=16 → chunks=ceil(B/16). Smoke 100g=21 chunks,
prod 1600g=300 chunks (14× more). Per-chunk Python overhead is ~constant
per chunk (gc.collect, empty_cache, dict construction, loop iteration, tensor
accumulation). 14× more chunks → 14× more overhead invocations.

**CORRECTION (post-wrap, user pushback)**: my "revised UP to 1.5-3×" was
an over-projection. CUDA Graphs eliminates per-CUDA-launch and per-alloc
overhead — NOT Python control flow. Honest projection: **5-15%** wall
reduction on the launch+alloc share (~110s at prod). It does NOT remove
gc.collect, empty_cache, dict construction, or the for-loop.

To actually attack the per-chunk Python loop, separate techniques (added
to tracker as 2a/2b/2c):
- **2a**: `--tier3-minibatch-size 32` (or higher) with packed memory
  savings → halves chunk count → halves per-chunk Python overhead.
  Est. 1.2-1.4×. Cheapest experiment.
- **2b**: audit `gc.collect()` + `torch.cuda.empty_cache()` per chunk —
  possibly removable or per-epoch-only. Est. 1.05-1.20×.
- **2c**: BC anchor caching: compute BC logits once per iter, not per
  chunk × epoch. Est. 1.05-1.10× if BC anchor is on.

These stack additively. Combined ceiling 2a×2b×2c×#2 ≈ 1.5-2.5× update
wall (NOT from any single technique).

ALSO: the "92% Python orchestration" decomposition is INFERRED from S62
prod profile alone. At smoke scale the split may differ. **Step A** of
the post-wrap investigation profiles smoke + compares to S62 prod. Until
that data is in hand, treat decomposition as STRONG INFERENCE not CONCRETE.

If 2a/2b/2c land projections: Phase 2 drops from ~5.7 days (~$200) to
~3-4 days (~$120). Material vs packed-only -7.3% saving.

Captured in:
- `memory/project_s64_phase_b_results.md` §3.4
- `memory/project_optimization_tracker.md` §1.1 + technique #2 row
- `docs/STATUS.md` CURRENT STATE section
- `docs/PROFILE_BOTTLENECKS_REPORT.md` S64 Phase B finding subsection

═══════════════════════════════════════════════════════════════════════════════
⚠️  S64 PHASE A COMPLETE (2026-05-14) — preserved below as historical
═══════════════════════════════════════════════════════════════════════════════

(Phase A wrap superseded by Phase B SHIPPED above. Phase A's
`collate_episodes_packed` function survives unchanged and is now wired into
production via the Phase B pipeline. See `memory/project_s64_phase_a_results.md`
for Phase A details + the §4-REVISED addendum at Phase B kickoff.)

═══════════════════════════════════════════════════════════════════════════════
⚠️  S64 PHASE 1 COMPLETE (2026-05-14) — preserved below for context
═══════════════════════════════════════════════════════════════════════════════

**FIRST ACTION FOR NEXT SESSION**: read memory/project_s64_phase1_torch_upgrade_results.md
§4 (Phase A spec). Then ask user to authorize Phase A start before writing
any code. Phase A = single-file change in pokemon-ai-starter/pokemon-ai/src/
ppo.py adding `collate_episodes_packed()` on a new branch `perf/seq-packing`
off master (master HEAD at the start of next session = `5c0a40e1`).

**S64 status**:
- Step-back FULL-CONFIRMED at prod scale: Option A (seq-packing on existing
  arch) wins. See [[project_s64_arch_revisit_profile_findings]].
- Phase 1 PASSED: torch 2.5.1+cu121 upgrade is feasible; 8/8 compat tests
  pass on prod via isolated venv (system torch 2.2.1 untouched).
  See [[project_s64_phase1_torch_upgrade_results]].
- **Three operational findings baked in for Phase A onward**:
  1. `LD_LIBRARY_PATH=/usr/local/lib/python3.11/dist-packages/nvidia/cudnn/lib:$LD_LIBRARY_PATH`
     required for cuDNN 9 — bake into S64 launch script (one-line wrapper).
  2. Varlen API path = `flex_attention` + `create_block_mask`, NOT a
     dedicated `varlen` module. Implementation pattern: per-episode causal
     BlockMask via `searchsorted(cu_seqlens, ...)`.
  3. Triton bumped 2.2.0 -> 3.1.0 — re-test S56 C5 compile path at Phase B.

- Awaiting user authorization to begin **Phase A: collate_episodes_packed**
  (design + impl). NO code change yet. Master at `5c0a40e1`.

**Read order for next session**:
1. `memory/MEMORY.md` (auto-loaded; entries 4n + 4o cover S64 step-back + Phase 1)
2. `memory/project_session_boot_protocol.md` (standing orders, philosophy)
3. `memory/project_optimization_tracker.md` (sequence roadmap; technique #1 row + §3 Open Questions current)
4. `memory/project_s64_arch_revisit_profile_findings.md` (full step-back reasoning, status FULL-CONFIRMED)
5. `memory/project_s64_phase1_torch_upgrade_results.md` (Phase 1 results + Phase A spec in §4)
6. This file (next-prompt.txt) — S64 wrap below + S63/S62/S58 wraps preserved further down for context

**The step-back question**: user paused before S64 implementation because
seq-packing is itself an arch-touching commitment (2-4 sessions of refactor).
The question: would we want to drop the temporal stack anyway, making seq-
packing partially sunk cost? Answer based on profile data: **NO — the cat
hot-spot lives in collate, not in the temporal stack.**

**Smoke evidence (CONCRETE)**:
- Smoke profile: 100g/2w/conc=25, 1 iter, canonical Phase 2 stack,
  `PROFILE_UPDATE_ITER=0` with `with_stack=True` + `record_shapes=True`
- Total `aten::cat` self CPU = 6.56s at smoke (34,198 events)
- **42% of cat time** = cat of `[16 × (200, 6, 285)]` -> `torch.stack` in
  `_stack_batch_dim` (ppo.py:301) — cross-episode batch-stack
- **6% of cat time** = pair-cats `[(X, 6, 285), (200-X, 6, 285)]` for various
  X -> padding cat in `_stack_pad_one_episode` (ppo.py:267) — per-episode
  pad-to-L_max-with-zeros
- Similar patterns for feature shapes (200, 4, 109) and (200, 6, 4, 23)
- **95% of cat time is in `collate_episodes` data prep. 5% is attn-internal
  qkv reshapes (avg 25us, total 0.34s).**

**Prod evidence (CONCRETE — FULL CONFIRMATION)**:
- Prod profile: 1600g/8w/conc=200, 1 iter, canonical Phase 2 stack, same
  PROFILE_UPDATE_ITER=0 + with_stack=True + record_shapes=True
- Update wall = 3256s profiled (vs S62 ~32 min unprofiled = 1.7x overhead,
  within projected 1.5-1.8x band)
- Trace exported: 9.06 GB (less than projected 15-25 GB)
- **541,593 cat events parsed (matches S62's 541k headline)**
- **Total cat self CPU = 93.89s = 23.6% of update CPU (398s)**
- **Decomposition**:
  - Big batch-stack (n>=8 same shape): 60.7% (57.0s) — `_stack_batch_dim`
  - Padding pair (2 tensors sum=200): 27.4% (25.7s) — `_stack_pad_one_episode`
  - Attn-internal (empty Input Dims): 11.2% (10.5s) — qkv reshapes inside FA
  - Other misc: 0.7% (0.66s)
- **88.1% of cat self CPU in collate-driven patterns at prod scale**
- **Top single signature**: `cat([(200, 6, 285)] x 16)` at 990 events x
  36.66 ms avg = **38.6% of all cat time = ~9% of update CPU from one
  callsite** (`_stack_batch_dim` line 301)
- **Bonus finding**: 160k `aten::fill_` calls at prod = 160,486 (vs S62's
  160,501) — essentially unchanged DESPITE S63's `set_to_none=True`. Means
  most fill_ are NOT grad zeroing — they're padding zeros from collate
  (`torch.zeros(pad_shape, ...)` at ppo.py:202/235/265/279). Seq-packing
  eliminates these too. Adds 62s CPU + 4.2s GPU to savings projection.
- **Total reachable by seq-packing: ~150s CPU + ~5s GPU = ~38% of update CPU**

**Stack reconstruction (TRIPLE-CONFIRMS callsite)**: for one specific 90ms
cat event with 16 × (200, 6, 285) inputs, extracted python_function events
spanning the cat's ts range on the same thread:
```
train_rl.py:1378 <module>
  -> train_rl.py:1194 main()
    -> ppo.py:1104 ppo_update_batched()
      -> ppo.py:126 collate_episodes()
        -> ppo.py:293 _stack_batch_dim()
          -> torch.stack()  <- the cat
```

**Code cross-check (CONCRETE)**: `ppo.py:301` is `_t.stack([d[k] for d in
per_ep_list], dim=0)`. `ppo.py:267` is `_t.cat([stacked, pad], dim=0)`.
Both inside `collate_episodes` (ppo.py:126). Matches the trace shapes exactly.

**Implication**: Option A (seq-packing on existing arch) IS THE RIGHT CALL.
Option B (drop temporal stack) addresses ~zero of dominant waste — temporal
stack consumes <8% of update wall, dominant waste is in data prep.

**Status at end of step-back (FULL-CONFIRMED)**:
- Branch `diag/update-profile-s64-with-stack` at `f08e9e1c` (cherry-pick of
  S62 diag onto master + with_stack/record_shapes flips). Pushed to origin.
  DIAG ONLY — never merge to master.
- Prod pod: python KILLED, GPU clear (1 MiB), 8 battle_servers up, HEAD on
  diag branch. Pod-side trace at `/tmp/update_profile_iter0.json` (9 GB,
  can be deleted now that local pull confirmed).
- All artifacts pulled to local `data/s64_artifacts/`:
  - `s64_smoke_with_stack.log` (52 KB) + `s64_smoke_trace.json` (631 MB)
  - `s64_prod_with_stack.log` (54 KB) + `s64_prod_trace.json` (9.06 GB)
- Local HEAD: `463827b4` (master). Uncommitted: memory file updates
  (project_s64_arch_revisit_profile_findings.md NEW, MEMORY.md pointer,
  tracker §1/§3/§4/§7 edits, this next-prompt section). Ready to commit
  as one S64 step-back wrap commit when user confirms.

**Canonical pre-S64 baseline (for post-S64 A/B)**:
The S64 prod profile uses the SAME canonical Phase 2 stack as S63 (no code
change, just env-gated profiler). So the baseline IS the S63 numbers from
[[project_s63_free_wins_results]] §1.2:

| Metric | Value |
|---|---|
| collect | 588s |
| **update** | **1850s** |
| W/L/T | 798/801/0 (49.9%) |
| steps | 76,533 |
| kl | 0.0155 |
| bc_kl | 0.0156 |
| pi | 0.0111 |
| v | 13.9351 |

Gate for S64 implementation A/B: update wall <= 1850 x 0.7 = 1295s
(>=30% reduction) + no health drift (kl, bc_kl, smart_avg-iter-2 within
+/-5pp of these baselines).

**S64 implementation plan (AFTER prod-confirmation + user authorization)**:
1. Torch upgrade 2.2.1 -> 2.5.1 (LTS) in isolation on dev pod first; verify
   BC v10 loads, poke-env works, CIS subprocess pattern works
2. Design memo for packed format: explicit `collate_episodes_packed` spec
   returning `(flat_feat_batches, cu_seqlens, max_seqlen)`
3. Phased implementation:
   - Phase A: `collate_episodes_packed` + unit tests
   - Phase B: refactor `forward_ppo_sequence` for packed input + varlen attn
   - Phase C: refactor `ppo_loss_batched` for cu_seqlens masking
   - Phase D: wire `--seq-packed` flag in train_rl.py
   - Phase E: A/B at canonical Phase 2 stack vs current master; gate >=30%
     wall reduction + no health drift
   - Phase F: merge on gate pass
4. Estimated cost: $10-30 across 2-4 sessions

**REFUTED IN S64 STEP-BACK (don't reopen)**:
- Drop temporal stack as priority for optimization arc — refuted by
  profile data (temporal stack <8% of update wall). May revisit
  post-Phase-2 as a quality exercise; NOT as part of this arc.

═══════════════════════════════════════════════════════════════════════════════
⚠️  S63 WRAP (2026-05-14, post-free-wins) — preserved below for context
═══════════════════════════════════════════════════════════════════════════════

**S63 outcome: free wins SHIPPED at -4.2% update wall. Below my a-priori 5%
gate but accepted because numerically equivalent + best-practice + audit
showed we're at the ceiling for the FREE WINS category. Big wins live in
S64 (sequence packing).**

**Read FIRST**: `memory/project_optimization_tracker.md` — sequence roadmap
+ operating principles. S63 status now SHIPPED; S64 (sequence packing) is
NEXT.

**Read SECOND**: `memory/project_s63_free_wins_results.md` — full data + the
"why we accepted below-gate" rationale (precedent for the multi-session arc).

**S63 changes shipped to master**:
- `optimizer.zero_grad(set_to_none=True)` at 12 callsites in `ppo.py`
- Defer chunk-stats `.item()` to epoch boundary in eager Tier3 hot path
  (uses `_ppo_loss_batched_internal` directly, tensor accumulators, one
  `.item()` per epoch instead of per chunk)
- Branch `perf/free-wins-zero-grad-item-audit` (commits `ef7815ff` +
  `22fc08f3`), merged to master at S63 wrap

**S63 measurement (1600g/8w/conc=200, 1 iter, canonical Phase 2 stack)**:
- collect: 588s (-27% from no-Option-B baseline; unchanged from S62)
- update: 1850s vs 1931s baseline = **-81s = -4.2%**
- Per-iter wall reduction: ~$7-8 per Phase 2 run + similar per multi-gen
- Health stats clean (kl=0.0155, bc_kl=0.0156, no NaN, no Traceback)

**Why projection was off** (8-15% projected, 4.2% actual):
- Profile (`project_s62_update_profile_findings.md`) showed 9000 .item()
  calls/iter at avg 4.3ms = 38s of sync wait
- My changes eliminated ~3600 of 9000 (the obvious chunk-loop ones)
- Remaining ~5400 are mostly NaN gates (line 1435 forward NaN check, line
  1455 loss NaN check) — NOT removable without correctness risk (forward
  NaN propagating into backward → corrupted gradients)
- Wall reduction 81s exceeded direct-cost-prediction (~50s) → the changes
  also enabled better GPU pipelining beyond direct sync elimination

**Precedent established for the multi-session arc**: gates are guidelines,
not numerical idols. When data refutes projection, reframe + ship + move on
(per principle §0.3). Don't perform discipline by holding zero-risk
positive changes behind arbitrary thresholds.

═══════════════════════════════════════════════════════════════════════════════
S64 NEXT — SEQUENCE PACKING (THE BIG ONE)
═══════════════════════════════════════════════════════════════════════════════

**Targets** (from S62 profile):
- 24% CPU `aten::cat` (541k per-turn temporal stack concat calls)
- 28% GPU `aten::copy_` (583k internal tensor copies, much from padding)
- Most of `_efficient_attention_forward` 9.5% (padding compute waste)

**Estimated payoff**: 1.5-3× update wall = $50-100 saved per Phase 2 run
+ similar per multi-gen + ablation. Compounds.

**Prerequisites** (BEFORE S64 implementation):
1. **torch upgrade 2.2.1 → 2.5.1** (LTS) — required for
   `torch.nn.attention.varlen` API. Verify compatibility:
   - poke-env, the BC checkpoint loader (PyTorch state_dict format)
   - CIS subprocess code (any torch IPC pattern that may have changed)
   - The compiled C5 train_step path in `make_compiled_train_step`
     (we don't use --compile but the function exists; should still load)
   - Recommend: torch upgrade in isolation on dev pod first; smoke; then
     merge to optimization branch

**Likely refactor scope**: `forward_ppo_sequence` in `model_transformer.py`
(currently builds padded `summary_grid (B, L_max, D)` and uses standard
attention; needs to switch to packed sequences with `cu_seqlens` boundary
tensor + varlen attention call). Possibly cascading into how episodes are
stored in trajectories (`build_ppo_episodes`) and collated
(`collate_episodes`).

Risk: MEDIUM-HIGH. Cascades. Could be 2-4 sessions. User authorized.

═══════════════════════════════════════════════════════════════════════════════
S62 END WRAP (post-update-profile) — preserved below for context
═══════════════════════════════════════════════════════════════════════════════

**S62 ended with a strategic pivot. Phase 2 prep is DEFERRED. The new arc
is a multi-session UPDATE-PHASE OPTIMIZATION SEQUENCE based on profile data.**

**Read FIRST**: `memory/project_optimization_tracker.md` — authoritative
roadmap + operating principles inline (no shortcuts, push back hard,
us-against-the-problem). It's the new "where are we" file for this arc.

**Read SECOND**: `memory/project_s62_update_profile_findings.md` — the
torch.profiler kernel breakdown that justifies the sequence priority.

**S62 update profile findings (decisive)**:
- Update phase is NOT GPU-compute-bound. It is ORCHESTRATION-bound.
- CUDA kernels = only 8% of update wall (147s of 1931s).
- TWO FREE WINS available (one-line changes, zero risk):
  - `optimizer.zero_grad(set_to_none=True)` — eliminates 13.3% CPU on
    `aten::fill_` (160k calls/iter)
  - `.item()` audit + batch — eliminates 9% CPU on `_local_scalar_dense`
    (9k calls × 4.3ms each = 38s sync wait per iter)
  - Combined: 8-15% wall, ~1 hour dev, ~$2-3 validation
- BIG architectural opportunity: sequence packing + varlen attention
  eliminates the 24% CPU `aten::cat` (541k calls) + 28% GPU `aten::copy_`
  (583k calls) waste. Estimated 1.5-3× wall. 2-4 sessions, MEDIUM-HIGH risk.
- Liger-Kernel REFUTED (we use GELU+LayerNorm not SwiGLU+RMSNorm).
- Regional `--compile` DEMOTED (we're not compute-bound on individual
  kernels; CUDA Graphs covers the dispatch-overhead ground better).

**Stacked optimistic ceiling**: 3-5× wall reduction across the sequence.
32 min/iter → 6-10 min/iter at prod scale.

**Phase 2 launch**: deferred until optimization sequence completes (or
hits diminishing returns). User-approved.

**Branch state**:
- master at `869ef285` — S62 prod-validation docs + canonical Phase 2 stack
- `diag/update-profile-s62` at `ec210df` — S62 profiler instrumentation (DIAG ONLY, do not merge)
- Profile artifacts: local `data/s62_artifacts/s62_update_profile_v2.log`; pod `/workspace/team_builder/data/s62_artifacts/update_profile_iter0.json` (5.6GB chrome trace, pull on demand)

**S63 NEXT (awaiting user approval)**: land the two free wins (0a + 0b)
on branch `perf/free-wins-zero-grad-item-audit`. Validate via 1-iter prod
A/B vs current master. Acceptance: ≥5% wall reduction + no health drift
(kl, bc_kl, smart_avg-iter-2 within ±5pp).

═══════════════════════════════════════════════════════════════════════════════
⚠️  S62 EARLIER WRAP (Fix #1 + Fix #2 prod validations) — preserved below
═══════════════════════════════════════════════════════════════════════════════

**S62 outcome: Fix #1 SHIPPED + prod-validated (-27% collect at 1600g/8w).
Fix #2 (--compile) REFUTED at prod (8% SLOWER); DROPPED from Phase 2 stack.
Phase 2 prep is now genuinely unblocked.**

**The user's "no shortcuts" pushback paid off** — without prod-scale validation
of BOTH Fix #1 and Fix #2 we'd have launched Phase 2 with --compile adding
~8% wall-time overhead. Saved a real ~$25-30 per Phase 2 run (5-day, $200-300
total) and similar savings on multi-gen.

**Updated canonical Phase 2 launch stack** (use this, NOT older versions):
```
--cis --pipeline --bf16 --tier3 --tier3-minibatch-size 16 \
--bc-anchor-ckpt v10 --bc-anchor-coef 0.1 \
--cis-min-batch 32 --cis-timeout-ms 50
```
**(NO --compile — REFUTED at prod scale per S62 validation)**

═══════════════════════════════════════════════════════════════════════════════
S62 — PROD-SCALE VALIDATION FINDINGS (THE NEW STUFF)
═══════════════════════════════════════════════════════════════════════════════

**Fix #1 Option B at prod scale** (see [[project_s62_fix1_b_results]] §1.1 +
[[project_s62_fix2_prod_validation]] for context):
- Phase C (4w/conc=50): -40% collect (3 iters consistent)
- Prod (8w/conc=200): -27% collect (1 iter A/B)
- Compression as predicted: at prod baseline meanq=7.7 (vs Phase C 3.9) → 71%
  of baseline fires were minbatch-driven → variant gain shrinks but still
  passes the 25% Phase D threshold. SHIPS.

**Fix #2 (--compile) at prod scale** (see [[project_s62_fix2_prod_validation]]):
- 4 data points all show ~27 μs/step with --compile vs 25 μs/step eager
  → compile is **8% SLOWER per step** at prod scale, on iter 1 cache-hit too
- S60's "9× speedup" was at 64g/2w smoke (4 chunks × 16 episodes); at 1600g/8w
  prod (100 chunks × 16 episodes) the per-call compile dispatch overhead
  exceeds the kernel-fusion savings
- TORCH_LOGS=recompiles confirmed: only 2 recompile events in entire 34-min
  update (both early on `resume_in_forward_ppo_sequence` graph-break resume).
  Recompile loop hypothesis REFUTED.
- Decision: DROP --compile from canonical Phase 2 stack
- The S60 engineering work (BC anchor + Tier3 + compile composition + the
  bc_anchor_enabled closure flag pattern) is still a real capability — we
  just don't pull the --compile trigger at prod

═══════════════════════════════════════════════════════════════════════════════
S62 — EARLIER (PRE-PROD-VALIDATION) WRAP — preserved below for reference
═══════════════════════════════════════════════════════════════════════════════

**S62 outcome: Fix #1 SHIPPED via Option B. -40% collect at Phase C scale.
Phase 2 prep is now unblocked.**

S61 produced the design memo (`memory/project_s61_fix1_design.md`) comparing
5 IPC-redesign options. Recommended Option E (hybrid): start with Option B
(cheap probe, $3-4) and conditionally escalate to A or C based on data.

S62 executed Phase A-E:
- **Phase A (code, ~30 min)**: added `--cis-min-batch` + `--cis-timeout-ms`
  flags (defaults preserved); enriched `[CIS-STATS]` with `meanq`,
  `timeout_pct`, fire-type breakdown. Branch `perf/cis-batch-window`,
  commit `f8cc6639`. Skipped A2 (separate logit-equivalence test) per
  right-sized-gate rationale (change is below bf16 GEMM noise floor).
- **Phase B (smoke, ~$1)**: dev pod 100g/2w/conc=25, baseline + variant.
  Variant collect = 64s vs baseline 119s = **-46%**. CIS-STATS confirms
  larger batches.
- **Phase C (3-iter A/B, ~$4)**: prod pod 200g/4w/conc=50.
  - Iter 0: collect 94s vs 157s (-40%)
  - Iter 1: collect 107s vs 182s (-41%)
  - Iter 2: collect 208s vs 346s (-40%; both inflated by --pipeline tail effect)
  - Mean: 136s vs 228s (-40%)
  - All health checks clean (no NaN, no FATAL, kl/bc_kl/v in normal range)
- **Phase D verdict**: -40% ≥ 25% threshold → Fix #1 sufficient via Option B.
- **Phase E (this wrap)**: results memo + this section + PROFILE_BOTTLENECKS_REPORT §5.

**Mechanism (per S62 CIS-STATS instrumentation)**:
- meanq 3.9 → 15 (3.9× larger batches)
- maxq 6 → 32-35 (now hits min_batch=32)
- timeout_pct 100% → 84-94% (some fires now satisfy minbatch)
- Fire rate halved → fewer round-trips
- fwd time 16.7ms → 26.5ms (sub-linear GPU amortization on 4× batch)

H3 (slot starvation under tight 15ms timeout + one-opp-per-worker dispatch)
was the dominant bottleneck at our test scales. H1 (worker-side GIL/pickle)
and Option C (shared-mem) were NOT needed. Could revisit if prod-scale
(8w/conc=200) shows much smaller gain.

═══════════════════════════════════════════════════════════════════════════════
S62 — STATE AT WRAP
═══════════════════════════════════════════════════════════════════════════════

**Git state**:
- master HEAD: `d0f1af2f` (unchanged from S60)
- Branch `perf/cis-batch-window` head: `f8cc6639` (S62 Phase A code, +56 LOC, 2 files)
- AWAITING USER DECISION on merge to master (load-bearing → user-gated)

**Local doc updates** (uncommitted at S62 wrap):
- `memory/project_s62_fix1_b_results.md` (new)
- `memory/MEMORY.md` (added 4i pointer)
- `memory/project_s61_fix1_design.md` (in-place updates §10.A/B per A2 skip + §10 S62 plan)
- `docs/PROFILE_BOTTLENECKS_REPORT.md §5` (added Option B SHIPPED row)
- `next-prompt.txt` (this section)

**Pod state (port 47913)**:
- No python training running; killed cleanly after Phase C variant
- 8 battle_servers listening on 9000-9007 (idle)
- GPU clear (1 MiB)
- HEAD on pod = `perf/cis-batch-window` (commit `f8cc663`)
- Pod-side mod still floating: `data/eval/registry/runs.jsonl` (gitignored eval-log; ignore)

**Verification artifacts** (local, gitignored, in `data/s62_artifacts/`):
- `s62_smoke_baseline.log` + `s62_smoke_variant.log` — smoke A/B
- `s62_phaseC_baseline.log` + `s62_phaseC_variant.log` — prod-scale A/B

═══════════════════════════════════════════════════════════════════════════════
S62 — NEXT SESSION (S63) PRIORITY
═══════════════════════════════════════════════════════════════════════════════

Per [[feedback_infra_first_means_first]]: all cost-compounding infra is now
DONE (Fix #2 S60, Fix #1 S62). **Phase 2 prep is the next priority.**

**Phase 2 prep checklist** (per S60 wrap §S58 list, with S62 corrections — NO --compile):

1. **Source 100-150 real elite gen-9-OU teams** for training pool
   (NEVER mix with the 16 mm-competitive eval teams — data leakage rule)
2. **lr scheduling design** — likely cosine decay or step decay; re-ablate
   post-S62 to confirm 1e-5 is still optimal with the updated stack (no compile,
   eager Tier3 + BC anchor)
3. **External opps PFSP scaling** (S58 task #14 still pending)
4. **H2H gauntlet wiring** — Kakuna/SmallRL/Minikazam adapters, ~3-4 hrs
5. **Phase 2 launch script**: canonical stack (per S62 prod validation):
   ```
   --cis --pipeline --bf16 --tier3 --tier3-minibatch-size 16 \
   --bc-anchor-ckpt v10 --bc-anchor-coef 0.1 \
   --cis-min-batch 32 --cis-timeout-ms 50
   ```
   **(NO --compile — refuted at prod scale per S62)**

**At Phase 2 launch (S63 or later)**:
- Recap in iter 0-1 that prod-scale (8w/conc=200) wall-time gain matches
  our 25-40% projection. If much lower, revisit per-slot timeout or design
  Option A (per S61 design §10.D fallback).
- Don't propose perm-at-eval, lr-decay flags, or other speculative additions
  until measured need (per [[feedback_perm_at_eval_wrong]]).

**DO NOT propose** (per S60-S62 corrections):
- Per-slot timeouts (Option B uniform timeout was sufficient at our scales;
  add only if measurement at prod scale demands)
- Worker-side aggregation (Option A) — refuted as primary need by S62 H3 data
- Shared-mem ring buffer (Option C) — same; not needed for current bottleneck
- Perm-at-eval (REFUTED in S60)
- Restarting battle_servers as routine policy (was an S58 hypothesis; doesn't
  fully restore throughput per S58 wrap; superseded by IPC fix anyway)

═══════════════════════════════════════════════════════════════════════════════
END S62 WRAP — older sections below (S60, S59, S58, ...) preserved for context
(but trust S62 wrap + memory entries OVER older content where they conflict)
═══════════════════════════════════════════════════════════════════════════════

═══════════════════════════════════════════════════════════════════════════════
⚠️  S60 WRAP (2026-05-13 evening) — AUTHORITATIVE READ FIRST
═══════════════════════════════════════════════════════════════════════════════

**S60 outcome: Fix #2 SHIPPED, Fix #3 REFUTED, priority hierarchy formalized,
multiple stale prescriptions corrected. Next session = Fix #1 design session.**

The S59 plan recommended three fixes in order (#3 vectorize → #2 compile
composition → #1 design → #1 impl). S60 executed in that order:
- **#3 vectorize collate_episodes**: tried, REFUTED by microbench.
  Advanced-index scatter is strictly slower than V1; pre-alloc+slice-assign
  is within noise. The 55s/iter figure was viztracer-INCLUSIVE — child C
  ops (torch.cat, torch.as_tensor) are bandwidth-bound, not reachable by
  Python-level vectorization. Diagnostic scripts kept on master at
  `scripts/diag/bench_collate_*.py` + `test_collate_episodes_vec_bitexact.py`.
  See `memory/project_s60_fix3_refuted.md`.
- **#2 BC anchor + Tier3 + compile composition**: SHIPPED on branch
  `perf/compile-bc-anchor-composition` (merged to master via this wrap).
  Plumbs bc_logits + bc_anchor_coef_t through compile boundary via
  `bc_anchor_enabled: bool` closure flag in `make_compiled_train_step`.
  Adds minibatch chunking to compiled path. Removes the train_rl.py
  FATAL mutex that previously forced "BC anchor OR compile, not both".
  **Prod-pod smoke: 9× update-phase speedup on iter-2 (190s → 21s) with
  BC anchor + minibatch=16 at --cis + 64g/2w.** No NaN, bc_kl recorded,
  clean training stats. See `memory/project_s60_fix2_design.md`.

**Updated priority order for S61**: Fix #1 design session (NEXT, no code).

═══════════════════════════════════════════════════════════════════════════════
S60 — CORE OPERATING PRINCIPLES (APPLY ON EVERY DECISION)
═══════════════════════════════════════════════════════════════════════════════

These are the load-bearing principles for the project. Full detail in
the linked memory files; the boot protocol (`memory/project_session_boot_protocol.md`)
has the canonical versions. Listed here for inheritance without re-read.
**The user will NOT repeat any of this each session.** Apply every time.

**Priority hierarchy (S60 — NEW)**:
1. **Quality / correctness** — ABSOLUTE TOP. Never compromised.
2. **Cost ($)** — top operational driver. Hobby project; pod $ comes out
   of pocket. Fast/cheap infra compounds across experiments. Prioritize
   infra investment that compounds even if calendar-slow.
3. **Calendar time** — LAST. "Nice to be sooner" but not a constraint.
See `feedback_priority_hierarchy.md`.

**The trajectory (every decision serves this)**:
  Phase 1 v3 (DONE) → Phase 2 elite gen 9 (NEXT, must work first time)
  → Multi-gen (5-7wk / $400-600 per run) → Doubles/VGC/Triples/AG/in-game

**No shortcuts. Ever. (both directions)**:
- No "good enough for now" / half-finished work
- No SPECULATIVE additions either — adding flags/features without
  measured need is also a shortcut. Both create touched-again work.

**No assumptions. Label CONCRETE / STRONG INFERENCE / CONJECTURE**:
- CONCRETE = directly measured this session, citing tool + sample size
- STRONG INFERENCE = measurement-derived + a standard assumption
- CONJECTURE = guess based on architecture or analogy
- Don't promote conjecture to concrete when summarizing. Flag measurement
  confounds explicitly (tool overhead, scaling caveats, etc.).
See `feedback_profile_driven_method.md`.

**Don't be married to any approach. Willing to reframe when data refutes**:
- S59 reframed Track B three times (sim → WS → orchestration). Each
  reframe = methodology working, not failure.
- S60 reframed Fix #3 (vectorize collate) from "low-risk warm-up win" to
  REFUTED in ~1 hour via microbench. Save the work, move on.

**Verify before recommending OR acting**:
- Memory snapshots are point-in-time. Code is truth. Grep / read before
  citing.
- Verify state BEFORE acting, not just before recommending. SSH exit
  codes can lie. File existence can change between sessions.
- Save artifacts BEFORE destructive cleanup. 30s save cost vs hours of
  rework if data is lost.

**Honest opinions, including pushback**:
- "It's me and you against the problem" — partnership, not deference.
- If user framing has a precision-bug or premise-bug, surface it gently
  and clarify before acting. Pattern rewarded in S52/53/54/57/59/60.
See `feedback_ask_when_uncertain.md`.

**Ask before destructive or load-bearing actions**:
- Cost of 60s clarifying question << cost of irreversible mistake.
- Especially for: kill / reset --hard / force push / dropping data /
  changing load-bearing prod paths.

**Step back periodically**:
- Triggers: stuck >30 min, user pushes back on plan, multiple components
  seem broken in sequence, cost projections keep being wrong.
- Re-read memory + this file + strategic frame. Consider if a different
  approach resolves multiple issues at once.

**"Done" means done**:
- Code clean (no half-completions, no "TODO fix later")
- Validation gate met (correctness A/B for risky changes)
- Docs that reference the area updated
- Memory updated if behavior or rationale changed
- next-prompt updated if next session needs to know

**Diagnostic on isolation branch; findings/scripts on master via cherry-pick**:
- Don't pollute master with exploratory diagnostic code.
- S59 + S60 pattern: diag code on `diag/*` branch; reusable bench
  scripts + findings docs cherry-picked to master.

**Don't call plateau / regression on <20 iters of data (S60 — NEW)**:
- Self-play training is NOISY. -5 Elo on 500-game ladder is within CI.
- Check per-opponent breakdown, not just aggregate. Stylistic shifts
  (better vs some, worse vs others) ≠ regression.
- Apply this to inherited framings too (don't propagate prior session's
  premature framings uncritically).
See `feedback_premature_plateau_call.md`.

**Infra-first means INFRA FIRST (S60 — NEW)**:
- After landing an infra fix, the next question is "what's the next infra
  fix?", NOT "what does Phase 2 prep need?"
- Phase 2 prep is POST-ALL-INFRA. Don't interleave.
See `feedback_infra_first_means_first.md`.

**Perm-at-eval is conceptually WRONG — do not propose it (S60 — NEW)**:
- The next-prompt §S58 prescription is REFUTED.
- Perm exists for invariance (TRUE Pokemon learning), not measurement-
  fudging. Eval should match deployment distribution (canonical).
See `feedback_perm_at_eval_wrong.md`.

═══════════════════════════════════════════════════════════════════════════════
S60 — PHILOSOPHY REFINEMENTS (NEW MEMORY ENTRIES — READ THESE)
═══════════════════════════════════════════════════════════════════════════════

S60 surfaced four philosophy refinements / corrections via user pushback.
All saved as memory files. Apply on every decision going forward:

1. **`feedback_priority_hierarchy.md`** — **Priority hierarchy**:
   quality > cost > calendar. Hobby project, pod $$ comes out of pocket,
   so fast/cheap infra COMPOUNDS across experiments. Calendar speed is
   LAST. Resolves "ship fast vs no shortcuts" tension: do infra right,
   even if calendar-slow, because cost compounds and quality is
   non-negotiable.

2. **`feedback_perm_at_eval_wrong.md`** — **Perm-at-eval is conceptually
   WRONG**, don't propose it. The next-prompt §S58 prescription
   ("+7pp smart_avg easy win") has been REFUTED. The whole reason we
   apply perm during training is to teach TRUE Pokemon learning
   (invariance), not for measurement-fudging. Adding perm at eval MASKS
   the symptom that the model isn't truly invariant. Other Pokemon PPO
   codebases (Metamon, ps-ppo) keep eval canonical. The §S58 section
   has been annotated with the refutation.

3. **`feedback_premature_plateau_call.md`** — **Don't call a plateau /
   regression on <20 iters of data.** S58 wrap framed the BC anchor v3
   run as "smart_avg plateau at 70%" based on iter 29 → iter 50 = -2pp
   (one eval, within noise). I propagated that framing in S60, then
   added an iter 49 Elo "regression" framing (-5 Elo from iter 39 peak).
   Both were premature: per-opp breakdown showed iter 49 vs iter 39 is
   a stylistic shift (better at SH/Tactical, worse at SmartDmg/Strategic,
   mean wr -0.6pp = within noise). -5 Elo is within 500-game CI.
   Self-play training is NOISY; small windows are not trends.

4. **`feedback_infra_first_means_first.md`** — **Infra-first means INFRA
   FIRST**, not "do the easy infra and then jump to Phase 2 prep." After
   Fix #2 shipped, my instinct was to propose Phase 2 prep (task #14,
   elite teams, etc.). User correctly pushed back: Fix #1 (IPC redesign)
   is the next priority per the priority hierarchy. Phase 2 prep is
   POST-ALL-INFRA, not interleaved.

═══════════════════════════════════════════════════════════════════════════════
S60 — WHAT'S TRUE NOW (CORRECTIONS TO OLDER FRAMINGS)
═══════════════════════════════════════════════════════════════════════════════

Older sections of this file may still contain these stale framings.
Where you see them, trust the S60 correction:

| Older framing (now stale) | S60 correction |
|---|---|
| "Perm-at-eval is an easy +7pp Phase 2 prep item" (§S58 #6) | REFUTED — perm-at-eval is conceptually wrong; do not propose. The §S58 section has been annotated. |
| "BC anchor v3 plateaued at smart_avg=70%" (S58 wrap) | Premature — 10 iters not enough for plateau call. Iter 49 Elo confirms stylistic shift, not regression. |
| "BC anchor + --tier3 --compile is FATAL" (train_rl.py mutex) | RESOLVED — Fix #2 plumbs BC through compile boundary; mutex removed. |
| "Fix #3 (vectorize collate) ~10% update savings" (PROFILE_BOTTLENECKS_REPORT §5) | REFUTED — Python-level vectorization doesn't capture the time. PROFILE_BOTTLENECKS_REPORT.md §5 has been annotated. |
| "Phase 2 prep after Fix #2" (my S60 instinct) | Wrong — Fix #1 design session is next. Phase 2 prep is post-all-infra. |

═══════════════════════════════════════════════════════════════════════════════
S60 — STATE AT WRAP
═══════════════════════════════════════════════════════════════════════════════

**Git state**:
- master HEAD: should be updated to include the perf branch merge (this
  wrap session). Was `fb88d795` (S60 diag scripts) before perf merge.
- Branch `perf/compile-bc-anchor-composition`: 2 commits
  (`cbc9ec01` main + `a6743f0d` followup that removed a stale
  NotImplementedError guard). Merged to master via this wrap.

**Prod pod state (port 47913)**:
- No python training running; killed cleanly after smoke
- 8 battle_servers listening on ports 9000-9007 (idle)
- GPU clear
- HEAD on pod = `perf/compile-bc-anchor-composition` after smoke validation;
  needs `git checkout master && git pull` before next session if used
- Stashed pod-side edits (eval-registry appends + S59 launch_profile_run.sh
  edits) are floating. Not popped. Recoverable via `git stash list` if needed.

**Dev pod state (port 19373)**: untouched in S60. Available for Fix #1
implementation when that lands.

**Last Elo ladder run**: iter 49 of BC anchor v3 = 1172.3 (-5.0 from iter
39 peak 1177.3). NOT a regression — within 500-game CI. Per-opp
breakdown shows stylistic shift (see S60 corrections table above).

═══════════════════════════════════════════════════════════════════════════════
S60 — NEXT SESSION (S61) PRIORITY: FIX #1 DESIGN SESSION
═══════════════════════════════════════════════════════════════════════════════

**Per `feedback_infra_first_means_first.md`: do NOT jump to Phase 2 prep.**
Fix #1 (Worker↔CIS IPC redesign) is the next infra item.

**What S61 should do (no code; design + discussion only)**:

1. **Load the load-bearing CIS code into context**:
   - `pokemon-ai-starter/pokemon-ai/src/mp_centralized_collect.py` (Phase 4.6 CIS)
   - Worker-side mp.Pipe recv_loop code
   - `memory/project_cis_4_3_design.md` (pool-mirror multi-slot)
   - `memory/project_cis_4_6_design.md` (Option B unified recovery)
   - `memory/feedback_cis_thread_safety.md` (pipe IPC + Lock + async dispatch)
   - `memory/feedback_asyncio_gather_at_scale.md` (S58 architectural rule)

2. **Compare IPC redesign options** (per PROFILE_BOTTLENECKS_REPORT.md §5):
   - **Worker-side submission aggregation**: batch the 25 concurrent
     battles' inference requests into fewer larger CIS submissions
   - **CIS batching window tuning**: bump `timeout_ms` (currently 15ms)
     so CIS forms larger batches per round-trip
   - **Shared-memory ring buffer**: replace mp.Pipe with shared-mem
     (faster IPC mechanism)
   - **Unix domain socket binary protocol**: different IPC mechanism
   - **Hybrid**: combinations of the above

3. **Design the correctness A/B gate** before any implementation:
   - Same init/seed/hyperparams as a baseline run
   - N-iter A/B against current master at production scale
   - smart_avg + per-opp Elo must match within noise OR strictly improve
   - This is the absolute red line per `feedback_priority_hierarchy.md`

4. **OUTPUT of S61**: a design memo (`memory/project_s61_fix1_design.md`)
   that picks the lowest-risk path capturing most of the 30-60% collect
   gain. User reviews + approves before any code.

5. **DO NOT START implementation in S61** — design first, code in S62+.

═══════════════════════════════════════════════════════════════════════════════
END S60 WRAP — older sections below preserved for historical context
(but trust S60 wrap + memory entries OVER older speculation)
═══════════════════════════════════════════════════════════════════════════════

═══════════════════════════════════════════════════════════════════════════════
⚠️  S59 WRAP (2026-05-13) — AUTHORITATIVE READ FIRST
═══════════════════════════════════════════════════════════════════════════════

**Do NOT trust older sections of this file uncritically.** Multiple framings
that read as fact in older sections (S55-S58) have been REFUTED by S59
measurement. The "task #22 simulator rewrite" framing is the canonical
example — it appeared as fact in S58 but rested on a conjectural "18× speed
gap" number with no measurement source.

**The authoritative S59 outputs**:
- `docs/PROFILE_BOTTLENECKS_REPORT.md` — concrete profile findings with
  CONCRETE/CONJECTURE labels per claim
- `memory/project_track_b_findings_s59.md` — recon outcome summary
- `memory/feedback_profile_driven_method.md` — methodology that caught it

**Headline findings (S59)**:

1. **WS layer is NOT the bottleneck.** Round-trip = 0.165ms localhost,
   6000 req/sec. Per-iter WS cost <1%.

2. **Showdown sim is NOT the bottleneck.** Direct BattleStream ceiling =
   529-623 turns/sec/Node; multi-Node ~5000. Our prod = 22-41 turns/sec.
   **We use ~0.5-1% of sim's available throughput.**

3. **THE bottleneck = workers blocked on mp.Pipe IPC waiting for CIS
   responses.** Worker_0 viztracer at 800g/8w: **82% of wall time in
   `_ConnectionBase.poll`**. CIS GPU at 57% util — NOT saturated. The
   issue is **round-trip latency × number-of-round-trips**, not CIS speed.

4. **Update phase ~70% GPU-bound, 11% in `collate_episodes` Python loop,
   5% backward Python-side, ~14% misc.** GPU-bound portion is partly
   addressable via torch.compile (task #12) but bulk is fundamental.

**Top 3 fixes (see PROFILE_BOTTLENECKS_REPORT.md §5 for ROI table)**:
1. **Worker↔CIS dispatch redesign** — 30-60% collect savings, 1-2 wk,
   HIGH risk (load-bearing prod path), **design session before code**
2. **Task #12: BC anchor + Tier3 + compile composition** — 15-25% update
   savings, 1-2 days, medium risk
3. **Vectorize `collate_episodes`** — 10% update savings, 2-3 days, low risk

**Recommended sequencing for S60+**:
1. **Start with Fix #3** (collate_episodes) — low risk, independent, validates
   profile-driven workflow
2. **Then Fix #2** (compile composition) — biggest update gain, fast
3. **Design discussion for Fix #1** — comparing IPC redesign options
   (shared mem ring buffer vs explicit submit-aggregation vs Unix socket
   binary protocol vs hybrid) — no code until design approved
4. **Fix #1 implementation** across multiple sessions

**REFUTED claims (do NOT act on these from older sections)**:
- ❌ "18× speed gap to ps-ppo" — number was conjectural, no source
- ❌ "Task #22 Replace WS+Node Showdown stack with direct simulator
  binding" — wrong layer; sim isn't the issue
- ❌ "Foul-play Rust engine FFI is the real fix" — would attack wrong layer
- ❌ "Phase 3 blocker is the sim stack rewrite" — Phase 3 may still need
  throughput work but NOT this specific rewrite

**S59 also accomplished**:
- v4_freshbs killed + archived at `data/cloud_runs/003_bc_anchor_v4_freshbs/`
  (BC anchor validated; iter 49 + 50 in Elo ladder + smart_avg=70%)
- Diagnostic branch `diag/profile-bottlenecks-s59` pushed (env-var-gated
  viztracer hooks + bench scripts + analysis tools — selectively
  cherry-pick to master if any becomes permanent diagnostic)

**Diag branch state at S59 wrap**:
- Branch `diag/profile-bottlenecks-s59` head: `7e3423b4` (SIGUSR1 patch)
- master HEAD: `7afa3395`
- No fix code shipped to master yet — fixes start in S60

**Pod state at S59 wrap**: Pod up (port 47913), python killed, GPU clear,
8 battle_servers may still be alive. Safe to restart cleanly when fix
work resumes.

**Philosophy reaffirmed + expanded in S59** (per user explicit framing —
also mirrored in MEMORY.md + `feedback_profile_driven_method.md`):

- **No shortcuts. Ever.** Two directions:
  (a) Don't ship "good enough for now" / half-finished work
  (b) **Don't ADD speculative features either.** S59 example: user rejected
      lr-decay / perm-at-eval / SE-rate-stop / entropy-cap flags because
      no measured need. Speculative additions are shortcuts too, just
      inverted. Both create touched-again work.

- **No assumptions. Label CONCRETE vs CONJECTURE.** Every perf claim,
  every "X is the bottleneck", every inherited number — tag with evidence
  source OR mark conjectural. S59 caught a multi-session "18x speed gap
  to ps-ppo" framing with NO measurement source that would have triggered
  a 2-4 wk rewrite of the wrong layer.

- **Don't be married to any approach or idea.** Willing to reframe when
  data refutes. S59 reframed Track B three times (sim rewrite -> WS
  rewrite -> orchestration fix). Each reframe was the methodology
  working, not failure.

- **Verify before recommending from memory/docs.** Older sections of
  next-prompt.txt + various docs/*.md have outdated or conjectural content.
  Newer findings override older speculation. When in doubt, grep current
  code or run a measurement.

- **Honest opinions, including pushback when user's premise is buggy.**
  Partnership tone — "me and you against the problem". The user has
  explicitly rewarded pushback in S52, S53, S54, S57, S59.

- **Ask before destructive or load-bearing actions.** Cost of 60s
  clarifying question << cost of irreversible mistake on wrong premise.

- **Step back periodically.** Strategic reflection on the WHOLE approach,
  not just the file in front of you. Triggers: stuck >30 min, user
  pushback on plan, multiple components seem broken in sequence, cost
  projections keep being wrong. S57 + S59 both had productive step-backs.

- **The pod is paid time.** Quality > speed; cost discipline =
  thoroughness, not haste. Don't run up the bill on re-investigated
  settled questions OR on hypothetical fixes without measurement.

- **"Done" means done.** Complete + validated + documented + handoff
  captured. Not "works for now".

- **Long-term focus on every change.** Every change should serve the
  trajectory: Phase 1 v3 (done) -> Phase 2 elite gen 9 -> multi-gen ->
  other formats. If a change doesn't serve that or would need touching
  again later, re-evaluate before doing it.

- **Verify state BEFORE acting, not just before recommending.** SSH exit
  codes can lie (255 on a dropped connection doesn't mean the command
  failed). File existence can change between sessions. Before any
  non-trivial action: check actual pgrep / ls / git rev-parse output.
  Cheap to verify; expensive to act on wrong state.

- **Save artifacts BEFORE destructive cleanup.** Always scp logs / pull
  snapshots / write MANIFEST.txt BEFORE pkill / SIGKILL / overwriting.
  S59 example: archived v4 run + profile JSONs before clearing pod state.
  Cost of saving: 30s. Cost of losing irreplaceable data: hours of rework.
  Also applies to failed/superseded attempts — keep in archive (e.g.,
  `data/profiling/round1/`), don't delete.

- **Use three-way evidence labels: CONCRETE / STRONG INFERENCE /
  CONJECTURE.** Binary is too coarse. STRONG INFERENCE = measurement-
  derived + a standard assumption (e.g., "70% GPU-bound" inferred from
  python-vs-wall gap). Don't promote conjecture to concrete when
  summarizing.

- **Flag measurement confounds explicitly.** Every profiling tool adds
  overhead; every benchmark has scaling caveats. Surface caveats alongside
  numbers, not hidden away. S59 example: "viztracer adds 20-30% overhead
  — absolutes inflated, relatives reliable" stated alongside every
  viztracer number.

- **Diagnostic code on isolation branch; findings/scripts on master via
  cherry-pick.** Don't mix exploratory diagnostic code with production
  master. S59 pattern: profile_hook + train_rl injections stayed on
  `diag/profile-bottlenecks-s59`; docs/findings + reusable bench scripts
  cherry-picked to master. Lets us experiment without polluting master
  while still merging the durable value.

- **Make sure next session knows exactly what to do.** This section is that.

═══════════════════════════════════════════════════════════════════════════════
END S59 WRAP — older sections below preserved for historical context
(but trust S59 wrap + project_track_b_findings_s59.md OVER older speculation)
═══════════════════════════════════════════════════════════════════════════════

⚠️ **STANDING-ORDER ARCHITECTURE (set up S55, project-eternal)**:

The user has explicitly set up `MEMORY.md` (auto-loaded every session) + `memory/project_session_boot_protocol.md` (mirrored at `docs/SESSION_BOOT_PROTOCOL.md`) so future sessions inherit FULL context — philosophy, sequencing, DO/DO NOT (with WHY), locked decisions, plan trajectory — WITHOUT the user repeating anything. **The user will not remind you of fundamentals every session.** Apply the boot protocol on every decision.

**Mandatory reads at session start, in this exact order (also listed in MEMORY.md ⚠️ READ FIRST)**:
1. `memory/project_session_boot_protocol.md` — STANDING ORDERS. Read FIRST. Internalize on every session.
2. `memory/feedback_engineering_standard.md` — quality standard
3. `memory/feedback_ask_when_uncertain.md` — when to ask vs guess
4. `memory/project_strategic_frame.md` — locked sequencing (vertical → horizontal)
5. `memory/project_phase1_v3_diagnosis.md` — S57 collapse diagnosis (3-stage type-knowledge erosion). Required before any Phase 2 design discussion.
6. `memory/project_bc_anchor_design.md` — S57 BC anchor mechanism + 5 verified failure modes. Required before any BC anchor / Tier 3 / forward_ppo_sequence work.
7. `memory/project_s58_session_narrative.md` — **S58 full narrative: ghost-tie root cause + 3 fixes shipped + open post-restart slowdown investigation. Read BEFORE touching the cis collector path, BEFORE relaunching Phase 2, or BEFORE investigating wall-time.**
8. `memory/feedback_asyncio_gather_at_scale.md` — **architectural rule from S58: don't fanout heterogeneous coros via asyncio.gather; use process-per-task.**
9. `memory/project_tier3_c5_design.md` — Tier 3 compile boundary
10. `memory/reference_cloud_pods_usage.md` — pod connections, file paths, launch patterns, what NOT to do
11. THIS FILE (`next-prompt.txt`) — session-specific state, S58 wrap + S59 entry
12. `docs/PHASE1_V3_OBSERVATIONS.md` — Phase 2 design (NOTE: predates S57 diagnosis — use `project_phase1_v3_diagnosis.md` for the corrected version.)
13. **First action (S59)**: SSH prod and check `/tmp/bc_anchor_1600g_v4_freshbs.log` for iter 50 completion. The iter line collect time tells us whether fresh battle_servers fixed the post-restart slowdown. See `project_s58_session_narrative.md` Phase J for the experiment design + interpretation rules.

If files conflict, surface — don't silently reconcile. Don't half-ass. Don't shortcut. Don't pivot to deferred items (§4 + §5 of boot protocol enumerate exactly what's now vs deferred).

---

**S58 wrap (2026-05-13) — S59 entry. Read `memory/project_s58_session_narrative.md` for the FULL narrative; this is the summary.**

S58 took the BC-anchored Phase 2 launch from "validated on dev, broken on prod" through three production-blocker fixes, ultimately landing a Phase 2 run that's training cleanly but with anomalously slow wall-time. The slowdown investigation is open at session wrap.

═══════════════════════════════════════════════════════════════════
S58 — THINGS WE TRIED, WHY, AND RESULTS
═══════════════════════════════════════════════════════════════════

**Problem we entered with**: prod 1600g run was producing 36-76% "ties" per iter as pool grew past 4-5. Phase 1 v3 collapse pattern feared.

**Investigation chain**:
1. Ruled out turn-cap forfeits (math impossible: 780 ties × 300 turns >> total step count)
2. Ruled out CIS inference timeouts (only 442 timeout events across 42 iters, can't explain thousands of ties)
3. Ruled out Showdown `|tie` emissions (0 occurrences in any battle_server log)
4. Ruled out WS disconnects (literal count = 0)
5. Ruled out Endless Battle Clause (0 mentions in logs)
6. Added `[CIS-STATS]` instrumentation → confirmed CIS forward ~16ms healthy, NOT the bottleneck
7. Added `[GHOST-END]` instrumentation for empty-traj battle finishes → captured ZERO events despite hundreds of "ties" — meaning `_battle_finished_callback` wasn't being called for the ghost games
8. **Found root cause by reading code**: `mp_centralized_collect.py:2049` `asyncio.gather()` of 5 opp coros per worker, sharing one event loop, made small-batch opps starve. Their per-batch `asyncio.wait_for(max(300, n_for_opp*30))` fired before completion → cancelled batches → `battle.won=None` → counted as "ties" in iter line via `opp_t = sum(1 for b in player.battles.values() if b.won is None)`
9. **Verified industry standard against Metamon** (`metamon_ref/metamon/rl/self_play/launch_models.py`): one subprocess per agent. No in-process opp-level asyncio. Confirmed our `asyncio.gather` pattern is the unusual choice.

**Fix #1 — one-opp-per-worker dispatch** (commit `548033e8`):
- Added `_allocate_opps_to_workers(opp_pool_msg, n_workers, total_games)` — PFSP-weighted allocation, each worker gets exactly one opp + game count
- Both orchestrator dispatch sites updated (sync + bg-collector pipeline)
- Each worker's existing `asyncio.gather(opp_coros)` now runs ONE coro → no contention
- Tested at 8w validation: iter 40 = 1599/1600 trajs, 0 ties, 28 min collect
- Tested at 15w validation: iter 40 = 1599/1600 trajs, 0 ties, 20 min collect (faster due to better balance)

**Fix #2 — defensive cmds_sent filter** (commit `641de8b8`):
- Phase 2 launch attempt at 15w hit a NEW bug: iter 41 → 10 workers crashed mid-iter → reset → attempt 2 → KeyError 0 at `_cis_wait_results_and_aggregate:2499` dict comprehension
- Root cause: on retry after `CISResetNeeded`, `self._cmds_sent` had stale wids not in the fresh `manager.result_pipes`
- Defensive patch: filter `cmds_sent` to valid wids; log WARN if it triggers
- Doesn't fix the underlying cmds-sent-staleness (still unresolved — task #21 includes this) but prevents run-killing crashes

**Fix #3 — deadline 8.0× multiplier** (commit `d092c4c5`):
- After fix #2, Phase 2 launch at 8w hit deadline-too-tight: iter 40 attempts 1 AND 2 both timed out at the 1600s deadline (`4.0 * expected_collect_s`)
- Cold start (CIS spawn + 6-slot model loads + 8 worker spawns + WS setup) takes 60-90s, pushing total collect past 1600s
- Validation 8w iter 40 took 1704s — past the same deadline, made it through by chance only
- Bumped multiplier 4.0 → 8.0. Deadline now 3200s = 53 min, comfortable cold-start absorbance.
- Steady-state iters finish in ~1200s so the higher multiplier only matters for cold starts

**Phase 2 v3 launch** (out-dir `bc_anchor_1600g_v3`):
- Resume from `snapshot_0039.pt` with all three fixes
- iter 40-49: ALL CLEAN. 0 ties, 1600 games each, win rate climbing 52.6% → 56.5% (peak iter 48) → 53.8% (iter 49)
- Snapshot saved at iter 49: pool grew 5 → 6 (`snapshot_0049.pt`)
- **First in-training eval at iter 50: smart_avg = 70%** (SH 76, SmartDmg 70, Tactical 68, Strategic 68) — comparable to iter 29's 70.2%, slightly better on SmartDmg/Tactical, slightly worse on Strategic. 10 iters of training produced ~0 net change in smart_avg.

═══════════════════════════════════════════════════════════════════
S58 — WHERE WE ARE AT (session wrap)
═══════════════════════════════════════════════════════════════════

**The post-restart wall-time slowdown is PARTIALLY resolved** (mixed evidence — was prematurely claimed "resolved" earlier in this doc):
- Validation 8w iter 40 collect: 1704s = 28 min
- Prod v3 iter 40-49 collect: 2540-3201s = 42-53 min (slowdown era)
- **Fresh-bs iter 50 collect: 1759s = 29.3 min** ← matches validation (HONEYMOON)
- **Fresh-bs iter 51 collect: 2601s = 43 min** ← drift back, but still better than v3
- **Net read**: fresh battle_server restart helps (50min → 43min steady ≈ 14% saved), but doesn't fully restore validation pace. Iter 50 may have been a one-off post-restart fast iter.
- **Operational rule**: still useful to restart battle_servers when training is killed (recovers SOME throughput). But don't assume it fully fixes the slowdown.

**S59 first action**: SSH prod, check `/tmp/bc_anchor_1600g_v4_freshbs.log` for iter 52+ status. If iter 52 stays around 40-43 min → 43 min is the steady-state for this allocation pattern (8w with imbalanced workers). If iter 52 climbs back toward 50 min → state-accumulation is faster than expected, may need every-N-iters defensive restart (which is non-trivial coordination with in-flight training).

═══════════════════════════════════════════════════════════════════
S58 — THINGS THAT STILL NEED OPTIMIZATION
═══════════════════════════════════════════════════════════════════

1. **Post-restart slowdown** — PARTIALLY resolved. Fresh battle_server restart helps (50min → 43min steady), but doesn't fully restore validation 28min pace. Iter 50 hit 29min (one-off?), iter 51 dropped to 43min. Need iter 52+ data to see if 43min is the floor at this allocation/pool size. Long-term fix probably structural (task #22 simulator stack).

2. **15-worker mid-iter crash** (task #21) — UNRESOLVED. At `--mp-workers 15`, iter 41+ had ~10 workers stall mid-iter (5/15 received at deadline). Validation iter 40 at 15w completed cleanly, so the bug is iter-41+ specific or post-resume-specific. Need controlled experiment to reproduce. MUST fix before Phase 3 launch.

3. **Recovery-path cmds_sent staleness root cause** (folded into task #21) — defensive patch in place but the actual race condition (cmds_sent outliving a reset cycle) isn't pinned. Investigate properly before Phase 3.

4. **PFSP allocator polish** — at `--mp-workers 8 + n_opps=5`, allocations like `[2,2,2,1,1]` workers/opp produce per-worker game counts `[160,160,160,160,160,160,320,320]`. Max-per-worker = 320 = bottleneck for collect time. A smarter allocator could shift opp game totals slightly to balance workers (~20-30% wall-time saving at 8w). Moot at 15w if we fix the 15w bug.

5. ~~**18× speed gap to ps-ppo** (task #22)~~ **REFUTED IN S59** — see `docs/PROFILE_BOTTLENECKS_REPORT.md` + `memory/project_track_b_findings_s59.md`. The "83 states/sec" figure had no source (actual prod = 22-41 turns/sec). The gap to ps-ppo is mostly architectural (they're stateless, Random Battles, no temporal) NOT closable by infra. Sim ceiling is 5000+ turns/sec — we use 0.5-1% of it. WS round-trip = 0.165ms. **The real bottleneck is workers blocked on `mp.Pipe` IPC waiting for CIS responses (82% poll-wait). Different fix entirely (orchestration, not sim rewrite).** Old paragraph preserved below for context only:
~~our ~83 states/sec vs ps-ppo's ~1500 on similar hardware. Structural: WS+Node+poke-env+asyncio stack is the bottleneck. Phase 3 (multi-gen) at current throughput would take 3-4 weeks per run = unsustainable. Real fix: direct simulator binding (foul-play Rust engine in `foul_play_ref/`, or rewrite battle_server.js critical path in C). 2-4 weeks of focused engineering. **MUST happen before Phase 3 launch.**~~

6. **Smart_avg ceiling at ~70%** — v3 went 10 iters without smart_avg improvement. Could indicate BC anchor over-constraint (coef=0.1 too high), insufficient pool diversity (PFSP of self-snapshots only), or genuine plateau. Worth a coef ablation (0.05 / 0.1 / 0.2) OR adding Metamon bots to PFSP (task #14) BEFORE the next Phase 2 attempt.

═══════════════════════════════════════════════════════════════════
S58 — KEY COMMANDS (preserved for S59)
═══════════════════════════════════════════════════════════════════

**SSH to prod**: `ssh -i ~/.ssh/id_ed25519 -p 47913 -o StrictHostKeyChecking=no root@195.26.233.30`

**Full kill + restart battle_servers** (use BEFORE any Phase 2 relaunch if battle_servers have been beat up):
```bash
pkill -KILL -f "python.*train_rl"
pkill -9 -f "node battle_server"
pkill -9 -f "SCREEN.*bs_"
pkill -9 -f "SCREEN.*battle_server"
sleep 3
cd /workspace/team_builder/pokemon-ai-starter/pokemon-ai/src
screen -dmS battle_server bash -c "node battle_server.js 2>&1 | tee /tmp/battle_server.log"
sleep 1
for p in 9001 9002 9003 9004 9005 9006 9007; do
    screen -dmS bs_$p bash -c "node battle_server.js --port $p 2>&1 | tee /tmp/battle_server_$p.log"
    sleep 0.3
done
sleep 5
ss -tlnp 2>/dev/null | grep -E ":(900[0-7])"  # verify all 8 listening
```

**Phase 2 relaunch command** (validated config, 8 workers):
```bash
cd /workspace/team_builder/pokemon-ai-starter/pokemon-ai/src
nohup python -u train_rl.py \
  --resume <PATH_TO_SNAPSHOT.pt> \
  --bc-anchor-ckpt data/models/bc/v10_padded_for_cis_dev.pt \
  --bc-anchor-coef 0.1 \
  --device cuda \
  --servers 9000,9001,9002,9003,9004,9005,9006,9007 \
  --bf16 --cis --pipeline --mp-workers 8 --tier3 --tier3-minibatch-size 16 \
  --games-per-iter 1600 --max-concurrent 200 \
  --n-iters 200 --warmup-iters 0 \
  --lr 1e-5 --lam 0.95 --ent-coef 0.02 --target-kl 0.03 \
  --grad-accum 1 --turn-cap 300 \
  --reward-style terminal \
  --eval-interval 10 --eval-games 200 --eval-team-set metamon-competitive \
  --snapshot-interval 10 \
  --early-stop --early-stop-patience 3 \
  --procedural-teams /workspace/raw_data/pokemon_usage/2024-04 \
  --adaptive-entropy --adaptive-entropy-low 0.65 --adaptive-entropy-high 0.95 \
  --win-rate-mode ema --win-rate-ema-alpha 0.3 --win-rate-ema-window 50 \
  --out-dir data/models/rl_v10/<RUN_NAME> \
  > /tmp/<RUN_NAME>.log 2>&1 &
```

DO NOT use `--mp-workers 15` until task #21 (15w mid-iter crash) is resolved.

**Monitoring (greps)**:
- `grep -E "^\[..:..:..\] Iter " /tmp/<LOG>.log` — iter completions
- `grep "active opps" /tmp/<LOG>.log` — orchestrator dispatch summaries
- `grep -E "reset attempt|gave up" /tmp/<LOG>.log` — recovery events
- `grep "CIS-STATS" /tmp/<LOG>.log | tail -12` — CIS latency (every 60s)
- `grep -c "\[TIE\]\|\[GHOST-END\]\|\[TURN CAP\]\|\[FORFEIT\]" /tmp/<LOG>.log` — instrumentation events
- `grep "WARN iter.*cmds_sent had stale" /tmp/<LOG>.log` — defensive recovery triggers (should be rare; if it fires often, investigate)

═══════════════════════════════════════════════════════════════════
S58 — ELO + EVAL RESULTS THIS SESSION
═══════════════════════════════════════════════════════════════════

**ELO ladder data** (under `pokemon-ai-starter/pokemon-ai/src/data/eval/registry/`):
- `elo_v10_500g_focused_plus_iter29.json` — top: rl_v10_1600g_iter29 = **1162.8** [1151.9, 1173.2]
- `elo_v10_500g_focused_plus_iter39.json` — iter 39 added (computed during S58, not re-checked at wrap)
- Phase 1 v3 trio in ladder (padded for D1+D2): iter19=1142.7, iter39=1049.7, iter59=940.3 (confirms collapse trajectory)

**In-training evals** (200-game vs metamon-competitive bots):
- iter 29 (resume baseline, original 1600g run): smart_avg=72%
- iter 50 (v3 run, after 10 effective policy iters): smart_avg=**70%** (SH 76, SmartDmg 70, Tactical 68, Strategic 68)
- Net change: ~0pp average. v3's 10 iters did NOT move smart_avg. Investigate before Phase 2 v4.

**Win rate vs PFSP pool over v3 iters 40-49**: 52.6 → 51.8 → 53.4 → 52.7 → 52.1 → 54.1 → 54.8 → 56.0 → 56.5 → 53.8. Net positive, slow trend.

═══════════════════════════════════════════════════════════════════
**S57 wrap (2026-05-10) — preserved below for full historical context.**
═══════════════════════════════════════════════════════════════════

This was a long, dense session. Multiple bugs found and fixed. BC anchor designed + implemented. Phase 1 v3 finished. Diagnosis revised from `docs/PHASE1_V3_OBSERVATIONS.md`. **A real validation experiment is in flight on dev pod RIGHT NOW** — first eval data lands ~1.5 hr from S57 end (around iter 9). Don't kill it without checking; check status first.

(NOTE: the "in flight" experiment above WAS the S57 BC anchor isolation run; it has long since completed. The S58 narrative above carries forward from that point.)

═══════════════════════════════════════════════════════════════════
PHILOSOPHY (apply to EVERY decision, no exceptions)
═══════════════════════════════════════════════════════════════════

The user has been EMPHATIC about this throughout S57. Quoting:

  "no shortcuts -> in order to reach the optimal long term solution
  that is truly correct and optimal with no compromises on training
  - if we intend to revisit things, we would rather do them now -
  cause of the pods that are rented now and may not be later -
  multi gen needs to be fully ready - with no need to go back and
  fix or optimize fundamental things"

  "this is the only chance we are getting for the real deal final
  solution"

  "remember no shortcuts - we dont have these dev pods or cloud pods
  forever - make sure next session knows that - and that this is the
  run to the final form"

### LONG-TERM TRAJECTORY (the load-bearing context)

  Phase 1 v3 (DONE)        →  V1 baseline measurement (failed via
                              type-knowledge erosion; collapse diagnosed S57)
  Phase 2 (NEXT)           →  ELITE GEN 9 — the run to the final
                              SINGLE-FORMAT-SINGLE-GEN form. With BC anchor +
                              Tier 3 minibatching + external opps replicas +
                              team mix + perm + lr schedule + early-stop refinements.
                              ~$200-300, 5-7 days, must work first time.
  Multi-gen (AFTER Phase 2) →  THE FINAL SINGLE-PLAYER FORM. Gens 6-9 in one
                              model. ~$400-600/run × multiple iterations.
                              Cannot afford to find infra bugs mid-flight here.
  "The other things" (later)→  Doubles / Triples / VGC / AG / in-game battles
                              / team building. Architecture is format-agnostic
                              by construction; this is data + tokenizer
                              extension work, not infra rework.

EVERY decision must serve this trajectory. If a current task doesn't help
get to elite gen 9 → multi-gen final form, OR if it would need to be
touched again later, RE-EVALUATE before doing it.

### TIME WINDOW (the hard constraint)

Dev pod + cloud pods are rented NOW. They may not be later (RunPod
availability fluctuates, project budget is finite). The expensive thing
isn't dev time — it's WAITING to fix things during a paid run, OR
discovering bugs at production scale that block multi-gen. Use the dev
pod NOW, while it's available, to:
- Run the BC anchor validation (in flight)
- Solve task #12 (compiled+minibatch+BC anchor composition)
- Solve task #13 (CIS regression)
- Solve task #14 (external opps replicas — REQUIRED for Phase 2)
- Wire H2H gauntlet, source elite teams, add lr sched / early-stop / entropy cap
- Smoke test full Phase 2 stack at production scale BEFORE Phase 2 launches

### "DONE" MEANS DONE

No "good enough for now." No "we'll fix it later." No "ship and revisit."
If something might need revisiting after Phase 2 ends or during multi-gen,
FIX IT NOW while pods are rented + context is fresh. The cost of doing
it right today is much less than the cost of debugging in production
during a 5-7 day $200+ run, OR finding it during a 5-7 week $400-600
multi-gen run.

### THOROUGHLY UNDERSTAND BEFORE TOUCHING

Per `feedback_engineering_standard.md`: BEFORE any non-trivial code
change, READ:
1. The file you're about to change + its callers
2. The docs that reference the area (cookbook, design memos, memory files)
3. §K dead paths in this file + §5 in `project_session_boot_protocol.md`
   (has this been tried + abandoned?)
4. `project_strategic_frame.md` (does this align with strategy?)
5. If load-bearing or non-obvious: surface options + ask user before committing.

Skipping the read step is how regressions ship. The S57 session uncovered
SIX bugs that would have silently failed Phase 2 — every one was found by
reading code carefully + running real tests at scale. The "look at it
and start typing" anti-pattern would have shipped each of those bugs to
Phase 2 instead.

This includes understanding what we ALREADY HAVE (perm impl exists, just
not enabled at eval; external opps framework exists, just doesn't scale).
DO NOT re-implement what exists. Read it, expose it, extend it.

### STEP BACK PERIODICALLY (S57 lesson — separate from "thoroughly understand")

"Thoroughly understand before touching" is about technical depth on the
file in front of you. **"Step back periodically" is about strategic
reflection on the whole approach.** Different skill, learned the hard way
in S57. Trigger conditions for stepping back:

- **Stuck on a single approach for >30 min** without measurable progress
- **User pushes back on the plan** (especially with a specific question or
  pointed observation — "wait, are we sure X?" or "isn't Y the actual issue?")
- **Multiple components seem broken in sequence** — likely common cause,
  not independent failures (S57: "Tier 3 broken → BC anchor broken → CIS
  broken" all turned out to be SAME BC anchor pollution causing all three)
- **Cost projections keep being wrong** (S57: projected 30 hr → actual 50+ hr
  → re-projected → realized you're projecting on the wrong stack entirely)

When triggered:
1. Stop editing/launching anything
2. Re-read the relevant memory files + this file
3. Re-read `project_strategic_frame.md` (does the current approach serve the long-term goal?)
4. List active tasks (`TaskList`) + ask: is the highest-leverage one still being worked?
5. Consider: is there a totally different approach that resolves multiple issues at once?
6. If still unclear, ASK the user with a clear options summary

S57 example of NOT stepping back: I dropped CIS, dropped --compile, dropped
--pipeline during a debugging cascade — each individually rationalized.
A 5-minute step-back would have caught that the BC anchor bug fix was the
root-cause-resolver, and that re-testing CIS with the FIXED BC anchor would
have unblocked the fast stack. Instead, I burned ~$5-10 of pod time on
slower paths. The user explicitly asked "isn't it possible CIS started
failing because of the BC thing itself, and now that BC is fixed CIS might
work?" — that one question saved hours.

**S57 explicit user instruction (quoting): "be ready to take a step back
look at the tasks and plans for them - if it needs, read through docs and
code - and decide whether there is a better solution to what we need and
want."** Do this periodically. Don't just execute the plan; re-evaluate it
when triggered.

═══════════════════════════════════════════════════════════════════
WHAT'S IN FLIGHT (check FIRST before doing anything)
═══════════════════════════════════════════════════════════════════

**S58 entry (2026-05-10): TWO BC-anchor validation runs in parallel.**
Same stack on both pods (CIS + Tier3 + minibatch=16 + BC anchor 0.1 + bf16
+ pipeline) at different scales. Prod 1600g is the PRIMARY signal (Phase 2
scale); dev 200g is the parallel small-scale baseline.

Status check commands (read these before any action):
```
# PRIMARY: prod pod 1600g
ssh -i ~/.ssh/id_ed25519 -p 47913 root@195.26.233.30 \
  "ps -eo pid,etime,command | grep 'python.*train_rl' | grep -v grep | head -1; \
   grep -E '^\\[..:..:..\\] Iter|EVAL: SH=|FATAL|smart_avg' /tmp/bc_anchor_1600g.log | tail -25; \
   echo === LAST 5 ===; tail -5 /tmp/bc_anchor_1600g.log"

# Parallel: dev pod 200g (out-dir _002 after a relaunch — _001 died at CIS spawn)
ssh -i ~/.ssh/id_ed25519 -p 19373 root@154.54.102.26 \
  "grep -E '^\\[..:..:..\\] Iter|EVAL: SH=|FATAL|smart_avg' /tmp/bc_anchor_iso_v2.log | tail -25; \
   echo === LAST 5 ===; tail -5 /tmp/bc_anchor_iso_v2.log"
```

| Run | Pod | Scale | Out-dir | Log | Notes |
|---|---|---|---|---|---|
| Prod 1600g | port 47913 | 1600g × 200 iters | `bc_anchor_validation_1600g/` | `/tmp/bc_anchor_1600g.log` | PRIMARY. Phase 2 scale. Launched S58 ~2026-05-10 12:00 UTC. Wall-time projection: ~15-25 min/iter sustained (1600g vs 200g, 8× collect scale). Stop anytime user needs the pod. |
| Dev 200g | port 19373 | 200g × 200 iters | `bc_anchor_isolation_002/` | `/tmp/bc_anchor_iso_v2.log` | PARALLEL small-scale signal. Faster eval cadence (~3-4 min/iter measured S57 = first eval iter 9 in ~30 min). Relaunch after `_001` died at CIS spawn timeout (see KNOWN FLAKES below). |

Stack on both: `--cis --pipeline --bf16 --tier3 --tier3-minibatch-size 16 + BC anchor coef=0.1`
- Prod uses `--max-concurrent 200` (8 workers × 200 = 1600 = 1 perfect wave)
- Dev uses `--max-concurrent 50`
- Init from `data/models/bc/v10_padded_for_cis_dev.pt` (zero drift from anchor at iter 0)
- Eval every 10 iters (eval-games=200, metamon-competitive teams)
- Hyperparams identical to Phase 1 v3: lr=1e-5, lam=0.95, ent=0.02, target-kl=0.03

**Acceptance criteria (BOTH runs, same purpose)**:
- WIN: SE rate stays >40% across multiple eval points (vs Phase 1 v3 falling 44.6→30.4 over equivalent iter range) → BC anchor confirmed as the primary fix to type-knowledge erosion
- PARTIAL: SE holds early but degrades later → BC anchor coef may need tuning (try 0.2 or 0.3)
- FAIL: SE drops similar to Phase 1 v3 → diagnosis incomplete; refine before Phase 2

The 1600g signal is more authoritative for Phase 2 (matches launch scale) but iter cadence is slower (~15-25 min vs ~3-4 min). Use the 200g run for FAST iteration on coef tuning if WIN/PARTIAL/FAIL signal needs refining; use 1600g for the GO/NO-GO decision before Phase 2.

**bc_kl will GROW over iters** as model drifts more (anchor pushes back harder). bc_kl going UP is normal and EXPECTED (means anchor is doing its job). Watch SE rate trajectory in evals — that's the signal.

**Note on CIS path history**: S57 morning runs used `--mp` (slow at 200g — 25 min/iter) because CIS appeared broken. The "broken" CIS was actually BC-anchor-state-dict-pollution causing CIS workers to hang during model load. AFTER the BC anchor bug fixes (`d1b101bb` + `51511c18`), CIS works cleanly. **Task #13 is RESOLVED via the BC anchor fixes themselves.** A SEPARATE residual CIS spawn flake exists — see KNOWN FLAKES below.

═══════════════════════════════════════════════════════════════════
KNOWN FLAKES (intermittent, non-blocking, document for future sessions)
═══════════════════════════════════════════════════════════════════

**1. CIS worker spawn timeout (180s ceiling) — borderline on cold load**

Symptom: `RuntimeError: CIS worker 0 not ready in 180.0s` at iter 0 startup.
Log lines AFTER the traceback show `[CIS-multi N=8 slots=17] loaded 17 model
slot(s)` — workers DID finish loading, just past the parent's 180s deadline.

Hardcoded at `mp_centralized_collect.py:2169`:
```python
cis_handles = cis_server.spawn(ready_timeout_s=180.0)
```
Per-worker poll loop at L1356 — first worker that exceeds 180s fails the
whole spawn.

Each worker loads 17 model slots × 20M params (340M params per worker × 8
workers = 2.7B params loaded onto shared GPU). Cold disk cache + concurrent
GPU init contention can push the slowest worker past 180s. Warm-cache runs
typically finish in 30-60s.

S58 evidence: dev pod `_001` instance hit this AFTER the same stack had
completed 3 successful iters at 3 min/iter on a prior run. Same code, same
pod, different outcome — pure flake.

**Mitigation if it persists** (NOT yet shipped — just retry first):
- Bump `ready_timeout_s=180.0` → `300.0` at L2169
- Or expose as CLI flag `--cis-spawn-timeout`
- Or implement retry-on-spawn-flake (2 retries with full kill+restart between)

**Don't relaunch identically more than 2x** without bumping the timeout.
The flake rate is low enough that retry usually works, but if 2 consecutive
launches fail at spawn, edit the code.

═══════════════════════════════════════════════════════════════════
WALL-TIME REALITY (S57 measurements — critical for Phase 2 planning)
═══════════════════════════════════════════════════════════════════

See `memory/reference_walltime_measurements_s57.md` for full table. Headlines:

| Stack | 200g iter wall | per-game cost |
|---|---|---|
| Single-proc (no --mp), OLD code, no BC anchor | 7.6 min | 2.3 sec/game |
| `--mp 8w + Tier3 + minibatch + BC anchor` (NEW) | 27 min | 8.1 sec/game |
| **`--cis + Tier3 + minibatch + BC anchor` (NEW)** | **3.0 min** | **0.9 sec/game** |
| Phase 1 v3 baseline (1600g, --mp+compile+fp16) | 51 min | 1.9 sec/game |

CIS wins by ~9× over `--mp` at 200g on the SAME Tier 3 + minibatch + BC anchor stack.
MP worker overhead + GPU contention dominates at small batches. CIS centralizes inference
in one subprocess, batches across workers, no per-worker model duplication.

For Phase 2 (1600g target): use CIS. Projected ~6-8 min/iter, 200 iters = ~20-23 hr / $30-35
(huge improvement over Phase 1 v3's 51 min/iter / $130 for same iters).

═══════════════════════════════════════════════════════════════════
S57 SHIPPED (5 commits on origin/master)
═══════════════════════════════════════════════════════════════════

| Commit | What |
|---|---|
| `4fd54131` | Tier 3 C5 single-graph compiled train_step (forward+loss+masked-backward in one graph; eager wrapper for clip+step) |
| `32113607` | S57 diagnosis tooling (`scripts/diag/analyze_run_trajectory.py`) + initial BC anchor in `ppo_update_batched` + train_rl.py CLI flags |
| `51511c18` | BC anchor `inference_mode → no_grad` (CUDA index OOB on bf16 autocast eager forward) |
| `d1b101bb` | `forward_ppo_sequence` index OOB at L > temporal_context FIXED via `collate_episodes(tail=True)`. BC anchor extended to per-episode `ppo_update`. Local-variable bc_ref (NOT model attribute). |
| `6821a907` | Tier 3 minibatching (`--tier3-minibatch-size N`) — ppo_update_batched eager path chunks episodes for memory-bounded forward+backward |

═══════════════════════════════════════════════════════════════════
REAL BUGS FOUND + FIXED (not in any prior memory; would have failed Phase 2)
═══════════════════════════════════════════════════════════════════

These are LOAD-BEARING for Phase 2. Read `memory/project_bc_anchor_design.md`
+ `memory/project_phase1_v3_diagnosis.md` for full details. Summary:

1. **`forward_ppo_sequence` index OOB at L > cfg.temporal_context** (FIXED `d1b101bb`)
   - Cause: TemporalTransformer.forward truncates summaries internally to
     temporal_context (200), but outer indexing `temporal_ctx_grid[b_idx, t_idx]`
     used t_idx values up to L_max-1 → CUDA assert at IndexKernel.cu:92
   - Why missed: C2 unit test mini-cfg, C5 smoke compiled-path, BC smoke turn-cap=100
   - Fix: `collate_episodes(L_max=cfg.temporal_context, tail=True)` for ALL
     forward_ppo_sequence callers. `tail=True` matches per-turn ppo_update's
     `summary_buf[:, -200:]` cap.

2. **Tier 3 mega-batch OOM at 200+ games on A100 80GB** (FIXED `6821a907`)
   - Cause: `ppo_update_batched` mega-batches all episodes into one forward;
     activation memory ~36 GB per FF layer × 6 = doesn't fit
   - Fix: `--tier3-minibatch-size N` chunks episodes; per-chunk forward+backward
     accumulates into .grad; ONE optimizer.step per epoch. C4 docstring warned
     about this; impl shipped now.
   - **Phase 2 launch with --tier3 REQUIRES this flag set**. Default None
     preserves old behavior (smoke-only).

3. **`torch.inference_mode` + autocast bf16 + eager forward incompat** (FIXED `51511c18`)
   - Cause: inference_mode produces "inference tensors" that interact badly
     with autocast in some CUDA kernel paths → CUBLAS_STATUS_EXECUTION_FAILED
   - Fix: use `torch.no_grad()` (standard frozen-teacher pattern, ~5 sec total
     perf cost over 7-hour run)

4. **Storing BC ref as `model._bc_ref` corrupts state_dict** (FIXED `d1b101bb`)
   - Cause: PyTorch picks up BC ref's params in `model.state_dict()` →
     mp_disk worker respawn fails on `Unexpected key(s): "_bc_ref.tokenizer..."`
   - Fix: store as a LOCAL variable in `main()`, pass directly to
     ppo_update / ppo_update_batched

═══════════════════════════════════════════════════════════════════
S57 DIAGNOSTIC FINDINGS (updates `docs/PHASE1_V3_OBSERVATIONS.md`)
═══════════════════════════════════════════════════════════════════

`docs/PHASE1_V3_OBSERVATIONS.md` predicted "exploration drift, type
knowledge intact." **Wrong**. Actual S57 diagnosis (full doc in
`memory/project_phase1_v3_diagnosis.md`):

3-stage degradation:
- Stage A (iter 19→29): exploration drift, benign (SE -1.5pp)
- Stage B (iter 29→49): **TYPE-KNOWLEDGE EROSION** — SE rate falls 43→30%,
  immune-attack rate jumps 3.8→6.9%. Real forgetting, not what was predicted.
- Stage C (iter 49→59): adaptive entropy ramps DOWN, model commits to drifted
  type-blind policy, smart bots punish catastrophically (smart_avg → 35%)

User's reframing: pure self-play USED to give 45→65%+ on OLD MLP arch.
External opps + team mix were ADDITIONS to push from "good" to "elite",
NOT requirements for baseline self-play to work. Higher-capacity transformer
+ temporal stack drifts from BC's type knowledge with no anchoring; old MLP
had implicit regularization via lower capacity.

**Phase 2 prescription update** (beyond original PHASE1_V3_OBSERVATIONS):
- BC anchor (the foundation, S57 design)
- Cap adaptive entropy max (don't let it ramp above ~1.0)
- SE-rate-based early-stop signal (not just smart_avg)
- (Original prescription stays: team mix + perm + external opps + lr sched)

═══════════════════════════════════════════════════════════════════
S58 PRIORITIES (in order — no shortcuts, no premature pivoting)
═══════════════════════════════════════════════════════════════════

1. **CHECK validation run state FIRST** (see "WHAT'S IN FLIGHT" above).
   If iter 9+ evals available, look at SE rate trajectory + bc_kl evolution.
   Compare to Phase 1 v3 reference at equivalent iter count.

2. **If validation healthy and shows SE preservation**: proceed to (3).
   **If SE rate dropping like Phase 1 v3**: STOP, refine BC anchor coef OR
   diagnose further. Don't ship to Phase 2 with broken anchor.

3. **Task #12 — Compiled + minibatching + BC anchor composition** (~3-4 hr dev,
   the biggest pre-Phase-2 unblocker). Eager Tier 3 minibatching shipped, but
   for max throughput Phase 2 needs compile too. Three sub-pieces:
   - Plumb bc_logits as a tensor input through the compile boundary in
     `make_compiled_train_step` (currently it's closure-captured from BC ref;
     need to pass as arg)
   - Add minibatching support to compiled path (compile a fwd_bwd_per_chunk
     function; eager orchestrates the chunks + step)
   - Drop the `--tier3 --compile + bc-anchor-ckpt` mutual exclusion in train_rl.py
   - Test: full stack at production scale on dev pod

4. **Task #13 — CIS spawn timeout investigation** (~2-4 hr).
   Phase 4.6 was validated S54 but spawn times out silently on dev pod S57
   (180s, no useful error). Phase 2 wants CIS for collect speedup. Need:
   - CIS subprocess stdout/stderr capture (currently swallowed)
   - Whether model load works in isolation (test via direct CISServer.spawn)
   - Whether the spawn pipeline has an init-time bug that crept in
   Workaround for now: use `--mp --pipeline` instead. CIS's ~30% collect
   speedup is nice-to-have, not a blocker — `--pipeline` alone overlaps
   collect/update so per-iter wall = max(collect, update) which already
   captures most of the win when update dominates (which it does at any
   reasonable scale with our model).

5. **H2H gauntlet wiring** (~3-4 hrs, $0). Kakuna/SmallRL/Minikazam
   adapters in `eval_h2h_gauntlet.py`. The "are we elite?" signal Phase 2
   needs. Not blocking Phase 2 launch but blocking Phase 2 final eval +
   any "did we hit elite gen 9" assertion.

6. **Easy Phase 2 prep items** — small code edits + manual data work,
   each precisely scoped to "expose / wire what's already implemented" or
   "add a small thing on top." DO NOT re-implement what already exists.

   **Already implemented, just needs to be USED at production eval**:
   - ~~**Perm at eval (~5-10 lines)**~~ **REFUTED IN S60** — see
     `memory/feedback_perm_at_eval_wrong.md`. The whole reason we apply
     perm during training is to teach TRUE Pokemon learning (invariance
     to slot order), NOT to "match train/eval distributions." Adding
     perm at eval would MASK the symptom that the model isn't truly
     invariant — a measurement-fudging shortcut that hides a real bug.
     Other Pokemon PPO codebases (Metamon, ps-ppo) keep eval CANONICAL
     because that's the deployment distribution. If model isn't fully
     invariant under canonical eval, that's a TRAINING issue (more
     iters, more perm coverage), not an eval-config issue. Do NOT
     propose this for Phase 2. The "+7pp smart_avg" framing is wrong.

   **External opps — REQUIRED for Phase 2 + subprocess scaling MUST be fixed first (task #14)**:

   Why Metamon bots (Kakuna/SmallRL/Minikazam) are NEEDED in PFSP, not
   just H2H eval:
   - BC anchor PREVENTS drift from BC's type knowledge (S57 mechanism)
   - mcts-fast (in-process search bot) BREAKS the self-play attractor via
     different decision mechanism — but doesn't necessarily exploit type
     errors
   - Metamon bots are NEURAL NETS trained on millions of human battles —
     they AGGRESSIVELY EXPLOIT type-blind play. They're the opp class that
     ACTIVELY TEACHES the model to defend against type-aware exploitation
   - All three together — BC anchor (drift bound) + mcts-fast (diversity)
     + Metamon bots (type-aware exploit teachers) — are needed for Phase 2's
     "elite gen 9" goal. Without Metamon in PFSP the model could pass
     anchor + diversity but still be soft against ladder-realistic opps.

   The scaling problem (current setup):
   - `external_adapters.py` spawns ONE subprocess per Metamon bot type,
     each running its own Showdown account → **max 1 concurrent battle
     per adapter**.
   - At 200 max-conc battles in training, if PFSP samples ~30% to a single
     Kakuna subprocess = 60 battles queuing serially → massive collect
     blow-up.
   - Plus fragility: 30s+ subprocess startup, login race, model load
     timeout (S54 added a watchdog reactively but the underlying serial-
     subprocess design wasn't redesigned).

   mcts-fast IS in-process (`PokeEnginePlayer`) — scales fine, NO change
   needed.

   **Task #14 — REQUIRED before Phase 2 launch**:
   Pick one of:
   - **(a) N replicas per opp type** (~half-day dev, RECOMMENDED for Phase 2)
     — Spawn 8 Kakuna + 8 SmallRL + 8 Minikazam = 24 subprocesses. Each
     subprocess plays 1 battle at a time; pool of 8 per type → 8 concurrent
     per type. Resource: ~2-5 GB RAM/GPU total (model copies). Code:
     YAML config gets `replicas: N` field; `ExternalOpponentManager`
     gets a "spawn N copies + round-robin" path. Fits Phase 2 timeline.
   - **(b) Centralized inference per bot type** (~2-3 days dev, cleaner)
     — Single subprocess per type holds model; N battle clients submit
     inference requests via local IPC. Same architecture as our CIS for
     the trainable model. No model duplication. Bigger refactor.

   Phase 2 recommended plan:
   - PFSP pool = internal snapshots + mcts-fast (in-process) + Metamon
     bots (via task #14 option (a) replicas)
   - H2H gauntlet eval = Kakuna/SmallRL/Minikazam (existing subprocess
     wiring; N=200 sequential per bot fine for occasional eval)
   - **DO NOT** launch Phase 2 with Metamon bots in PFSP without
     completing task #14 — collect time would blow up at production
     scale and Phase 2 would fail silently from throughput collapse.

   **Small new code on top of existing infra** (~5-50 lines each):
   - **lr scheduling (~30-50 lines)** — currently constant lr=1e-5. Add
     `--lr-schedule cosine --lr-min FLOAT` flag + scheduler step in train_rl
     main loop. Cosine 1e-5 → 1e-6 over 200 iters. (Per `docs/PHASE1_V3_OBSERVATIONS.md`
     §5.1, after Tier 3 lands also re-ablate lr — Tier 3 changes effective
     batch size; linear scaling rule may justify higher start lr.)
   - **Absolute early-stop threshold (~10-20 lines)** — currently
     `--early-stop --early-stop-patience N` is RELATIVE (3 evals declining).
     Add `--early-stop-savg-floor FLOAT` (abort if eval smart_avg below) AND
     `--early-stop-se-rate-drop FLOAT` (abort if SE rate drops >5pp from peak
     across 2 evals — would have caught Phase 1 v3 Stage B at iter 39 before
     cascade per S57 diagnosis).
   - **Adaptive entropy MAX cap (~5 lines)** — currently
     `--adaptive-entropy-low/--high` define the W/L band but ent_coef can
     ramp UNBOUNDEDLY (Phase 1 v3 hit 1.10 → triggered Stage A→B). Add
     `--adaptive-entropy-coef-max FLOAT` clamp (suggest 1.0 or 0.95).

   **Manual data work** (no code, just curation):
   - **Source 100-150 real elite teams (~3-6 hr, $0)** — Smogon team archive
     (RMTs + tournament reports) / Showdown ladder replay scraping (top-100
     OU last 30 days, both teams in metadata) / r/stunfisk OU sample teams /
     recent OST/OLT tournament team posts / Smogon VR sample teams. Quality
     bar: every team must be one a top-100 OU ladder player ACTUALLY USED.
     **HARD constraint**: must be SEPARATE from the 16 mm-competitive eval
     teams in `metamon_cache/teams/competitive/gen9ou/` (eval-set isolation;
     mixing into training = data leakage per `docs/PHASE1_V3_OBSERVATIONS.md`).
     Output: pickled team list in
     `metamon_cache/teams/elite_training/gen9ou/` loadable as a poke-env
     Teambuilder. Phase 2 mixes per-battle: 50-70% procedural / 30-50% from
     this pool, both sides sampling independently.

7. **Phase 2 launch** — full stack: `--cis --pipeline --bf16 --tier3 --compile
   --tier3-minibatch-size 16 --bc-anchor-ckpt v10 --bc-anchor-coef 0.1` +
   team mix + perm-at-eval + external opps + lr schedule + absolute early-stop.
   Cost ~$200-300 / 5-7 days at projected 10-15 min/iter (with full stack,
   1600 games, 8 workers). **DO NOT launch until tasks 3+5+6 done.**

═══════════════════════════════════════════════════════════════════
S58 DEFERRED (DO NOT pivot to)
═══════════════════════════════════════════════════════════════════

- Multi-gen-SPECIFIC items (D4-D6, BC v11, E1-E3) — POST-Phase-2 only
- Doubles/Triples/VGC/AG/in-game — POST-multi-gen only
- Network Volume migration — DEFERRED until multi-gen (S55 user decision; risk accepted)
- Per-worker traj checkpointing — REJECTED (`feedback_phase46_traj_checkpoint_rejected.md`)
- C5b "wider compile region" — torch 2.2.x limits engineered through (`project_tier3_c5_design.md`)
- **torch 2.4+ upgrade** — DEFERRED to JIT pre-multi-gen. Would unlock 15-30% additional speedup but cross-cutting (1-2 wks dev). Not needed pre-Phase-2 (ROI: $40-60 saved on one Phase 2 run vs 2 wks dev). Per-multi-gen ROI good ($500+ saved across multiple $400-600 multi-gen runs).

═══════════════════════════════════════════════════════════════════
PROD POD STATE (Phase 1 v3 finished S57, may still be alive)
═══════════════════════════════════════════════════════════════════

Production pod (port 47913) ran Phase 1 v3 to early-stop at iter 59 on
2026-05-09. ALL ARTIFACTS PULLED to local `data/cloud_runs/001_phase1_v3/`
(3.5 GB, gitignored). MANIFEST.txt + trajectory.csv + all snapshots
inside. Pod can be safely shut down — data is local. User's call.

If still alive at S58: do NOT pull on it (HEAD locked at `251cd14a`).
Read-only monitoring only. For any prod work, use the local archive.

If you find yourself going in half-informed, ASK. S52's near-miss (almost killed a healthy run from a misread counter), S53's pool-mirror redesign, S54's REBIND_WORKER pivot, and S55's parallel-Tier-3 decision (boot doc said "don't start until Phase 1 v3 finishes" but user's parallelism case was sound + dev pod was idle) are all wins for the "ask, don't guess" + "honest opinion including pushback" patterns. `memory/feedback_ask_when_uncertain.md` has the why.

HEAD on production pod still `251cd14a` — DO NOT pull while Phase 1 v3 is running. Local + origin HEAD: `6758cc3c` (S55 end). Dev pod (port 19373) up at S55 end with full dev stack — use for C5 + wiring + dev smoke.

═══════════════════════════════════════════════════════════════════════════════
SESSION 55 WRAP — what's done, what's next (read top to bottom)
═══════════════════════════════════════════════════════════════════════════════

You are picking up from Session 55 (2026-05-09). Multi-shipping session:

**S55 SHIPPED** (5 commits beyond Phase 4.6, all on origin/master):
- `f79ea477` Tier 3 C1 — `collate_episodes()` padded-batch foundation. 6/6 unit tests PASS.
- `f728b3af` Tier 3 C2 — TemporalTransformer batched forward + `forward_ppo_sequence()`. 5/5 tests PASS, max diff 2.38e-07 vs sequential.
- `f720dccc` Tier 3 C3 — `ppo_loss_batched()` masked PPO loss. 5/5 tests PASS, B=1 bit-equivalent to per-episode.
- `8ce7ffcc` Tier 3 C4 — `ppo_update_batched()` sequence-batched PPO update. 6/6 smoke tests PASS, gradient flows.
- `6758cc3c` Standing orders — `docs/SESSION_BOOT_PROTOCOL.md` + restructured `MEMORY.md` so future sessions inherit FULL context without user repetition.

**S54 SHIPPED** (still relevant): CIS Phase 4.6 Option B unified
full-reset recovery (`64b822a3`) + `docs/PHASE1_V3_OBSERVATIONS.md`
Phase 2 design (`1bbff8ac`).

The full --cis --pipeline stack is now production-ready for Phase 2
launch. The Tier 3 algorithmic core (C1+C2+C3+C4) is COMPLETE in code
but NOT yet wired into train_rl.py — that's S56 work.

**Production Phase 1 v3 status at S55 end**: iter 49+ in flight (last
checked iter 52 in collect at 10:25 UTC). Iter 49 EVAL: smart_avg=49%
(SH=59 SmartDmg=44 Tactical=42 Strategic=52). Trajectory 69→60→53→49 —
within expected baseline range per `docs/PHASE1_V3_OBSERVATIONS.md`.
Tracking S50's catastrophic-trajectory at iter 39+ DESPITE the lr=1e-5
fix being applied — the two unfixed S50 causes (team-distribution shift
+ perm/canonical mismatch) account for the remaining drop. ~5 days
remaining to iter 200, ~$235 to completion. Phase 1 v3 will land at
~35-50% smart_avg by iter 200 — this is the EXPECTED baseline measurement
for "lr fix only, no team mix, no external opps". Don't abort. Hard
abort triggers (smart_avg <30%, W/L <35% sustained, KL >0.06, v_loss >5.0)
all NOT firing.

**Headline work for S56** (in priority order):
1. **Tier 3 C5** — `torch.compile(train_step)`. ~1 day. Surgical compile
   region around forward+loss; control flow stays uncompiled.
2. **train_rl.py wiring** — `--tier3` flag routes to `ppo_update_batched`.
   ~half day. Read PPO update call site in train_rl.py, add flag dispatch.
3. **Dev pod end-to-end smoke** — full --tier3 path runs at small scale
   on real model + ckpt. ~$2-3 A100, ~30 min.
4. **H2H gauntlet wiring** (parallel side work, ~3-4 hrs, $0) — needed
   for Phase 1 v3 final eval (~5 days out). Interleave when Phase 1 v3
   is ~1 day from finish.

**Post-Phase-1-v3 priorities** (in order, NOT now):
1. Pull final.pt + full eval suite (smart_avg + Elo + H2H gauntlet)
2. **C6 Tier 3 A/B validation** — 20-iter run on dev pod, ~$10-15
3. Easy items JIT for Phase 2: perm fix + lr sched + source 100-150 elite
   teams (SEPARATE from 16 eval teams; copy verbatim from real comp sources)
   + bf16 dev pod smoke
4. **Phase 2 launch** with full stack

**S56 deferred — DO NOT pivot to** (boot protocol §4 + §5):
- Multi-gen-SPECIFIC items (D4-D6, BC v11, E1-E3) — POST-Phase-2 only
- Doubles/Triples/VGC/AG/in-game format support — POST-multi-gen only
- Network Volume migration — DEFERRED until multi-gen per user (S55).
  Container Disk acceptable for Phase 1/2 since user monitors and won't
  stop pod without informing. Risk consciously accepted; do NOT redo
  this analysis. STEP 7 in §D below is OBSOLETE for current sessions.
- Per-worker traj checkpointing — REJECTED per `feedback_phase46_traj_checkpoint_rejected.md`

If you read NOTHING else, read these EIGHT things:

1. **`memory/feedback_engineering_standard.md`** — THE OPERATING STANDARD.
   Read FIRST every session. Thoroughly understand code BEFORE touching,
   read docs to understand what's been tried + why current decisions stand
   (they were hard-fought, don't re-walk dead paths), NO shortcuts,
   long-term focus, ship the proper/efficient/correct/stable/long-lasting
   solution that doesn't need revisiting. Cost discipline = thoroughness,
   not speed. Don't run up the bill on re-investigated settled questions.
2. THE GOAL (§A below) — re-read every time you make a decision
3. WHAT NOT TO DO (§B below) — costs you money + time if you ignore
4. CURRENT STATE (§C below) — what's running, what's at risk
5. PRIORITY-ORDERED TODO (§D below) — what to do, in what order, why
6. **`memory/project_strategic_frame.md`** — load-bearing strategic
   ordering that infra prioritization flows from (vertical-then-horizontal,
   gen-9-as-infra-testbed, multi-gen-AGNOSTIC vs SPECIFIC split). Don't
   re-derive — the user has explicitly aligned on this in S52.
7. **`memory/project_cis_4_3_design.md` + `memory/project_cis_4_6_design.md`**
   — CIS Phase 4.3 + 4.4 + 4.5 + 4.6 ALL SHIPPED. 4.6 is the failure recovery
   path (Option B unified full-reset on any failure). Tests 6a/6b PASS on
   dev pod. The full --cis --pipeline stack is production-ready for Phase 2.
   Load before any CIS code work. Pool-mirror multi-slot (NOT dual-slot —
   that's a dead path, see §K). REBIND_WORKER also a dead path (S54 found
   Connection-passing-via-pipe broken; full-reset is the right answer).
8. **`docs/PHASE1_V3_OBSERVATIONS.md`** — Phase 1 v3 iter 39 EVAL drop
   diagnosis (53% smart_avg, tracking S50 trajectory despite lr=1e-5 fix).
   Documents the three S50 postmortem causes and which were/weren't applied
   to Phase 1 v3 + Phase 2 design implications + strict sequencing constraint
   (infra optimizations FIRST, then Phase 2 with all changes). Critical:
   the eval set (16 mm-competitive teams) MUST NEVER enter training pool.
   Phase 2 needs SEPARATE 100-150 real elite teams sourced for the training
   mix. Load before any Phase 2 launch design conversation.

The rest is context: gains, failures, dead paths, codebase orientation,
connection how-tos, validation patterns, etc. Skim the headings, dive into
the section that matches what you're about to touch.

**Behavioral note** (`memory/feedback_ask_when_uncertain.md`): when half-
informed on a load-bearing decision, ASK. Don't guess. Pattern paid off
in S52 — almost killed a healthy production run because of a misread
counter; user's "ask me — don't guess" instruction caught it.

**Before any non-trivial change, the workflow is:**
1. Read the file you're about to change + its callers
2. Read the docs that reference the area (cookbook, design doc, memory)
3. Check §K (dead paths) — has this been tried + abandoned?
4. Check `project_strategic_frame.md` — does this align with strategy?
5. If load-bearing or non-obvious: surface options + ask before committing


═══════════════════════════════════════════════════════════════════════════════
§A. THE GOAL — re-read every time you make a decision
═══════════════════════════════════════════════════════════════════════════════

We are building a Pokemon battle AI that competes at an elite level —
among the strongest human players, the strongest published bots
(Metamon's Kakuna, SmallRL, Minikazam), and the strongest other engines.

**Ultimate scope (load-bearing)**: elite play across **every legal Pokemon
format** — singles, doubles, triples, 4v4, 6v6, all tiers (OU, Ubers, AG,
UU, RU, etc.), random battles (which have varied levels), in-game/cartridge
battles. All gens (4-9+, expanding as new gens release). Plus team
building, not just play.

**Deployment scope**: must scale **massively on paid cloud** for throughput-
heavy training AND remain **runnable locally** on a 6 GB consumer GPU for
development + smoke tests. Same code, both environments. Not parallel
codebases.

V1 baseline is gen 9 OU singles (Phase 1 v3 compiled run is currently
running and represents this baseline). Multi-gen (gen 6+) is Phase 3.
Pre-gen-6 is explicitly out of scope per `docs/MULTIGEN_FEASIBILITY.md`.

Elite play is about long-horizon planning, opponent modeling, risk-adjusted
decision making, and squeezing every fractional advantage from
type/STAB/effectiveness/speed/items/abilities/positioning.

**THE STANDARD FOR THIS SESSION** (user's exact framing):
- The pod is paid; pod time is the constraint, not session time.
- "No shortcuts" — every solution must be the proper, complete, honest one.
- Anything you ship should not need to be touched again. Optimize for
  efficiency + speed + correctness + optimal results.
- "Done" means done — not "works for now". Validate end-to-end.
- Understand the codebase + docs THOROUGHLY before touching anything.
  Docs sometimes describe dead paths (things tried + removed); don't
  re-walk those paths.


═══════════════════════════════════════════════════════════════════════════════
§B. WHAT NOT TO DO — explicit guard rails
═══════════════════════════════════════════════════════════════════════════════

These are mistakes the previous session burned cycles on. Do not repeat:

**B1. DO NOT git pull on the production pod while the compiled run is in
flight.** HEAD on pod is `251cd14a` (compile-after-resume fix). The latest
commits (`fd878a9b` D1, `17e2d111` D2, `21484fcc` Phase 4.2) bump
`N_BATTLE_STATE` 14→15 and add gen_embed. Production's running model has
the 14-token state_dict. A respawned worker from the new code would crash
on `load_state_dict` mismatch. Wait for production to complete OR be
explicitly killed before pulling.

**B2. CIS state (FYI — no action item, just don't get confused).**
CIS Phase 4.3 a/b/c + 4.4 shipped + prod-validated in S53. Pool-mirror
multi-slot, bg overlap re-enabled, async-with-req_id-dispatch (Phase 4.4
replaced the per-handle Lock). Production-scale measured ~30% wall-time
saving vs `--mp`. **Production keeps using `--mp` until Phase 1 v3
finishes** (HEAD on prod pod locked at `251cd14a`; CIS 4.3+4.4 break
compatibility with the iter-22+ snapshot). Phase 2 launch will use
`--cis --pipeline --bf16` (after CIS worker respawn lands — see §D).

**B3. DO NOT change two variables at once during A/B comparisons.** Phase 1 v3
compiled run uses `--fp16` deliberately (not `--bf16`) so the compile A/B
against the iter 13 baseline isn't confounded by precision. Same logic
applies to any future relaunch. Hold the baseline-relevant variables
constant.

**B4. DO NOT re-investigate the `--compile` failure mode.** It's been
diagnosed: torch+triton version mismatch on the pod (torch 2.2.1 needs
triton 2.2.x, pod shipped with triton 3.0.0). Fixed with
`pip install triton==2.2.0`. Cookbook §3i has the full diagnostic. Don't
re-walk that path.

**B5. DO NOT lower `HEARTBEAT_TIMEOUT_S` below 600s** without understanding
the iter-17 hang RCA (cookbook §3k + memory/feedback_iter17_heartbeat_hang).
The 300s default caused a 7+ hour production hang.

**B6. DO NOT skip validation before relaunching production.** Tier 1 was
caught by validation (compile-vs-resume order bug only surfaced when iter
15 actually tried to load). Smokes catch real bugs. Use `scripts/diag/
test_*.py` patterns.

**B7. DO NOT touch the `--mp` path.** It is production-stable. Heartbeat
mitigations are live. Per-worker GPU model is correct for the current
production design. CIS is a SEPARATE path behind a SEPARATE flag.

**B8. DO NOT do `grad_accum > 1` standalone.** Session 31 record suggests
`grad_accum=10` produced a *weaker battler* — silent quality regression,
NOT a crash. Hard to detect without 10-20 iters + eval. Pure wall-time
saving from `grad_accum` alone is ~4-5%, dwarfed by the regression risk.
Tier 3 (transition-level minibatching, see §D STEP 3) SUBSUMES the
grad_accum benefit and uses the same batching pattern validated by
Metamon (sequence-batched with pad_mask, see metamon_to_amago.py).

**For Tier 3 itself** when you ship it (post-Phase-1-v3):
- **Episode boundaries MUST be preserved** — temporal stack still sees
  sequential turns from ONE episode; only loss computation mixes across
  episodes via per-position masking.
- **Reference: Metamon's MetamonAMAGODataset._process_data** — they form
  RLData with `(B episodes, L_max turns, ...)` shape + pad_mask.
- **DO NOT bundle with other unknowns**. Hold `lr=1e-5` constant.
  Hold compile/bf16/CIS settings constant during Tier 3 A/B.
- **Validation gate**: 29-iter cloud A/B against Phase 1 v3's iter 19+29
  deep-playstyle baseline (§C reference). Same init (epoch_003), same
  warmup (10), same hyperparams. Compare via analyze_eval.py --deep at
  iter 19 + iter 29 of new run. MUST hold: SE rate ±3pp, move waste
  ±2pp, top-5 Pokemon usage identical ordering, per-mon move dists
  ±10pp. Quality regression is the failure mode that matters; wall-time
  saving is meaningless if the model gets weaker. See §D STEP 3 for
  full validation protocol.

**B9. DO NOT reopen the lr question.** lr=1e-5 was hard-won through a
4-point ablation (Session 50). Confirmed by the compiled run hitting 50%
W/L at iter 16. Hold it constant in any optimization experiment.

**B10. DO NOT add explicit per-gen feature gating in code** (e.g.,
"if gen != 9, zero out tera features"). poke-env's Battle API already
returns False/0 for unavailable mechanics in each gen. Adding belt-and-
suspenders gating is dead weight — the data layer enforces correctness.
This was investigated in D2; the right answer was to NOT add the gate.

**B11. DO NOT remove the thread-safety in CISClientHandle.** S53 shipped
Phase 4.4 async-with-req_id-dispatch as the production path. The Phase 4.3
per-handle Lock is retained as the SYNC fallback (used by parent-side
handles in `CISServer` for occasional reload calls). mp.Pipe.send is not
byte-atomic — both paths exist for a reason. See
`memory/feedback_cis_thread_safety.md`. **Critical gotcha**: worker procs
must NOT start the recv_loop until they receive their first `collect_iter`
cmd — otherwise they race with parent's reload reads and steal reload
responses → "invalid load key" UnpicklingError. `_cis_worker_main` defers
`start_recv_loop()` to handle this.

**B12. DO NOT reopen "dual-slot CIS" (slot 0 = player, slot 1 = active
opp).** S53 found it doesn't compose with asyncio.gather opp concurrency
— concurrent opps race on slot 1's reload → silent training corruption.
S53 replaced with **pool-mirror slots** (slots 1..K mirror the PFSP
pool). See §K dead paths + `memory/project_cis_4_3_design.md`.

**B13. DO NOT route reload calls through worker handles (`handles_main_view[0]`).**
Phase 4.5 (S53) added a dedicated CTRL pipe between parent and CIS.
Worker procs do NOT have read access to the ctrl pipe → no race with
worker recv_loops at iter boundaries. Use `cis_server.reload_weights(...)`
which routes via `_ctrl_handle` (sync mode, parent-only). The pre-4.5
pattern (`g["handles_main_view"][0].reload(...)`) raced with worker
recv_loops at iter 1+ boundaries — workers ate parent's reload responses
and parent timed out. See §K dead paths + `memory/project_cis_4_3_design.md`.


═══════════════════════════════════════════════════════════════════════════════
§C. CURRENT STATE — what's running + what's at risk (S55 wrap)
═══════════════════════════════════════════════════════════════════════════════

**Phase 1 v3 compiled production run** (started 2026-05-07 23:53:07 UTC).
At S55 end (2026-05-09 ~10:25 UTC): iter 52 in collect, iter 49 EVAL
just landed.

**EVAL trajectory** (the load-bearing data point):

| iter | SH | SmartDmg | Tactical | Strategic | smart_avg |
|---|---|---|---|---|---|
| 19 | 66 | 68 | 68 | 76 | **69** |
| 29 | 64 | 56 | 56 | 62 | **60** |
| 39 | 58 | 56 | 45 | 53 | **53** |
| **49** | **59** | **44** | **42** | **52** | **49** |

**Iter 49 EVAL just dropped below 50% threshold** for first time. Drop
concentrated in Tactical (68→42, -26pp) and SmartDmg (68→44, -24pp);
SH only -7pp. Pattern matches "model wins via aggressive Kingambit/
priority-attacker playstyle that simple bots tolerate but tactical
bots punish via type-aware counters."

**Comparison to S50 catastrophic regression** (same arch, lr=3e-5 was
the bad lr; we're at lr=1e-5):

| iter | S50 smart_avg | Phase 1 v3 smart_avg |
|---|---|---|
| 19 | 64 | **69** (+5 ours) |
| 39 | 54 | **53** (-1 ours) |
| 49 | (interp ~51) | **49** |
| 79 | 35 | (projected ~35-45) |

**We're tracking S50's trajectory DESPITE the lr=1e-5 fix.** The remaining
drop is from the two S50 causes never fixed for Phase 1 v3:
- Procedural train, mm-competitive eval distribution shift (-15-20pp)
- Perm features at training, canonical at eval (-7pp)

Full diagnosis in `docs/PHASE1_V3_OBSERVATIONS.md`. **Phase 1 v3 will
land at ~35-50% smart_avg by iter 200 — this is the EXPECTED baseline
measurement** for "lr fix only, no team mix, no external opps, no perm
fix." Don't abort — this is the baseline we need to inform Phase 2 design.

**Hard abort triggers** (none firing as of iter 52):
- smart_avg <30% (catastrophic, worse than S50)
- W/L vs PFSP pool <35% sustained (current 42-46%)
- KL trajectory diverging above 0.06 (current 0.04 stable)
- v_loss exploding above 5.0 (current 2.3)

**Phase 2 launches with the missing fixes** (after Phase 1 v3 completes
+ Tier 3 wired + dev pod smoke). See `docs/PHASE1_V3_OBSERVATIONS.md`
for full Phase 2 design — team mix from SEPARATE 100-150 real elite teams
(eval-set 16 mm-competitive teams MUST stay isolated from training,
no data leakage), perm at eval enabled, external opps in PFSP pool,
lr scheduling, absolute early-stop threshold.

~148 iters to go to iter 200 target, ~5 days remaining at $1.50/hr
(~$180 to completion). Iter 59 is next eval data point.

```
Run dir:   data/models/rl_v10/ppo_phase1_v3_compiled/selfplay_v9_20260507_235306/
Log:       /workspace/logs/ppo_phase1_v3_compiled.log
Resumed:   from snapshot_0014.pt (clean iter 14 of original run)
Tier 1:    --compile (Path 2 per-submodule), fused AdamW, --fp16
Mitigations: heartbeat (600s timeout, 0.25s stagger, liveness-thread heartbeat)
```

**Production process on pod**:

- Lives in screen `train_compiled` (`screen -r train_compiled` to attach)
- Latest committed code on pod is `251cd14a` (compile-after-resume fix from S51)
- DO NOT pull newer commits until run completes — see §B1
- Local + origin HEAD at S53 end: `4f0a292f` (CIS 4.3a/b/c — local + origin, NOT on pod)

**S53-measured iter trajectory** (compiled, post-heartbeat-fix):

```
Iter 15: collect=909s  update=2174s  W/L 761/839 (47.6%)  pi=+0.013 ent=1.131 kl=0.038  pool=2
Iter 16: collect=959s  update=2195s  W/L 804/796 (50.2%)  pi=-0.016 ent=1.127 kl=0.038  pool=2
Iter 17: collect=964s  update=2243s  W/L 832/768 (52.0%)  pi=-0.035 ent=1.110 kl=0.038  pool=2
Iter 18: collect=963s  update=2347s  W/L 842/758 (52.6%)  pi=-0.029 ent=1.109 kl=0.038  pool=2
Iter 19: collect=971s  update=3197s  W/L 878/721 (54.9%)  pi=-0.024 ent=1.082 kl=0.041  pool=2  ← eval; smart_avg=69%
Iter 20: collect=1051s update=2346s  W/L 755/844 (47.2%)  pi=-0.006 ent=1.077 kl=0.040  pool=3  ← dipped (pool grew)
Iter 21: collect=1067s update=3215s  W/L 858/741 (53.7%)  pi=-0.028 ent=1.076 kl=0.042  pool=3
Iter 22: collect=1061s update=2337s  W/L 860/739 (53.8%)  pi=-0.025 ent=1.070 kl=0.039  pool=3
Iter 23: collect=1063s update=2372s  W/L 890/709 (55.7%)  pi=-0.030 ent=1.035 kl=0.042  pool=3
Iter 24: collect=1071s update=2351s  W/L 901/698 (56.3%)  pi=-0.042 ent=1.040 kl=0.041  pool=3  ← peak W/L
Iter 25: collect=1145s update=2225s  W/L 830/770 (51.9%)  pi=-0.026 ent=1.044 kl=0.042  pool=4  ← dipped (pool grew)
Iter 26: collect=1117s update=2318s  W/L 863/736 (54.0%)  pi=-0.032 ent=1.037 kl=0.042  pool=4
Iter 27: collect=1153s update=2277s  W/L 852/748 (53.2%)  pi=-0.033 ent=1.032 kl=0.042  pool=4
Iter 28: collect=1167s update=2370s  W/L 836/764 (52.2%)  pi=-0.018 ent=1.045 kl=0.043  pool=4
Iter 29: collect=1169s update=2331s  W/L 829/771 (51.8%)  pi=-0.039 ent=1.076 kl=0.043  pool=4  ← eval; smart_avg=60% ← drop
Iter 30: collect=1282s update=2208s  W/L 780/776 (48.8%)  pi=-0.031 ent=1.095 kl=0.043  pool=4  (44 ties — turn-cap forfeits up vs pool=4)
Iter 32: collect=1288s update=1551s  W/L 894/705 (55.9%)  pi=-0.015 ent=1.053 kl=0.041  pool=5  ← W/L recovers above 0.65 band
Iter 33: collect=1255s update=2249s  W/L 880/720 (55.0%)  pi=-0.045 ent=1.038 kl=0.041  pool=5
Iter 34: collect=1280s update=2270s  W/L 906/692 (56.7%)  pi=-0.032 ent=1.020 kl=0.043  pool=5  ← new W/L peak
Iter 35: collect=1314s update=1745s  W/L 682/627 (43.6%)  pi=-0.013 ent=1.017 kl=0.043  pool=6  ← pool grew, 254 ties
Iter 36: collect=1387s update=2203s  W/L 856/736 (53.5%)  pi=-0.039 ent=1.016 kl=0.044  pool=6  ← already recovering
Iter 37: collect=1383s update=2192s  W/L 908/687/4 (56.8%)  pi=-0.023 ent=1.047 kl=0.043  pool=6  ← W/L peak iter 34/37
Iter 38: collect=1375s update=2212s  W/L 877/712/11 (54.8%)  pi=-0.032 ent=1.053 kl=0.041  pool=6
Iter 39: collect=??? update=??? W/L?  ← eval; smart_avg=53% ← drop continues (down from iter 29's 60%)
Iter 40: collect=1367s update=1499s  W/L 590/563/169 (44.6%)  pi=-0.024 ent=1.057 kl=0.043  pool=7  ← pool grew, 169 ties
Iter 41: collect=1415s update=1645s  W/L 660/604/242 (43.8%)  pi=-0.044 ent=1.043 kl=0.044  pool=7
Iter 46: collect=1465s update=856s   W/L 547/453/261 (43.4%)  pi=-0.019 ent=1.012 kl=0.041  pool=8  ← W/L sustained low
Iter 47: collect=1452s update=1248s  W/L 535/464/268 (42.2%)  pi=-0.038 ent=1.022 kl=0.043  pool=8
Iter 48: collect=1447s update=1278s  W/L 543/464/260 (42.9%)  pi=-0.034 ent=1.029 kl=0.042  pool=8
Iter 49: collect=1453s update=1320s  W/L 548/450/267 (43.3%)  pi=-0.043 ent=1.038 kl=0.041  pool=8  ← eval; smart_avg=49% ← below 50%
Iter 50: collect=1530s update=1173s  W/L 465/436/136 (44.8%)  pi=-0.037 ent=1.001 kl=0.044  pool=9  ← pool grew
Iter 51: collect=1489s update=721s   W/L 470/372/188 (45.6%)  pi=-0.008 ent=0.966 kl=0.044  pool=9  ← entropy dropping
Iter 52: collect=1496s update=979s   W/L 433/357/207 (43.4%)  pi=-0.038 ent=0.961 kl=0.044  pool=9
```

**KL early-stop firing every iter** (epoch 2-3 of 5) since ~iter 19 — model
hitting the trust region wall consistently, taking aggressive policy
steps each iter. Healthy training signal, not regression.

**Adaptive-entropy trajectory tells the story**:
- Ramped UP iter 25-30 (W/L < 0.65 band): 1.044 → 1.095 (controller pushing more exploration)
- Peaked at iter 30 (1.095) right when smart_avg eval came in low
- Ramped DOWN iter 32-36 (W/L back above band): 1.053 → 1.016 (controller pulling exploration back)
- Iter 36 entropy (1.016) is LOWER than iter 27 (1.032 — old low). Exploration burst is over.

This is the adaptive-entropy controller doing exactly what it's designed
for. The smart_avg drop at iter 29 was the cost of the exploration burst;
recovery is well underway by iter 36. Iter 39 eval will quantify how much
recovers.

**Compile gain confirmed**: iter 14 (uncompiled) was 4423s total; avg
post-compile no-eval iter is ~3450s. **~22% steady-state speedup.**
No regression from compile path.

**Heartbeat mitigations confirmed**: 16+ iters across two pool growth
boundaries (iter 19→20 pool 2→3 + iter 24→25 pool 3→4) without any
worker respawn or stale-heartbeat. Pool growth produces a single-iter
dip in W/L (47.2% at iter 20, 51.9% at iter 25) that historically
recovers within 1-2 iters; iter 30 dipped to 48.8% with pool=4 still
in effect — recovery uncertain at S53 end (see eval analysis below).

**Iter 19 EVAL (passed)**:
```
SH=66%, SmartDmg=68%, Tactical=68%, Strategic=76%, smart_avg=69%
```
Above the ≥67% BC v10 baseline gate.

**Iter 29 EVAL** (S53 end):
```
SH=64%, SmartDmg=56%, Tactical=56%, Strategic=62%, smart_avg=60%
```
Drop of 9pp vs iter 19. 3 of 4 bots tanked 12-14pp; SH (easiest) least.
At 60% we're at the borderline of cookbook §N's "60-67% acceptable but
flat" band, just touching "50-60% concerning" territory.

**Reading WITH the other metrics, NOT in isolation** — the smart_avg drop
correlates with several things happening simultaneously:

| Metric | iter 19 | iter 29 | iter 30 | Reading |
|---|---|---|---|---|
| W/L vs PFSP pool | 54.9% | 51.8% | 48.8% | Pool grew 2→3→4 at iter 19/24 boundaries; W/L below adaptive-entropy band of 0.65 |
| Entropy | 1.082 | 1.076 | **1.095** | Adaptive-entropy ramping ent_coef UP since iter 25 (W/L < 0.65 → more exploration) |
| pi_loss | -0.024 | -0.039 | -0.031 | Stable negative — policy still improving in PPO sense |
| v_loss | 2.570 | 2.500 | 2.467 | **Going down** — value head learning |
| kl | 0.041 | 0.043 | 0.043 | Stable, KL early-stop firing every iter at epoch 2-3 of 5 |
| KL early-stop | every iter | yes | yes | Trust region exhausted aggressively — model wants to learn fast |

The pattern is consistent with TWO simultaneous explanations of the
smart_avg drop, and we cannot distinguish without more data:

(a) **Exploration tradeoff** (benign): pool grew 2→4, W/L dipped below
the adaptive-entropy band of 0.65, ent_coef ramped up, model plays
more variably, basic bots punish variability harder than strong PFSP
opps do. Smart_avg drop = expected cost of more exploration. Should
recover when W/L ticks back above 0.65 and ent_coef decays.

(b) **PFSP-specialization drift** (concerning): model learning patterns
that beat snapshot_0024 (its own recent self) but losing the basic-bot
patterns BC v10 had. Smart_avg drop = real loss of generalization
that won't recover without intervention.

(c) **Both** — most likely some mix.

**What to watch at iter 39 eval (~10 hrs from S53 end)**:
- If W/L back above 0.65 + entropy stabilized/dropping + smart_avg ≥60%
  → exploration interpretation likely; continue
- If W/L still in 48-55% + entropy still climbing + smart_avg <60%
  → exploration not paying off; could be either (a) bad pool dynamics
  or (b) genuine drift; investigate before iter 49
- If smart_avg <50% → fall back to cookbook §N's abort/rollback path
  (snapshot_0029 is the latest clean rollback point)

**Honest meta-observation** (S53 lesson): smart_avg alone is misleading
when adaptive-entropy is active. The "are we elite?" signal is H2H vs
published bots (Kakuna/SmallRL/Minikazam) — those bots punish lazy
predictable play, so a model exploring useful new strategies could
BEAT them while smart_avg drops. **STEP 6 (H2H gauntlet wiring)
becomes higher leverage** because of this — without it, we're guessing
about the underlying training quality.

**Deep playstyle comparison iter 19 vs iter 29** (canonical reference,
run via `analyze_eval.py --replay-dir ... --deep` on prod replays):

This is the BASELINE for distinguishing real drift from exploration.
Future evals can be compared against this trajectory. Catastrophic
forgetting would show fundamentally different patterns; exploration
shows degraded execution with intact tools.

| Category | iter 19 | iter 29 | Δ | Reading |
|---|---|---|---|---|
| **Type knowledge (FLAT)** | | | | Model still has Pokemon/move/type knowledge |
| SE Hit Ratio (flagged) | 44.6% | 43.1% | -1.5 | Type knowledge intact |
| Immune Hit Ratio | 11.8% | 11.9% | +0.1 | Stable |
| Move Waste (used resisted/immune when SE known) | 8.8% | 8.9% | +0.1 | Stable |
| Status conditions inflicted (tox/par/brn/psn total) | 762 | 723 | -5% | Same status game |
| Switch Prediction (SE vs prev opp) | 37% | 36% | -1pp | Same anticipation |
| Recovery Efficiency (useful ≥3 turns) | 70% | 69% | -1pp | Same recovery game |
| **Switch quality (small drift)** | | | | More noise in switch choices |
| Good switch (favorable matchup) | 54% | 50% | -4pp | |
| Bad switch (unfavorable matchup) | 16% | 19% | **+3pp** | Higher entropy → noisier switches |
| Avg HP at switch | 53% | 55% | +2pp | Switching earlier |
| **Strategic shift (specialization)** | | | | More hazards + grinding, less direct setup |
| Setup % | 8.1% | 5.5% | **-2.6pp** | Less direct combat-setup |
| Hazard % | 7.4% | 10.4% | **+3.0pp** | Stealth Rock count +46%, Spikes +37% |
| Switch Rate (total) | 21.7% | 24.2% | +2.5pp | More positioning |
| Voluntary Switch | 11.3% | 13.4% | +2.1pp | More proactive switches |
| Avg Turns/Game | 32.0 | 34.1 | +2.1 | Longer games |
| **Combat outcomes (real degradation)** | | | | Pressure / lead WR dropped meaningfully |
| KO Ratio | 1.37 | 1.14 | -0.23 | Worse trades, still net positive |
| Momentum dominance | 50% ours | 41% ours | **-9pp** | Real drop in sustained pressure |
| Lead WR (stays in) | 69% | 60% | -9pp | Opening play tilted |
| Lead WR (switches) | 75% | 64% | -11pp | Opening play tilted |
| Endgame WR | 57% | 54% | -3pp | Slightly worse |
| **RNG variance (not skill)** | | | | |
| Our crits | 588 | 508 | -14% | Crit luck variance against us |
| Opp crits | 716 | 767 | +7% | Same direction |

**Same Pokemon usage in both evals**: Gliscor / Ting-Lu / Alomomola /
Pecharunt / Corviknight in same order, same per-mon move distributions
(Gliscor still 30% Protect / 20% Knock Off / 19% Spikes; Ting-Lu still
Earthquake/SR dominant; etc). **Not forgetting — same toolkit, slightly
different weights.**

**Forgetting signature (NOT seen)**:
- ✗ SE rate would drop 5-10pp (we see -1.5pp — flat)
- ✗ Immune rate would spike (flat)
- ✗ Move waste would jump (flat)
- ✗ Top Pokemon usage would shift (identical top 5)
- ✗ Per-mon move distributions would shift (basically identical)
- ✗ Status game would change fundamentally (-5% total — variance)

**Exploration signature (matches what we see)**:
- ✓ Switch quality slightly noisier (+3pp bad switches)
- ✓ Momentum dominance drop (-9pp; explore = give up some pressure)
- ✓ Lead WR drop (-9pp; opening play hits rigid heuristic patterns harder)
- ✓ KO Ratio drop (still net positive 1.14)
- ✓ Strategic-play tilt (+3pp hazards, +2.5pp switching, +2 turns/game)

**Followup support — iter 32-36 trajectory** (S53 end):
- W/L recovered above 0.65 band (55.9% iter 32, **56.7% iter 34** — new high)
- Entropy MONOTONICALLY DROPPING since iter 30 (1.095 → 1.016) as
  adaptive-entropy ramps ent_coef back down. Exploration burst over.
- Iter 35 pool grew 5→6 (snapshot_0034 added). Single-iter dip to
  43.6% with 254 ties (turn-cap forfeits) — same pattern as iter 20
  and 25.
- Iter 36 already recovering (53.5%).

**For future evals (iter 39, 49, 59...) — track these vs iter 19/29 baseline**:
- SE rate: should stay in 42-46% (drop below 40% = real concern)
- Immune rate: should stay in 11-13% (jump above 15% = real concern)
- Move Waste: should stay in 8-10% (jump above 13% = real concern)
- Top 5 Pokemon ordering: should stay same (different = team usage drift)
- Bad switch rate: should stay in 16-22% (jump above 25% = noisy decision making)
- Momentum dominance: track — drops below 35% = sustained pressure loss

**If smart_avg drops further at iter 39** (e.g., to 50-55% range), check
the deep metrics above before treating as regression. If type knowledge
+ Pokemon usage hold, it's still exploration. If type knowledge degrades,
it's actual drift and warrants intervention (lower entropy band, lower lr,
or auxiliary BC loss on BC's data distribution — NOT KL-to-BC which would
cap improvement).

**Diagnostic-signal reminder** (load `feedback_diagnostic_signals.md`):
the per-worker `LIVE +Ns iter=N] n_done=X/200` counter is broken/lagging.
**Use FLOW lines + battle server logs at `/tmp/battle_server_900X.log`
as truth signals, not n_done.**


═══════════════════════════════════════════════════════════════════════════════
§D. PRIORITY-ORDERED TODO — do in this order, with rationale
═══════════════════════════════════════════════════════════════════════════════

Order matters. Importance + cost + dependency are baked into the order.

**STEP 1 (do FIRST, every session): check production status**
Cost: 30 seconds. ALWAYS the first thing you do.
```bash
ssh -i ~/.ssh/id_ed25519 -p 47913 root@195.26.233.30 \
  "date -u && grep -E 'Iter [0-9]+:.*W/L|FATAL|stale' /workspace/logs/ppo_phase1_v3_compiled.log | tail -5 && tail -3 /workspace/logs/ppo_phase1_v3_compiled.log"
```
What to look for:
- Iter line completion → still healthy
- `FATAL` or `stale_heartbeat` → run is in trouble; diagnose before touching anything
- Iter 19 EVAL line → smart_avg should be ≥67%

## SPEED TRACK — these unlock the multi-million-trajectory goal

User's framing: speed gates everything. The "elite measurement" + "infra
hygiene" items only become useful when training throughput is high
enough to make runs affordable. Speed work first, ops on the side.

**STEP 2: CIS Phase 4.6 — SHIPPED in S54 (commit `64b822a3`)** ✅

Pivoted from REBIND_WORKER (Q1 Option ii) to **Option B unified
full-reset on any failure** after dev-pod validation showed
Connection-passing-via-mp.Pipe.send hangs in this RunPod environment
(DupFd via resource_sharer either deadlocks or 30s+ slow). Honest
re-evaluation surfaced that mp_disk's 0/36-iters failure track record
+ ~$0.50-6.50/run cost made full-reset the right answer over granular
respawn. Single recovery path covers worker death AND CIS death.

What landed: CISWorkerManager (lifecycle), CISResetNeeded exception,
_orchestrator_full_reset(), retry-on-reset loops in mp_centralized_collect_sync
+ CISBgCollector. Plus latent bug fixes:
- Parent's CIS pipe FDs were never closed after worker spawn → CIS
  blocked when buffer filled on dead worker's resp pipe → reset hung
- L2371 racy reload pattern in CISBgCollector.start
- cis_server.is_alive() check (V9RLPlayer fallback was hiding CIS death)

Validation (dev pod A100 80GB):
- Phase 4.3 sustained regression: PASS
- Test 6a (kill -9 worker mid-iter): PASS — full reset → re-collect
  → all 4 worker pids changed → CIS pid changed
- Test 6b (kill -9 CIS subprocess mid-iter): PASS — same path, full
  reset triggered via cis_server.is_alive() check

See `memory/project_cis_4_6_design.md` for full design + gotchas.
Per-worker traj checkpointing was REJECTED (not deferred) per Q3 cost
analysis — see `memory/feedback_phase46_traj_checkpoint_rejected.md`.

**STEP 3: Tier 3 transition-level minibatching** — C1+C2+C3+C4 SHIPPED in S55 ✅. C5 + train_rl.py wiring + dev pod smoke pending (S56).

**Status**:
- ✅ C1 `collate_episodes()` — `f79ea477` — 6/6 unit tests PASS
- ✅ C2 batched temporal + `forward_ppo_sequence()` — `f728b3af` — 5/5 tests PASS, max diff 2.38e-07 vs sequential
- ✅ C3 `ppo_loss_batched()` — `f720dccc` — 5/5 tests PASS, B=1 bit-equivalent to per-episode
- ✅ C4 `ppo_update_batched()` — `8ce7ffcc` — 6/6 smoke tests PASS, gradient flows
- ⏳ C5 `torch.compile(train_step)` — bundled with C4, ~1 day dev next session
- ⏳ train_rl.py `--tier3` flag wiring — ~half day next session
- ⏳ Dev pod end-to-end smoke — ~$2-3 next session
- ⏳ C6 20-iter A/B vs Phase 1 v3 — runs AFTER Phase 1 v3 finishes (needs clean baseline + GPU). ~$10-15.

**Critical context retained for S56 sessions** (don't re-derive):

Rationale (why we did this): PPO update phase was bound by per-episode
batching (~48 episodes/forward). Published practice (ps-ppo, Metamon)
uses 256-2048 transitions/gradient — we were 48× below. Estimated
**4-10× update speedup** if quality holds. For 5M trajectories, this
is the difference between ~70 days and ~22-30 days post-CIS.

**Reference: Metamon's source.** `metamon_ref/metamon/rl/metamon_to_amago.py`
shows the right batch shape for transformer-temporal-with-on-policy-PPO:
- `MetamonAMAGODataset._process_data`: forms RLData with variable-length
  episodes, shape `(obs={K:(L+1,...)}, actions=(L,A), rews=(L,1), dones=(L,1))`
- `MetamonAMAGOExperiment.edit_actor_mask` / `edit_critic_mask`: pad_mask
  + missing_action_mask combine to mask loss terms per `(B, L, G, _)` batch
- `MetamonTstepEncoder` (line 477) and AMAGO's traj_encoder confirm our
  spatial + temporal split maps directly to their tstep + traj encoder

**NOT a reference: VGC-Bench** (cameronangliss/vgc-bench). Same library
stack (poke-env + PettingZoo), same LR (1e-5 base, annealing), same
self-play / FP / DO methods, same arch family (transformer encoder).
But: STATELESS per-turn — their `policy.py:AttentionExtractor.forward()`
reshapes obs to `(B, 12, -1)` with NO temporal dim. Their batch_size=512
is "free" because each transition is self-contained. Their multi-team
failure (79% W/L on 3 teams → 40% on 30 teams) is likely the stateless-
arch ceiling. Don't confuse "their LR matches ours" with "we should
copy their batching strategy" — they sidestepped the temporal-stack
batching problem by not having a temporal stack. Metamon is the right
reference because they kept temporal AND scaled batch.

**Implementation approach**: borrow the batch SHAPE from Metamon (sequence
of variable-length episodes, padded to L_max in batch, masked loss).
Don't borrow the algorithm — they use V-trace (off-policy with experience
replay), we stay strict on-policy PPO. Just bigger batches per forward.

**The work**:
- Reshape PPO update path from per-turn `(B, ...)` to per-episode `(B, L, ...)`
- Causal masking in temporal attention (no future-info leak)
- PPO loss per-timestep with pad_mask zeroing padding positions
- Trajectory storage refactor: per-turn dicts → `(B, L, ...)` tensors per episode

**Validation protocol — apples-to-apples vs Phase 1 v3** (cost: ~$10-15
A100, ~6-7 hrs):

After STEPs 2-4 land in code + Tier 3 unit tests pass, run a controlled
A/B against Phase 1 v3's known baseline. We have FULL deep-playstyle
data for Phase 1 v3 at iter 19 + iter 29 (canonical reference in §C
above). Same init + same warmup + same hyperparams = clean signal on
whether Tier 3 changed training quality.

Run config:
```
A100 80GB, second pod (don't disturb Phase 1 v3 prod pod)
--init-from data/models/bc/v10_cloud_gen9/epoch_003.pt   ← SAME init as Phase 1 v3
--warmup-iters 10                                          ← SAME warmup
--n-iters 29                                              ← run to iter 29 (matches eval point)
--cis --pipeline --bf16                                   ← full speed stack
+ all Phase 4.3-4.5 + Phase 4.4-async + ctrl pipe + CIS respawn
+ Tier 3 batching + train_step compile
Same lr, target_kl, ent_coef, adaptive-entropy band, etc.
```

**Acceptance gates — tiered by what matters most**:

| Tier | Metric | Compare against | Acceptance |
|---|---|---|---|
| **MUST hold (real degradation = abort)** | SE Hit Ratio | Phase 1 v3 same-iter | within ±3pp |
| | Move Waste | same-iter | within ±2pp |
| | Top 5 Pokemon usage ordering | same-iter | identical or ±1 position |
| | Per-mon move dist (top 3 moves) | same-iter | within ±10pp each |
| | Tests 1-3 + Test 4 sustained on Tier 3 code | dev pod | bit-exact PASS |
| **SHOULD hold (training healthy)** | W/L at iter 19 | Phase 1 v3 iter 19 (54.9% pool=2) | ±5pp at same pool size |
| | W/L at iter 29 | Phase 1 v3 iter 29 (51.8% pool=4) | ±5pp at same pool size |
| | Entropy trajectory shape | similar arc | can shift up/down 0.05, but should rise/fall similarly with pool growth |
| | KL early-stop firing | same epoch range (2-3 of 5) | clipping at trust region |
| | smart_avg at iter 19 | 69% baseline | within ±5pp |
| | smart_avg at iter 29 | 60% baseline | within ±5pp |
| **NICE to have (true improvement)** | Wall-time/iter | claim 10-15 min | actual ≤15 min |
| | smart_avg trajectory | flatter dip | might hold higher than 60% if Tier 3 has cleaner exploration |

Run analyze_eval.py with `--deep` mode at iter 19 + iter 29 of the new
run. Compare against Phase 1 v3's iter 19/29 deep playstyle reference
(documented in §C above). Match-or-exceed criteria above = Tier 3
ships to Phase 2.

**Confounds to acknowledge** (noted, not fatal):
- bf16 update vs Phase 1 v3's fp16: known small numerical drift, can
  shift specific values 1-2pp without changing qualitative behavior.
- Different rng_seed: legitimate exploration trajectory divergence.
  Comparing distributions, not exact paths.
- PFSP pool composition diverges: pool SIZES match (snapshot_interval=5
  → 2/2/3/3/3/3/3/3/3/3 then 4 at iter 24, then 4 at iter 29) but the
  specific snapshots in the pool differ. Some legitimate divergence
  expected.
- CIS+pipeline overlap effect: collect-K-1 happens during update-K,
  which is a slightly different on-policy regime. Should not change
  qualitative behavior; might shift entropy dynamics slightly.

**If A/B fails the MUST tier**: investigate before shipping. Likely
suspects: Tier 3 batching introduced a bug (loss masking wrong, episode
boundaries leaking, pad_mask dimensions off). Roll back to current
arch + revisit Tier 3 design.

**If A/B fails the SHOULD tier but MUST tier holds**: Tier 3 is
quality-preserving but training dynamics shifted. Likely fine to ship
to Phase 2 — Phase 2's 200-iter run will resolve dynamics. Document
the drift for next-time-me.

**STEP 4: `torch.compile(train_step)`** (~2-3 days, BUNDLE WITH STEP 3)
Rationale: ~10-15% additional saving on update phase by fusing forward
+ backward + optimizer step into one graph. Per-submodule forward
compile is already shipped (S51, Path 2); the train_step compile fuses
the boundaries.

**Why bundled with Tier 3**: `torch.compile` is shape-sensitive.
Variable batch shapes cause recompiles that eat the win. Tier 3's
fixed `(B, L_max)` shape gives compile what it needs. Standalone
train_step compile on the current per-turn variable-shape path would
fight against shape changes — net loss likely.

**STEP 5: Phase 2 launch with full speed stack + training-distribution fixes** (~5-7 days, ~$200-300)

After STEPs 2-4 land + parallel ops items (STEP 6+7+8), launch Phase 2:
`--cis --pipeline --bf16` + Phase 4.4 async-dispatch + 4.5 ctrl-pipe
+ 4.6 reset recovery + Tier 3 + train_step compile. Init from Phase 1
v3's final.pt (or last clean snapshot if final.pt not generated).

**CRITICAL Phase 2 design changes from Phase 1 v3** — see
`docs/PHASE1_V3_OBSERVATIONS.md` for full reasoning. Strict order:
infra optimizations FIRST, then Phase 2 launches with ALL of these:

1. **Team distribution mix** (STEP 8 below sources the teams):
   - 50-70% procedural teams (current `pokemon_usage/2024-04`) — keeps
     Pokemon-fundamentals breadth + slot-permutation invariance
   - 30-50% NEW: 100-150 real elite-player teams (separate pool, NOT the
     16 mm-competitive eval teams — those stay isolated for eval ONLY,
     no data leakage)
   - Per-battle random pick, both sides independently sample
2. **Perm/canonical eval consistency**: enable perm at eval (one-line
   config) OR document why we keep canonical-at-eval. S50 measured
   ~+7pp recovery from this fix.
3. **External opponents in PFSP pool**: mcts-fast (in-process) +
   Kakuna/SmallRL/Minikazam (after STEP 6 wires the H2H gauntlet).
   Breaks the self-play attractor that S50 + Phase 1 v3 both hit.
4. **lr scheduling** (optional but recommended): cosine decay 1e-5→1e-6
   over 200 iters, OR step decay. After Tier 3 lands, MUST re-ablate
   lr (4-point: 5e-6, 1e-5, 2e-5, 5e-5) — Tier 3 changes effective
   batch size, may justify higher lr per linear scaling rule.
5. **Absolute early-stop threshold**: `smart_avg < init_baseline - 10pt
   → stop immediately`. Catches drift sooner than relative-to-best.
6. **bf16** (`--bf16` flag) — autocast on PPO update path is in code
   (S52 commit `fb2127a3`); just needs the flag at relaunch.

Phase 1 v3 is already post-D1+D2 arch with `gen_embed.weight` baked in
(initialized at zero, never updated since gen_embed contributes a zero
vector for the gen-9-only training distribution — equivalent to "no gen
info", which is what BC v10 was trained on). BC v11 retrain is NOT
needed for Phase 2 since we stay gen-9-only.

Target wall-time: ~10-15 min/iter (vs current 55 min). Target cost:
$200-300 for 200 iters. Target smart_avg trajectory: hold ≥65% across
iter 19/29/39 evals (significant improvement over Phase 1 v3's
69→60→53). The team mix + external opps + perm fix should recover
most of the -16pp drop at iter 39 we saw in Phase 1 v3.

## OPS TRACK — parallel side work, $0 GPU, hours not days

These can be done concurrently with Speed Track. They become useful AT
Phase 2 launch but don't block earlier work.

**STEP 6: H2H gauntlet wiring vs Kakuna/SmallRL/Minikazam** (~3-4 hrs, $0)
`eval_h2h_gauntlet.py` already exists. "Wiring" = downloading 3 bot
weights/code + writing 3 small adapter classes for our
`make_self_play_opponent` factory + small local validation gauntlet.

Becomes the "are we elite?" signal we don't have today. smart_avg ≥67%
vs basic bots is necessary but insufficient — H2H against published bots
is the actual ceiling measurement. Needed at Phase 1 v3 iter 200 final
eval AND throughout Phase 2 to track elite-ness over time.

**STEP 7: Network Volume verification — DEFERRED until multi-gen (S55 user decision)**

Per user (S55): Network Volume migration is deferred until multi-gen
launch. Reasoning: Container Disk persists through pause/resume; only
voluntary stop+start (with re-provision on different host) loses data.
User monitors pod and won't stop without informing. Risk of RunPod
hardware failure / outage is non-zero (~$80 per failure event on Phase 2,
~$200+ on multi-gen) but accepted. Network Volume becomes mandatory for
multi-gen runs (5+ wk, $200+ per failure), not Phase 2.

Do NOT redo this analysis. Do NOT verify Container Disk vs Network
Volume on prod pod (waste of cycles — user has chosen). The original
"S49 lost a BC run" warning still stands but only applies if pod is
voluntarily stopped, which user won't do.

If a different user / future agent decides to pursue this for Phase 2:
the verify command is `ssh ... root@prod-pod "df -T /workspace && mount
| grep workspace"`. Don't migrate the running prod pod (would require
restart). Spin up a new pod with Network Volume from spawn for Phase 2.

This step exists in the doc for reference; it's NOT a current TODO.

**STEP 8: Source 100-150 real elite teams for Phase 2 training mix** (~3-6 hrs, $0)
Per `docs/PHASE1_V3_OBSERVATIONS.md` §2 — the team-distribution shift
is one of two unfixed S50 postmortem causes contributing to the iter 39
smart_avg drop (-15-20pp estimated). For Phase 2, augment the training
team pool with 100-150 real competitive teams from real strong players.

**Hard constraint — eval set isolation**: the existing 16
metamon-competitive teams (`metamon_cache/teams/competitive/gen9ou/`)
MUST NEVER enter the training pool. They are eval-only. Mixing them in
would be data leakage — the model would memorize them, eval inflates
artificially, eval signal lost. The 100-150 elite teams below must be
SEPARATE from these 16.

**Sources** (copy verbatim — don't generate, don't estimate, don't
synthesize):
- Smogon team archive (https://www.smogon.com/teams/) — gen 9 OU teams
  from RMTs and tournament reports
- Showdown ladder replay scraping — top-100 ladder players, gen 9 OU,
  last 30 days. Each replay has both teams in the metadata
- /r/stunfisk OU sample teams threads
- Recent OST / OLT tournament team posts
- Smogon VR (Viability Rankings) sample teams

Quality bar: every team must be one a top-100 OU ladder player ACTUALLY
USED, not a hypothetical "good team" composition. 100-150 teams gives
roughly tournament-level diversity coverage.

**Output**: pickled team list in `metamon_cache/teams/elite_training/gen9ou/`
or similar, loadable as a poke-env Teambuilder. The Phase 2 self-play
config picks per-battle: 50-70% procedural / 30-50% from this pool.
Both sides sample independently.

## DEFER — multi-gen-SPECIFIC, only when multi-gen is the next move

Per `project_strategic_frame.md`: multi-gen-SPECIFIC infra is JIT before
multi-gen launch, NOT shipped-and-cold. None of the below blocks Phase 2
(which stays gen-9-only — `gen_embed=0` baked into Phase 1 v3 + Phase 2
ckpts is mathematically equivalent to "no gen info", identical to BC v10's
training reality). Pick these up only when Phase 2 + H2H gauntlet shows
we're at the gen-9 ceiling and ready to go horizontal.

- **D4 HuggingFace replay corpus pull** (gens 6/7/8, ~$5, ~300-400 GB)
- **D5 Multi-gen `replay_to_memmap` validation** (script is gen-aware
  per `_parse_gen_from_format`, just needs running on new corpora)
- **D6 BC v11 multi-gen retrain** (A100 80GB, 5 epochs, ~5-7 days, ~$10-15)
  — only when we start training on gens 6/7/8. NOT needed for any gen-9
  vertical work (Phase 1 v3 → Phase 2 → wave training → FP → gauntlets).
  `gen_embed.weight` only acquires meaningful values when training data
  contains multiple gens; until then it stays at zero and the model
  behaves identically to a pre-D1+D2 BC ckpt for gen-9 inputs.
- **E1 per-iter format selection** (mix gens within one PPO run)
- **E2 per-gen real-battle smart bot validation** (1 hr cloud per gen)
- **E3 per-gen evaluation harness** (extends `eval_diag.py`)
- **Warmup speedup** (epochs=1 + no_grad during warmup, ~$15-25 saved
  per 20-warmup-iter run; bigger win on multi-gen with phase warmups)


═══════════════════════════════════════════════════════════════════════════════
§E. CONNECTING + MONITORING
═══════════════════════════════════════════════════════════════════════════════

**TWO POD SETUP (S55 end)**:

1. **Production pod** — Phase 1 v3 compiled run (DO NOT DISTURB, HEAD
   locked at `251cd14a`). Continuous run, no pause/stop.
   ```bash
   ssh -i ~/.ssh/id_ed25519 -p 47913 -o StrictHostKeyChecking=no root@195.26.233.30
   ```
   - Run dir: `/workspace/team_builder/pokemon-ai-starter/pokemon-ai/src/data/models/rl_v10/ppo_phase1_v3_compiled/selfplay_v9_20260507_235306/`
   - Log: `/workspace/logs/ppo_phase1_v3_compiled.log`
   - 8 battle servers on ports 9000-9007
   - Read-only monitoring only. Don't pull, don't push, don't restart.
   - At S55 end: iter 52 in collect, iter 49 EVAL=49% (within expected baseline range).

2. **Dev/validation pod (A100 80GB)** — kept up at S55 end for S56 immediate use.
   ```bash
   ssh -i ~/.ssh/id_ed25519 -p 19373 -o StrictHostKeyChecking=no root@154.54.102.26
   ```
   - HEAD: bring to latest origin (S55 end: `6758cc3c`) before any C5 / wiring work.
     Verify: `cd /workspace/team_builder && git log -1 --oneline`. If older, `git stash && git pull` (S54 captured a stash protocol; check via `git stash list`).
   - Has: torch 2.2.1+cu121 + triton 2.2.0 + poke-env 0.10.0 + Node 20 + battle_server.
   - Battle servers running on ports 9000-9007.
   - Ckpts in place: `data/models/bc/dummy_for_cis_dev.pt` (synthetic),
     `data/models/bc/v10_padded_for_cis_dev.pt` (BC v10 padded to current arch),
     `data/models/bc/v10_cloud_gen9/epoch_003.pt` (BC v10 original, source of truth)
   - Use for S56: Tier 3 C5 + train_rl.py wiring + dev pod end-to-end smoke (STEP 3 of §D);
     H2H gauntlet wiring (STEP 6); post-Phase-1-v3, C6 A/B validation.
   - Cost: ~$1.50/hr while up. Kill via RunPod console when done.

If either IP/port changes, find new in MEMORY.md or ask the user.

**Tail production log live**:
```bash
ssh -i ~/.ssh/id_ed25519 -p 47913 root@195.26.233.30 \
  "tail -F /workspace/logs/ppo_phase1_v3_compiled.log" | \
  grep --line-buffered -E "Iter [0-9]+: W/L|EVAL|smart_avg|FATAL|Snapshot|stale_heartbeat"
```

**Attach to production screen** (don't kill it!):
```bash
# Ctrl-A then D to detach without stopping the run
ssh ... root@195.26.233.30
screen -r train_compiled
```

**Check GPU + iter time**:
```bash
ssh ... root@195.26.233.30 \
  "nvidia-smi --query-gpu=utilization.gpu,memory.used,power.draw --format=csv,noheader"
ssh ... root@195.26.233.30 \
  "ps -o etime= -p \$(pgrep -f 'python -u train_rl' | head -1)"
```

**R2 sync**: there should be a `r2_sync_loop` screen mirroring
`data/models/rl_v10/ppo_phase1_v3_compiled/` to S3. If it died:
```bash
# Reattach + verify, or restart per cookbook §1e
```

**Triton check** (every fresh pod):
```bash
ssh ... "python -c 'import triton; print(triton.__version__)'"
# Expected: 2.2.0 for torch 2.2.1. If 3.0.0, run pip install triton==2.2.0
```


═══════════════════════════════════════════════════════════════════════════════
§F. RELAUNCH PROCEDURE — when the time comes
═══════════════════════════════════════════════════════════════════════════════

**Precision flag depends on scenario** (S52 update):
- **In-flight Phase 1 v3 resume from snapshot_NNNN.pt**: KEEP `--fp16`.
  Snapshot was trained with fp16; switching to bf16 mid-run changes the
  training distribution. `scripts/diag/relaunch_with_compile.md` enforces
  this.
- **Post-Phase-1-v3 relaunch / Phase 2 / brand-new run**: USE `--bf16`.
  Tier 2 bf16 (S52 commit `fb2127a3`) enables autocast on PPO update path
  → ~10-20% extra wall-time saving + cleaner numerics. Cookbook canonical
  command updated to `--bf16`.

If production needs to be relaunched (pod reboot, code upgrade, after
iter 200 final, etc.), follow `scripts/diag/relaunch_with_compile.md` for
in-flight, OR construct a fresh command from `docs/PPO_CLOUD_COOKBOOK.md`'s
TL;DR for new runs.

The canonical command structure (do NOT run blindly — always identify
the right snapshot first):

```bash
# 1. Find latest snapshot (live + R2 backup):
ls -t data/models/rl_v10/ppo_phase1_v3_compiled/selfplay_v9_*/snapshot_*.pt | head -1

# 2. Extract iter from filename:
LATEST_SNAP=...
ITER_N=$(basename "$LATEST_SNAP" .pt | sed 's/snapshot_0*//')
N_REMAINING=$((200 - ITER_N))

# 3. Verify triton + battle_servers:
python -c "import triton; print(triton.__version__)"   # expect 2.2.0
ss -ltn | grep -cE ':900[0-7]'                          # expect 8

# 4. Launch in screen:
cd /workspace/team_builder/pokemon-ai-starter/pokemon-ai/src
screen -dmS train_compiled bash -c "python -u train_rl.py \
  --resume $LATEST_SNAP \
  --pool-anchors data/models/bc/v10_cloud_gen9/epoch_003.pt \
  --device cuda --servers 9000,9001,9002,9003,9004,9005,9006,9007 \
  --fp16 --mp --mp-workers 8 --compile \
  --games-per-iter 1600 --max-concurrent 200 --opponent-device cuda \
  --n-iters $N_REMAINING --warmup-iters 0 --lr 1e-5 \
  --lam 0.95 --ent-coef 0.02 --reward-style terminal --grad-accum 1 \
  --adaptive-entropy --adaptive-entropy-low 0.65 --adaptive-entropy-high 0.95 \
  --win-rate-mode ema --win-rate-ema-alpha 0.3 --win-rate-ema-window 50 \
  --eval-interval 10 --eval-team-set metamon-competitive --eval-games 200 \
  --snapshot-interval 5 --early-stop --early-stop-patience 3 --turn-cap 300 \
  --procedural-teams /workspace/raw_data/pokemon_usage/2024-04 \
  --out-dir data/models/rl_v10/ppo_phase1_v3_compiled \
  2>&1 | tee /workspace/logs/ppo_phase1_v3_compiled.log"
```

**Verify launch banner shows**:
```
AMP dtype: fp16
optimizer: AdamW fused kernel enabled
Resumed from <snapshot>, starting at iter <N>, pool: K checkpoints
torch.compile: 5/5 submodules compiled (mode=default, dynamic=True)
Iters: <N_REMAINING>, Games/iter: 1600, Concurrent: 200
FP16: ON, Compile: ON, Pipeline: OFF, Collect: MP, Device: cuda
```

If any of those is missing, abort + diagnose.


═══════════════════════════════════════════════════════════════════════════════
§G. VALIDATION PATTERNS — how to know your work is correct
═══════════════════════════════════════════════════════════════════════════════

The cookbook §5 has the canonical 6-test pattern. Quick reference:

| Test | Scope | Acceptance |
|---|---|---|
| 1. Logits identity | 100 fixed batches, compare main vs (worker / CIS) forward | max abs diff < 1e-3 (allows fp16 noise) |
| 2. Data flow | pickle.gzip Trajectory round-trip | All fields exactly equal |
| 3. Numerical equivalence | 1-iter `--mp` vs `--pipeline` vs `--cis` | Iter line within 2σ noise |
| 4. 5-iter sustained | `--mp --mp-workers N` at games=200 | No NaN; v_loss descending; smart_avg ≥ BC baseline |
| 5. Wall-time smoke | 3-iter at games=1500-1600 production scale | Iter 0 ~13-14 min, iter 1+ ~12-13 min steady |
| 6. Failure recovery | kill -9 a worker mid-iter | Watchdog respawn; iter completes; subsequent iters normal |

**Existing test scripts in `scripts/diag/`** (reuse, don't recreate):
- `test_compile_new_arch.py` — compile equivalence + variable batch + backward
- `test_cis_phase1.py` — CIS logits identity (Test 1 for CIS)
- `test_cis_phase2.py` — multi-worker CIS batching + cross-worker logits
- `test_cis_phase3.py` — CIS weight reload round-trip
- `test_d1_gen_token.py` — gen-id token cross-gen output diff
- `test_d2_gen_aware_features.py` — gen_id batch plumbing
- `test_d3_per_gen_teambuilder.py` — per-gen team validation

**For CIS Phase 4.3**, write `test_cis_phase4_overlap.py` — verify bg
overlap actually overlaps (collect during update wall time saved).


═══════════════════════════════════════════════════════════════════════════════
§H. CODEBASE ORIENTATION
═══════════════════════════════════════════════════════════════════════════════

Source layout: `pokemon-ai-starter/pokemon-ai/src/`

**Hot files (production critical, touch carefully)**:
- `train_rl.py` — PPO training entry point. Argparse, dispatch, main loop.
  - `_collect_data` routes to --cis | --mp | sync collect
  - `_start_background_collection` is where bg overlap re-enables (Phase 4.3)
- `model_transformer.py` — TransformerBattlePolicy + Tokenizer (~20M params)
  - `N_BATTLE_STATE = 15` (gen-id token at index 14) post-D1
  - 5 submodules torch-compiled in production: tokenizer, spatial,
    temporal, action_head, value_head
- `mp_disk_collect.py` — production --mp path. Heartbeat mitigations live.
  POKE_LOOP threading rule applies (cookbook §3j).
- `mp_centralized_collect.py` — CIS path (--cis). Phase 1-4.2 done;
  Phase 4.3 work goes here.
- `inference_batcher.py` — InferenceBatcher (used in --mp path).
- `precision_config.py` (new) — global amp dtype helper. autocast_ctx() +
  set_amp_dtype(). Used by all autocast sites (battle_agent, inference_batcher,
  rl_pipeline, mp_collect_v3).
- `features.py` — `make_features` produces gen field; `build_turn_batch`
  emits batch["gen_id"]. Per-gen feature gating is implicit via poke-env.
- `team_generator.py` — gen-aware. Validated for gens 6/7/8/9.

**Cold files (don't modify, preserved for reference)**:
- `mp_collect_v2.py`, `mp_collect_v3.py` — legacy multi-process paths,
  no longer reachable from --mp. Don't use.
- `legacy/` — pre-transformer arch + experiments. Reference only.
- `backups/` — point-in-time copies.

**Eval files**:
- `eval_diag.py` — smart-bot eval (SH/SmartDmg/Tactical/Strategic)
- `eval_elo_ladder.py` — Bradley-Terry Elo
- `eval_metamon_competitive.py` — fixed 16-team eval
- `eval_h2h_gauntlet.py` — head-to-head champion gauntlet

**Test scripts**: `scripts/diag/` (see §G above)


═══════════════════════════════════════════════════════════════════════════════
§I. LOAD-BEARING RULES + KNOWN GOTCHAS
═══════════════════════════════════════════════════════════════════════════════

**Cookbook §3j: POKE_LOOP threading**. `PSClient._listening_coroutine` is a
`concurrent.futures.Future` running in a SEPARATE thread (poke-env's
POKE_LOOP), not an asyncio.Task in your loop. `cancel()` is fire-and-forget
across threads. Sequential `_cancel_listener + del player + del opponent`
patterns MUST yield wall-clock via `await asyncio.sleep(1.5)` afterward,
or POKE_LOOP backs up and the worker hangs at 99% CPU. Rule applies any
time you create poke-env Players in a sequential await pattern.

**Cookbook §3k: Heartbeat starvation at iter boundary**. Workers'
synchronous `torch.load` blocks asyncio; async heartbeat coroutine can't
fire; main respawns "stale" workers. Mitigations live: 600s timeout,
0.25s stagger, liveness-thread heartbeats. Rule: any blocking syscall in
asyncio context needs a separate-OS-thread liveness signal.

**`_orig_mod` prefix on compile-wrapped state_dicts**. `torch.compile(module)`
adds `_orig_mod.` to that module's state_dict keys. `load_checkpoint`
strips it (ppo.py:397); `_resume_from_checkpoint` strips it (after Session
51 fix in `251cd14a`). When writing new code that loads a state_dict,
ALWAYS strip the prefix. Use `strict=True` so mismatches surface loudly,
not silently leave half-loaded state.

**torch.compile must happen AFTER resume**. Because compile changes the
state_dict key space, compile() must be applied AFTER `load_state_dict`.
train_rl.py is structured this way (Session 51 `251cd14a`). Don't move it.

**spawn context, not forkserver**. mp_disk_collect uses spawn explicitly
to dodge the CPython 3.11 SemLock race at N≥4 forkserver workers. Don't
switch back.

**`--mp` requires transformer arch**. Legacy BattleAgent ckpts (sp_NNNN)
work as PFSP opps via `make_self_play_opponent` factory dispatch but
cannot be `--init-from` for `--mp`.

**vm.max_map_count read-only on RunPod**. Bypass with mp.Pipe (FD-only,
no SemLock). All current code already does this.

**TF32 + cudnn.benchmark already enabled** at module top of train_rl.py
(matches train_bc.py). Don't re-enable elsewhere.

**Adaptive-entropy is ON by default in production**: low=0.65, high=0.95.
Currently entropy runs ~1.13-1.17, ABOVE the band — adaptive logic is
reducing ent_coef in response. This is normal; don't try to "fix" it.


═══════════════════════════════════════════════════════════════════════════════
§J0. SESSION 53 — GAINS, METRICS, NON-ACTIONS
═══════════════════════════════════════════════════════════════════════════════

**Gains (10+ commits + 2 memory files, all on origin/master)**:
- `92caecd3` CIS Phase 4.3a: pool-mirror multi-slot + Tests 1-3
- `3accb9fd` Per-handle Lock around pipe IPC + Path/random imports + Test 4
- `475b32d1` CIS Phase 4.3b: CISBgCollector + bg overlap re-enable
- `4f0a292f` CIS Phase 4.3c: small-scale wall-time A/B (22.8% saving)
- `5b6fbdc6` next-prompt: Session 54 handoff
- `1fca242a` docs: incorporate prod-scale 4.3c results + remove stale CIS-pending statements
- `3cd924cb` test_cis_phase4_3c_walltime: add --cis-min-batch CLI flag
- `4d901830` CIS Phase 4.4: async-with-req_id-dispatch (~20% collect speedup, prod-scale measured)
- `02c2f3c9` docs: Phase 4.4 outcome + S54 priorities
- `b4e60bce` diag: pad_bc_v10_to_d1d2_arch script (BC v10 → current arch warm init)
- `7a70eeb2` docs: re-prioritize speed track first + remove stale references
- (Phase 4.5 ctrl-pipe fix expected to commit after validation; see below)
- Production-scale 4.3c + 4.4 on A100 measured. Pre-refactor (4.3) GPU at
  ~48% util lock-bound, iter 0 collect = 649s. Post-refactor (4.4) GPU
  saturating, iter 0 collect = 520s (-20%).
- Padded BC v10 init (synthetic warm-from-trained, gen_embed zeroed):
  iter 0 collect = 430s — battles end naturally faster (BC v10 plays
  competently → ~30-50 turns/battle vs ~150-200 turns with random init).
  W/L 254/146 (63.5%) shows real skill differential, not 50% noise.
- Updated memory: `project_cis_4_3_design.md`, `feedback_cis_thread_safety.md`

**Architectural change from S52 memo** (CRITICAL — read carefully):
S52 memo proposed strict 2-slot CIS (slot 0 = player, slot 1 = active opp).
S53 honest re-read found this **doesn't compose** with the existing
`asyncio.gather` opp concurrency in `_run_collect_in_worker_cis` —
concurrent opps on different workers would race on slot 1's reload,
producing silent training corruption. Two resolutions:
(a) serialize opps across workers — kills concurrency
(b) **Pool-mirror slots (Option D, what shipped)** — slot 0 = player,
    slots 1..K_max=16 mirror the PFSP pool. Each PFSP entry gets a stable
    slot index. Cross-worker batching preserved per slot. Lazy reload
    (only when path changes). ~3% of A100 80GB GPU memory at K=16.

**Tests + measurements (all PASS)**:
- Test 1 slot identity (K=2): bit-exact 0.0 max abs diff per slot
- Test 2A reload round-trip: bit-exact 0.0
- Test 2B slot isolation (reload slot 1, slot 0 unchanged): bit-exact 0.0
- Test 3 multi-worker partition (K=3, N=4): 12/12 within fp16 noise (<5e-3)
- Test 4 sustained smoke (2 iters real battles): 2/2 PASS, real PFSP visible
- 4.3b BG overlap (start/join semantics, 2 iters): 2/2 PASS
- 4.3c small-scale wall-time A/B: **22.8% saving, 77.3% overlap efficiency**

**Latent bugs fixed alongside** (good hygiene):
- `CISClientHandle.infer` now accepts `history` kwargs. Was broken pre-S53
  — `submit` was passing `history=` but `infer` didn't accept it. Would
  have errored on first real V9RLPlayer turn submit.
- `from pathlib import Path` + `import random` added at module top of
  `mp_centralized_collect.py`. Caused NameError surfaces in 4.3b
  orchestrator path that wasn't exercised before.

**CIS thread-safety issues surfaced + fixed (THREE iterations)**:
- **Fix 1 (Phase 4.3, S53)**: Test 4's first end-to-end run hit
  `_pickle.UnpicklingError: invalid load key` + Broken pipe. Root cause:
  `mp.Pipe.send` is not byte-atomic across threads; concurrent submits
  from one worker (player + opp at minimum) corrupted the pickle stream.
  Fix: per-handle `threading.Lock` wrapping each (send, poll, recv) cycle.
- **Fix 2 (Phase 4.4, S53)**: worker's recv-loop thread races with
  PARENT's `cis_handles[wid].reload()` reads at INIT time. Both processes
  have read access to the same resp pipe. If worker's recv_thread starts
  at __init__, it eats parent's reload responses → "invalid load key"
  on parent. Fix: `_cis_worker_main` constructs handle with
  `start_recv=False`, calls `cis_handle.start_recv_loop()` only on first
  `collect_iter` cmd (by which time parent's reload window has closed).
- **Fix 3 (Phase 4.5, S53)**: SAME race re-emerges at ITER 1+ boundaries
  — once worker's recv_loop is running (started at iter 0's first
  collect_iter), parent's reload at iter 1 start hits the same shared
  pipe and worker steals the response. Symptom: "untagged response
  dropped" warning in worker + reload timeout in parent + corrupted
  slot state. Fix: dedicated CTRL pipe between parent and CIS subprocess.
  CISServer now creates a ctrl pipe pair at spawn(); CIS service loop
  reads from worker pipes AND ctrl pipe via mp_wait. Reload calls go
  via `cis_server.reload_weights()` which routes via `_ctrl_handle`
  (sync mode, parent-only). Worker procs DO NOT have read access to
  ctrl pipe → race-free. **Use `cis_server.reload_weights()`, NOT
  `handles_main_view[0].reload()`** — see B13.
- All three fixes shipped + prod-validated. See `feedback_cis_thread_safety.md`.

**Production observations from S53**:
- Phase 1 v3 healthy through iter 22, iter 23 in update at S53 end
- Iter 19 EVAL: smart_avg=69%, passed ≥67% baseline gate
- W/L peaked at 54.9% (iter 19), dipped to 47.2% at iter 20 (pool grew),
  recovered to 53.7-53.8% by iter 22. Pool growth is expected stress.
- Heartbeat mitigations holding through pool growth boundary. No respawns.
- Avg post-compile no-eval iter: ~3300s = 55 min (-25% vs uncompiled 4423s)

**Production-scale 4.3c A/B (A100 80GB, S53)** — measured, not projected:
- N=8, max_concurrent=200, games=400, min_batch=8, 2 iters per variant
- Phase 4.3 Variant A (Lock-based sync): iter 0 = 649s, iter 1 = 539s, total 1548.6s
- Phase 4.3 Variant B (Lock-based pipeline): iter 0 sync = 667s, iter 1 join_wait = 388s, total 1415s
- Phase 4.3 Lock-based GPU peak: ~48% utilization (lock-bound)
- **Phase 4.4 (async-dispatch) Variant A iter 0 = 520s** vs 4.3's 649s
  → **~20% collect-time speedup** at production scale, GPU util saturating
- All correctness gates PASS at prod scale: no thread-safety bugs at conc=200,
  no UnpicklingError, all req_id matched correctly to futures, trajs collected
  cleanly with proper W/L (210/190 in 4.4 iter 0)

**Wall-time projection — full speed stack** (full prod scale, 1600 games/iter):

| Stack | Iter time | Saving vs current `--mp` |
|---|---|---|
| Current `--mp` no-pipeline (Phase 1 v3) | 54 min | — |
| `--cis --pipeline` Phase 4.3 (Lock-based, shipped S53) | ~40 min | ~26% |
| **`--cis --pipeline` Phase 4.4 + 4.5 (shipped S53)** | **~38 min** | **~30%** |
| + bf16 update autocast (already in code, needs `--bf16` flag at Phase 2 launch) | ~32 min | ~41% |
| + Tier 3 transition-level minibatching (C1-C4 SHIPPED S55; C5+wiring+smoke pending S56) | ~12 min | ~78% |
| + `torch.compile(train_step)` (Tier 3 C5, bundled with C4 — pending S56) | ~10 min | ~82% |

**For 5M trajectory goal (3125 iters at games=1600):**
- Current trajectory (`--mp`): 108 days, ~$3,900
- Post-CIS+pipeline+bf16: ~70 days, ~$2,500 (saves ~38 days, ~$1,400)
- Post-Tier-3 + train_step compile: **~22-26 days, ~$800-1,000** (saves
  ~80 days, ~$2,900 from current)

The full speed stack is the difference between "long but feasible" and
"actually nice." Each lever multiplies — CIS unlocks pipeline overlap,
Tier 3 attacks update phase, train_step compile makes Tier 3 even faster.


═══════════════════════════════════════════════════════════════════════════════
§J. SESSION 51 — GAINS, METRICS, FAILURES
═══════════════════════════════════════════════════════════════════════════════

**Gains (10 commits)**:
- `d5f500b8` Tier 1: per-submodule torch.compile + fused AdamW + --bf16 flag
- `50d4e80a` CIS Phases 1-3 (scaffolding + multi-worker + weight reload)
- `f41c38e6` Docs: CIS status + relaunch procedure
- `fd88d552` mp_disk heartbeat mitigations (3 of them)
- `251cd14a` train_rl: compile-after-resume + _orig_mod strip
- `8128beaa` CIS Phase 4 partial: worker entrypoint + orchestrator + history-batched forward
- `fd878a9b` D1 gen-id token in TransformerBattlePolicy
- `e6b0406c` D3 per-gen teambuilder validation
- `17e2d111` D2 gen-aware feature pipeline (gen_id plumbing)
- `21484fcc` CIS Phase 4.2: --cis flag + low-priority CUDA stream
- `f3728dcc` next-prompt + cookbook updates

**Metrics**:
- torch.compile speedup: 30.3% per-iter (iter 14 uncompiled 4423s vs
  iter 15 compiled 3083s). Update phase saw 37.5% saving.
- Cost saved: ~$50-75 over remaining 185 iters at $1.50/hr.
- Compile microbench (synthetic B=8 fp16 A100): 1.12-1.25× full forward,
  2.49× cudagraph-cached forward_spatial only.
- CIS Phase 1 logits identity: 0.0 max abs diff (bit-exact)
- CIS Phase 2 cross-worker batching: 5.72e-6 fp32, 7.81e-3 fp16 (= 1 fp16 ULP at log scale)
- CIS Phase 3 weight reload round-trip: 0.0 max abs diff bit-exact
- D1 cross-gen output diff (gen 9 vs gen 6): action_logits 9.15e-4
  (small but real conditioning)
- bf16 smoke: clean (kl=0.0285, no NaN)
- D3 per-gen teambuilder: 4 gens × 5 teams each PASS (no banned mons,
  no parse errors)
- E2 per-gen smart bot construction: 16/16 PASS

**Failures + recoveries**:
- **Iter 17 hang (original resumed run)**: 7-of-8 then 8-of-8 workers
  stale-heartbeat at pool-growth boundary. Run hung 7+ hours at 0% CPU
  in shutdown cleanup. Root-caused as torch.load blocking asyncio
  heartbeat coroutine. 3 mitigations shipped + validated.
- **First compiled relaunch state_dict mismatch**: forgot to move
  torch.compile() AFTER resume. Caught by launch banner crash. Fixed
  in `251cd14a` (compile-after-resume + _orig_mod strip).
- **CIS Phase 3 `_orig_mod` prefix bug**: silent strict=False masking
  caused half-loaded model state on reload-back-to-original. Caught by
  the test's bit-exact round-trip assertion. Fixed in same commit.

The pattern: every failure was caught by an early validation step
(launch banner, bit-exact test, smoke). **Don't skip validation.**


═══════════════════════════════════════════════════════════════════════════════
§K. DEAD PATHS (things tried + abandoned — DO NOT retry)
═══════════════════════════════════════════════════════════════════════════════

These have been investigated and rejected. Don't waste cycles re-walking:

- **`--opponent-device cpu`**: tried Session 50, broke at production scale
  (CPU 20M-param forward × conc=200 exceeds WS keepalive timeout).
  Cookbook §3h. Stay on `--opponent-device cuda`.
- **forkserver mp context**: SemLock race at N≥4 workers (CPython 3.11
  bug). Use spawn instead.
- **Torch tensor IPC across mp.Pipe**: mmap explosion at production scale.
  Numpy IPC only.
- **`--compile` on the OLD legacy arch**: not supported (different forward
  shape). Only the new TransformerBattlePolicy supports compile.
- **mode="reduce-overhead" for full forward**: cudagraph aliasing breaks
  on multi-module patterns + recompile churn at variable shapes. Use
  mode="default" for production, `dynamic=True`, `_dynamo.config.suppress_errors=True`.
- **Path 1 single-method compile** (`forward_spatial` only): covers ~40%
  of the path. Path 2 (per-submodule, all 5 modules) is the right answer.
  Don't revert.
- **Standalone `grad_accum > 1`**: Session 31 record suggests silent
  quality regression. Don't experiment standalone — Tier 3 (transition-
  level minibatching, §D STEP 3) SUBSUMES it via batch shape change.
- **`lr=3e-5`**: caused regression on transformer arch. lr=1e-5 won the
  Session 50 4-point ablation. Hold it.
- **Explicit per-gen feature gating in code**: investigated Session 51 D2.
  poke-env's API already returns False/0 for unavailable mechanics per
  gen. Adding code-level gates is dead weight.
- **Gigantamax / Doubles formats in V1**: out of scope. Tokenizer asserts
  n_active=1 (singles). Multi-active = future redesign per
  REWRITE_DESIGN.md.
- **Dual-slot CIS (S52 memo Option (i))**: explicitly tested, doesn't
  compose. The asyncio.gather opp concurrency in
  `_run_collect_in_worker_cis` means workers play multiple opps in
  parallel; with strict 2-slot, concurrent opp inferences race on
  slot 1's reload causing silent training corruption. S53 replaced
  with **pool-mirror slots** (1 player + K opp slots, K=pool size).
  Don't reopen this — pool-mirror is strictly better at every axis
  except GPU memory (which is trivial: ~3% of 80GB at K=16).
- **Removing CISClientHandle thread-safety**: BOTH paths in the code are
  load-bearing. Async-with-req_id-dispatch (Phase 4.4, default in
  workers) handles concurrent IPC; the per-handle Lock (Phase 4.3,
  retained in CISServer parent-side) handles serial reload calls.
  See `feedback_cis_thread_safety.md`. mp.Pipe.send is not byte-atomic;
  one path or the other is required.
- **Worker-side recv_loop starting at __init__ time**: races with
  parent's reload reads on the same pipe → "invalid load key"
  UnpicklingError. `_cis_worker_main` MUST defer `start_recv_loop()`
  until first `collect_iter` cmd is received. Don't move it earlier.
- **Routing parent reloads through worker handles** (e.g.,
  `handles_main_view[0].reload(...)` from Phase 4.4): races with worker
  recv_loops at iter 1+ boundaries. Worker eats parent's reload
  response (logged as "untagged response dropped"); parent's sync recv
  times out. Phase 4.5 added a dedicated ctrl pipe — use
  `cis_server.reload_weights()` which routes via `_ctrl_handle`
  (parent-only, no worker access).
- **VGC-Bench as a Tier 3 reference** (their LR matches ours, they use
  PPO + self-play, batch_size=512 transitions): explicitly reviewed
  and rejected as the right reference. They are STATELESS per-turn
  (no temporal stack — confirmed in their `policy.py` where
  `AttentionExtractor.forward()` reshapes to `(B, 12, -1)` with no
  temporal dim). Their batching freedom comes from architectural
  simplicity we deliberately don't share. Their multi-team failure
  mode (79% W/L on 3 teams → 40% on 30 teams) likely IS due to
  stateless arch hitting a ceiling. **Use Metamon's
  `metamon_to_amago.py` as the Tier 3 reference instead** — they have
  temporal + transformer + the right batch shape (sequence-batched
  with pad_mask).


═══════════════════════════════════════════════════════════════════════════════
§L. AUTHORITATIVE DOCS BY TOPIC
═══════════════════════════════════════════════════════════════════════════════

Read these first when you touch the relevant area:

- **Cloud PPO runs**: `docs/PPO_CLOUD_COOKBOOK.md` (single ref)
- **mp-disk arch**: `docs/MP_DISK_REDESIGN.md`
- **CIS arch**: `docs/CENTRALIZED_INFERENCE_DESIGN.md`
- **Multi-gen scope**: `docs/MULTIGEN_FEASIBILITY.md`
- **Cloud BC training**: `docs/CLOUD_RUNBOOK.md` §3-5 (BC-specific)
- **Phased curriculum**: `docs/PPO_PHASED_TRAINING.md`
- **Memory**: `memory/MEMORY.md` + topic-specific files

**Historical / outdated** (don't use for current decisions):
- `docs/PHASE1_POSTMORTEM.md`, `docs/PHASE1_DIAGNOSIS_REPORT.md`,
  `docs/PHASE1_INVESTIGATION_PLAN.md`, `docs/PHASE1_V2_CLOUD_CHECKLIST.md`
- `docs/ARCH_AUDIT.md`, `docs/STATUS.md`, `docs/RESEARCH.md`
- `docs/CLOUD_RUNBOOK.md` §11 (superseded by cookbook)

When docs and code disagree, trust the code. Use `git log --oneline`
to find the relevant commit + read its message for context.


═══════════════════════════════════════════════════════════════════════════════
§M. THE STARTUP ROUTINE (verbatim, do this exactly — S55+ updated)
═══════════════════════════════════════════════════════════════════════════════

1. Read `memory/MEMORY.md` (auto-loaded) — has the ⚠️ READ FIRST list at top.
2. Read `memory/project_session_boot_protocol.md` (or `docs/SESSION_BOOT_PROTOCOL.md`) — STANDING ORDERS. Internalize.
3. Read this entire file (`next-prompt.txt`). Yes, all of it.
4. Read `docs/PHASE1_V3_OBSERVATIONS.md` — Phase 2 design + iter 39/49 EVAL diagnosis.
5. Run the production status check (§E).
6. If production is healthy AND not at iter 200 yet: headline work is **Tier 3 C5 + train_rl.py wiring + dev pod smoke** (per S55 wrap above + boot protocol §4 + §8). Plan + discuss with user before coding. **H2H gauntlet wiring** (STEP 6) is parallel side work — interleave when Phase 1 v3 is ~1 day from finish (iter 195+).
7. If production has FATAL or stale-heartbeat: diagnose first. `feedback_iter17_heartbeat_hang.md` is the playbook.
8. If production is done (iter 200 reached): pull final.pt, run full eval suite (`eval_diag.py` + `eval_metamon_competitive.py` + H2H gauntlet), document final smart_avg + Elo + per-bot + H2H. Then **C6 Tier 3 A/B validation** (~$10-15 dev pod) + easy items JIT (perm fix + lr sched + source 100-150 elite teams + bf16 smoke) + **Phase 2 launch** with full stack.
9. Do NOT pivot to: multi-gen-SPECIFIC items (D4-D6, BC v11, E1-E3 — POST-Phase-2 only); doubles/VGC/triples/AG/in-game (POST-multi-gen only); Network Volume migration (deferred per user, see §D STEP 7); Easy items before Tier 3 ships.
10. Don't start coding before step 6-or-7-or-8's outcome is clear.

If you find yourself confused or context-loaded: ASK THE USER. Do not
guess. Past sessions confirmed "going in half-informed" is the pattern
that costs most. (See `feedback_ask_when_uncertain.md`.)

═══════════════════════════════════════════════════════════════════════════════
§N. EVAL EXPECTED OUTCOMES — what numbers mean
═══════════════════════════════════════════════════════════════════════════════

The compiled run hits its first eval at iter 19 (`--eval-interval 10`,
resumed-run inherits this). Run is 200 games × 4 smart bots = 800 games
per eval. Standard error ≈ ±3.5% per bot.

**smart_avg = average win rate across all 4 smart bots
(SimpleHeuristicsPlayer / SmartDamagePlayer / TacticalPlayer /
StrategicPlayer)**.

| smart_avg range | Interpretation | Action |
|---|---|---|
| ≥75% | Strong improvement over BC baseline | Continue, watch for plateau |
| **≥67%** | **Healthy: at or above BC v10 baseline** | **Continue (this is the target)** |
| 60-67% | Acceptable but flat — model not learning much vs baseline | Continue, but plan to re-evaluate at iter 29 |
| 50-60% | Concerning — possible quality regression | **Investigate**: kl trajectory rising? entropy collapse? gradient norms? Compare iter 19 (compiled) to iter 13 (uncompiled baseline) |
| <50% | Compile broke training quality | **Abort + rollback** to snapshot_0014 + relaunch without `--compile`. Cookbook §3i acceptance was end-to-end equivalence; if eval breaks, the equivalence assumption was wrong |

**BC v10 e3 reference (from Session 49)**: smart_avg ≈ 67%, Elo 1135.9
(measured, #1 all-time at the time). The compiled PPO run starts from
snapshot_0014 (already trained 14 PPO iters on top of BC). Expectation
is smart_avg should be ≥ BC baseline since we have additional training,
but PPO can plateau or temporarily dip vs BC if exploration gets noisy.

**Per-bot expected ranges** (can vary, these are rough):
- vs SimpleHeuristicsPlayer (SH): 70-85% (easiest)
- vs SmartDamagePlayer: 60-80%
- vs TacticalPlayer: 55-75%
- vs StrategicPlayer: 55-75% (hardest)

If ONE bot tanks to <50% while others hold, it's specialization (fine).
If TWO+ bots tank, that's concerning. Cookbook §5e has the kl/pi/v
trajectory diagnostics for these cases.

**For multi-gen runs (Session 52+)**: smart_avg-per-gen tracking is
essential. Don't average across gens — each gen has its own difficulty
calibration. See E3 in §D STEP 4.


═══════════════════════════════════════════════════════════════════════════════
§O. CARRY-OVER PRACTICES (lessons paid for in $$$ + wall-time)
═══════════════════════════════════════════════════════════════════════════════

Distilled from Sessions 49 / 50 / 50 cont. / 51. Each one cost real money
or hours when ignored. Don't relearn them.

1. **Read docs + code BEFORE estimating scope.** Sessions 49 + 50 + 51
   each burned evenings on "should be a quick fix" estimates. Trust
   line-cited audits. The compile diagnostic took ~2 hours of investigation
   before the right fix landed; the wrong fix would have been ~1 day.

2. **Verify before recommending from memory.** Memory snapshots are
   point-in-time, not current state. Grep for functions/flags before
   promising they work. The "AttributeError on new arch" claim was wrong
   in three places (cookbook + memory + next-prompt) — believing memory
   over the code wasted ~1 hour of investigation.

3. **Smoke ALWAYS before launching long runs.** The 6-test pattern in
   cookbook §5 is non-optional. Session 50 caught 5 bugs (V9RLPlayer
   kwarg, return-tuple shape, opp_records format, mp+pipeline GPU
   contention, --opp-cpu WS keepalive). Session 51 caught 3 more
   (compile-vs-resume order, _orig_mod prefix, heartbeat starvation).
   Each would have wasted 40+ hr in production.

4. **`--mp` is transformer-only.** Legacy `BattleAgent` ckpts (sp_NNNN)
   work as PFSP opponents (factory dispatches via
   `is_transformer_checkpoint`) but cannot be `--init-from` for `--mp`.

5. **Cloud: Network Volume MANDATORY for runs > 1 epoch.** Container
   Disk doesn't survive `podStop+podResume` if the pod re-provisions on
   a different host. Lost a partial BC run this way (Session 49). This
   matters for BC training (long jobs); for PPO production runs we use
   continuous training without stop/resume so it's lower risk, but new
   long-running jobs should default to Network Volume.

6. **Don't rush refactors under time pressure.** "Just ship tonight" is
   the pattern that produces next session's surprise gaps. Session 51's
   "go ahead with Path 2 properly" was the right call exactly because we
   refused to ship Path 1's incomplete coverage.

7. **Backward compat is a hard constraint** (until D1 broke it deliberately).
   Legacy ckpts must remain loadable as PFSP opponents — the
   `make_self_play_opponent` factory-dispatch invariant protects this.
   The D1 gen-id token addition deliberately breaks BC v10 → multi-gen
   model compatibility, which is OK because BC v11 retrains from scratch.

8. **Cloud-only bug discovery**: many bugs (SemLock race, GPU contention,
   heartbeat starvation, the iter-17 hang) only reproduce on Linux +
   RunPod + N≥4 workers + production scale. Schedule cloud debug time
   when context is fresh — these don't reproduce locally.

9. **POKE_LOOP threading rule** (cookbook §3j): any sequential code path
   in this codebase that creates poke-env Players AND calls
   `_cancel_listener` MUST yield wall-clock time afterward via
   `await asyncio.sleep(1.5)`. The `asyncio.gather` pattern is fine
   without this; sequential `await` is not. Caught the hard way in
   Session 50 cont. iter 10 hang.

10. **Heartbeat from a separate OS thread, not from asyncio coroutines**
    (cookbook §3k): when a worker does any blocking syscall (torch.load,
    big disk read, model copy), the asyncio loop is paused — async
    heartbeat coroutines can't fire. Liveness signaling MUST come from
    a thread that runs independent of asyncio. Session 51 iter 17 hang
    cost 7+ hours before this was fixed.


═══════════════════════════════════════════════════════════════════════════════
§P. COST AWARENESS — read `docs/COST_LEDGER.md`
═══════════════════════════════════════════════════════════════════════════════

The pod is paid time. Every choice is also a cost choice.

Quick rules:
- Cloud: $1.50/hr A100 80GB on RunPod
- Validation smoke at small scale: ~$1-3 each. **Always run before $60+ launch.**
- Hung run with 0% CPU: still costs $1.50/hr. Diagnose + kill cleanly.
- Per-iter Phase 1 v3 compiled cost: ~$1.30 per iter (~50 min × $1.50/hr)
- Multi-gen 5-7 week run estimate: $600-900 (with CIS Phase 4.3 + flash)

Update `docs/COST_LEDGER.md` at the end of each session: hours used,
$ spent, major outcomes. If a major optimization shipped, replace
estimated gains with measured production numbers.


═══════════════════════════════════════════════════════════════════════════════
END SESSION 54 BOOT — good luck
═══════════════════════════════════════════════════════════════════════════════
