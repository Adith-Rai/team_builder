# Pokemon AI requirements — pinned to working Windows env (Session 48).
# Linux/cloud should install with CUDA 12.1 wheels (RunPod's standard A100 template).
#
# RunPod PyTorch 2.x template already has torch + CUDA pre-installed; in that case:
#   pip install -r requirements.txt --no-deps  (and trust the pre-baked torch)
# Otherwise install torch first explicitly:
#   pip install torch==2.2.1 torchvision==0.17.1 torchaudio==2.2.1 \
#       --index-url https://download.pytorch.org/whl/cu121

# --- Core training ---
torch==2.2.1
torchvision==0.17.1
torchaudio==2.2.1
numpy==1.26.4
tensorboard==2.20.0

# --- Pokemon engine + battle env ---
poke-env==0.10.0
poke-engine==0.0.46

# --- Data / config ---
pyarrow==23.0.1
orjson==3.11.7
PyYAML==6.0.1

# --- Utilities ---
tqdm==4.67.3
requests==2.32.5
websockets==15.0.1

# --- Optional ---
einops>=0.7.0
gin-config>=0.5.0
