# Minimal external_adapters config for the train_rl.py smoke test.
# One fast MCTS opponent — confirms PoolEntry path works end-to-end.

opponents:
  - name: mcts-fast
    type: mcts
    search_time_ms: 80
    weight: 1.0
